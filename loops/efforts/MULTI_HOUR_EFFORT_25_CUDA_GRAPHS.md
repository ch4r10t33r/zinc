# Effort 25 — CUDA Graphs (kill the per-launch overhead; helps decode AND prefill)

> **Status:** 🔬 OPEN. The highest-value CUDA lever found 2026-06-13. Supersedes Effort 24 (batched prefill — proven DEAD END below).

Date: 2026-06-13. Forward paths: `src/compute/forward_cuda.zig` (qwen35/36 hybrid-SSM) + `forward_cuda_gemma.zig` (gemma4). Command/stream layer: `src/cuda/command.zig`, `src/cuda/cuda_shim.c`.

## Why this effort (the finding that redirects everything)

Decode AND prefill on this hardware are **LAUNCH-BOUND, not weight/compute-bound.** Profiled (nvidia-smi DURING a batched gemma-31b prefill, T=413): **full clock 2422 MHz but only 7–12% GPU util / ~76 W of 575 W — the GPU sits ~90% idle.** qwen decode profiled the same (10–12% util). The bottleneck is the **~480-kernel-per-step launch chain** (≈60 layers × ~8 kernels: norms, Q/K/V matvecs, attention, O, FFN), whose per-kernel launch + dependency latency dwarfs each kernel's compute.

**This is why Effort 24 (batched prefill) is a DEAD END:** the batched GEMM (5.9× isolated) and `attention_causal_batched` (6–15× isolated) are fast but were *never the bottleneck* → end-to-end NEUTRAL (gemma-31b dense ~0%, gemma-26b MoE +6–8%). Isolated kernel benches don't predict end-to-end wins when the GPU is idle between kernels. **Do not pursue batched prefill.**

**The real lever = cut the launch overhead.** A 10%-util launch-bound chain is exactly what **CUDA Graphs** are for: capture the per-step kernel sequence once, then `cuGraphLaunch` to replay it as ONE launch — eliminating per-kernel CPU-side launch cost and most of the inter-kernel GPU bubbles. The async CUstream ring already serializes the commands in order, so the same stream is graph-capturable.

## Target (any catalog model; measure decode tok/s + prefill tok/s)

Decode is the headline metric (zinc loses to llama everywhere there). If graphs lift decode util from ~10% toward saturation, that's a large multi-model win. Prefill benefits identically (same chain).

## Plan (incremental, validate-before-commit)

1. **Cycle 1 — ISOLATED PROOF (`~/cuda_proto`, no repo change):** a C harness that (a) launches N≈50 chained tiny kernels on a stream the normal way, timed; (b) stream-captures the same N into a graph (`cuStreamBeginCapture` → N `cuLaunchKernel` → `cuStreamEndCapture` → `cuGraphInstantiate`), then `cuGraphLaunch` M times, timed. Report per-iteration: N-launches vs 1-graph-replay, and the per-launch overhead saved on this GPU. GATE: if graph replay isn't materially faster than N launches at small kernel sizes, CUDA graphs won't help here either → LOG it as a dead end and pivot to deeper fusion. If it IS faster (expected — launch overhead is the whole problem), proceed.
2. **Cycle 2 — wire `cuGraph` capture into `src/cuda/command.zig`:** add a graph-capture mode to the async ring — on the first decode step, stream-capture the per-step command chain into a `CUgraph` + instantiate a `CUgraphExec`; on subsequent steps with identical shape, `cuGraphLaunch` the exec instead of re-recording. Push-constants/buffer pointers that change per step (position, KV offset) must be handled (kernel-node param update via `cuGraphExecKernelNodeSetParams`, or design the kernels to read pos/offset from a small device buffer updated before each replay so the graph topology is invariant). Behind a flag (e.g. `ZINC_CUDA_GRAPH`). `-I/usr/local/cuda/include` is already enabled (a0d463af) if the graph API needs headers; the driver `cuGraph*` API is in `cuda.h`.
3. **Cycle 3+ — validate + measure:** `scripts/validate_catalog.sh` MUST stay 5/5 token-correct (graph replay must be bit-identical to the un-captured chain). Interleaved A/B decode tok/s with the flag on vs off. Extend to prefill (capture the per-layer chain). Commit each validated increment to `perf/e25-cuda-graphs-<step>`.

## Validation contract

- `scripts/validate_catalog.sh` 5/5 token-correct with the graph flag ON (replay must equal the recorded chain — same kernels, same order, same params per step). If a single token diverges, the param-update handling is wrong → fix or revert.
- Interleaved back-to-back A/B (graph on vs off) to beat boost noise; report decode tok/s, GPU util (nvidia-smi during), and ms/token.
- Isolated-cache builds (`ZIG_LOCAL_CACHE_DIR`+`ZIG_GLOBAL_CACHE_DIR`, verify binary hash changed). Pinned GPU per the runner. Isolated box dir `~/zinc-e25`, never `~/workspace/zinc`.

## HARD RULES (from the playbook)

- Profile util/power to confirm the regime before/after — the whole point is moving util UP. A win that doesn't raise util is suspect.
- Graph replay must be **bit-identical** (it's the SAME kernels) → catalog 5/5 is non-negotiable; negatives are valuable, log + revert.
- Branches not main; validate before commit; never commit host/IP/port; don't disturb the parallel work or `~/workspace/zinc`.

## Cycle log

(append dated entries per cycle: cycle | change | built+hash-changed? | catalog 5/5? | A/B result (util before→after, tok/s) | branch/sha or revert+why | next)

**2026-06-13 — Cycle 4 (EXTEND CAPTURE TO MoE + MEASURE qwen36-27b): TWO NEGATIVES, code REVERTED, branch stays at cycle-3 `a18b955e`.** Two cycle-3-NEXT candidates probed; neither yields a validated win, so the in-progress code is reverted and the findings logged (negatives are valuable). **(1) MoE graph capture = NO-OP on the catalog.** Wrote a fully-batched-MoE capturability gate (`expertsBatched(wge,wue,wde)` = gate/up q4k + down q5k → the one-launch batched expert path with NO host readback; `moeGraphCapturable()` = true only when EVERY layer is batched, since one per-slot-fallback layer would `commitAndWait`+download the router ids mid-step = illegal during capture) and let MoE models with that property take `decodeStepGraph`. But the only catalog MoE on the box, `Qwen3.6-35B-A3B-UD-Q4_K_XL`, has **mixed expert quants** (gguf scan: `ffn_down_exps` = 36×q5k + **4×q6k**; `ffn_gate_exps`/`ffn_up_exps` = 39×q4k + **1×q5k** each) → `moeGraphCapturable()` returns false → graphs never engage. **Empirically confirmed** on the built cycle-4 binary (md5 `56b277d5`, isolated-cache, `rm -rf .zig-cache`): `ZINC_CUDA_GRAPH=1` on 35b-a3b prints NO capture line (stays on the async ring), runs clean. The other MoE (gemma4-26b) is in `forward_cuda_gemma.zig` (untouched; gemma is boost-saturated + async-forbidden anyway). So full-MoE capture has NO viable target on this box; making the 6 mixed-quant layers capturable needs either per-layer graph splitting (a host-sync boundary breaks whole-step capture) or batched q6k/q5k-gate expert kernels — both out of scope for one cycle. Reverted `forward_cuda.zig` to `a18b955e`. **(2) qwen36-27b dense graph A/B = NEUTRAL.** The dense 27b path was already 12/12 token-correct but perf-unmeasured. Interleaved OFF/ON, NGEN=160, 4090-pinned, 5 clean rounds all `occ 0→0` (uncontended): ON/OFF ratio **1.010, 0.996, 0.997, 1.073, 0.958** (OFF tok/s 35.3/35.3/35.4/35.6/36.8; ON 35.6/35.1/35.4/38.2/35.3). Three rounds dead-neutral (±1%); the two outliers (+7.3% an ON boost spike to 38.2, −4.2% an OFF boost spike to 36.8) are opposite-direction boost noise that cancel → **median ratio ≈ 0.997, no real benefit.** This DECISIVELY confirms the cycle-2 hypothesis that the graph win is inversely proportional to per-kernel compute: graphs reclaim inter-kernel launch bubbles, but 27b's larger matvecs (n_embd 5120, bigger FFN) do enough GPU work per kernel that the bubbles are a negligible fraction — so the +8% seen on the small 9B (n_embd 4096) **vanishes entirely at 27B scale.** Takeaway: the Effort-25 graph lever is real but SIZE-GATED — it pays off on small dense models and fades on large ones; the headline win remains qwen35-9b (cycles 2+3, ~+12% cumulative). No commit of code; this entry is the cycle's only artifact. Box scratch (`~/e25c4.sh`, `~/e25c4-bin`, `/tmp/lc-*`/`gc-*`) cleaned. **NEXT cycle 5 (if pursued):** the last real dense lever is GPU-side embed (kill the per-token CPU dequant + decouple tokens so the graph could chain across steps), but cycle 3 already flagged it as the small remaining host cost → likely diminishing returns like c3's +4.4%; weigh against re-profiling util to confirm the per-token floor.

**2026-06-13 — Cycle 3 (FOLD EMBED H2D + ARGMAX D2H INTO THE GRAPH): VALIDATED WIN, branch `perf/e25-cuda-graphs-decode` commit `a18b955e` (pushed, NOT main).** Cycle 2 left two per-token syncs outside the captured graph: the embed upload (host dequant + H2D) and the argmax download. This cycle folds both into the single `cuGraphLaunch`. **Shim** (`cuda_shim.c/.h`): `cuda_upload_async`/`cuda_download_async` = no-sync `cuMemcpyHtoDAsync`/`DtoHAsync` on the ctx stream (issued between `cuda_graph_begin`/`end_launch` they become memcpy graph nodes) + `cuda_alloc_host`/`cuda_free_host` (`cuMemAllocHost` pinned memory — required so the captured copies are truly async). **buffer.zig**: `uploadAsync`/`downloadAsync`/`allocHost`/`freeHost` wrappers. **forward_cuda.zig**: `host_embed` is now PINNED + a new pinned `host_tok[1]`; in `decodeStepGraph` the embed H2D is recorded as the graph's FIRST node and the argmax D2H as its LAST node, so both ride the one launch+sync instead of two extra per-token syncs. The CPU dequant of the embedding row still runs BEFORE launch into the stable pinned `host_embed` address (graph re-records every step but `cuGraphExecUpdate`s in place; the memcpy node reads the same pinned address each replay → picks up the new token). Non-graph path byte-identical (still synchronous `buffer.upload`). **Built isolated-cache** (`ZIG_LOCAL/GLOBAL_CACHE_DIR=/tmp/e25c3_*`, fresh md5 `88b07ea9…` ≠ cycle-2 baseline `8b19d1f2…`, `rm -rf .zig-cache`). **Catalog 5/5 token-correct with `ZINC_CUDA_GRAPH=1`** (qwen35-9b 12/12 + qwen36-27b 12/12 — both graph-captured WITH the folded embed/argmax, bit-identical; 35b-a3b MoE 12/12, gemma 31b teacher-forced 11/12 near-tie, gemma 26b 12/12 — unchanged). **A/B (4090 pinned, interleaved cycle-2-graph-ON vs cycle-3-graph-ON back-to-back, NGEN=160, qwen35-9b, 5 clean rounds with `occ 0→0` confirmed uncontended; a 6th round excluded — the parallel e24 gate restarted mid-round, `occ 0→1`):** c2→c3 — 87.91→91.65, 86.72→91.45, 88.10→91.95, 87.33→93.56, 92.82→92.87 tok/s — **c3 wins 4/5 (+4.3..+7.1%, ~+4.4% median), 1 tie (a c2 boost outlier at 92.82), ZERO regressions** AND far LOWER variance (c3 steady 91.4–93.6; c2 drifts 86.7–92.8) → folding the 2 syncs makes the replay even more deterministic. The modest end-to-end size (vs the per-sync cost) is because the async ring + cycle-2 graph already removed most CPU-side blocking; this reclaims the last 2 per-token stream drains. MoE/gemma untouched (graphs cover only the 2 dense qwen models). **BOX GOTCHA this cycle (cost time):** the 4090 is SHARED with the parallel e24 effort's `merge_gate.sh` (also 4090-pinned by the validate default UUID) → the catalog run thrashed/slowed (correctness unaffected) AND the A/B had to WAIT for e24 to release the 4090 (`occ` gating per round caught the contention). Also: a foreground `ssh '… bash ab.sh'` backgrounded by the harness gets its remote script ORPHANED when the pipe parent exits (stdout lost until kill-flush) → always `nohup … >FILE 2>&1 &` on the box and poll the FILE. **NEXT cycle 4:** extend graph capture to qwen36-27b's longer chain + measure its A/B (already 12/12 correct, perf unmeasured); consider capturing the MoE post-router sub-chain (the router host-readback still blocks whole-step capture); profile util again to confirm the per-token CPU/sync floor is now ~gone (embed dequant on CPU is the last host-side per-token cost left).

**2026-06-13 — Cycle 2 (WIRE cuGraph INTO THE DECODE RING): VALIDATED WIN, branch `perf/e25-cuda-graphs-decode`.** Change: stream-capture the **dense** per-decode-step kernel chain (all layer ops + the final rms_norm/LM-head/argmax tail) into a cached `CUgraphExec` and replay it as ONE `cuGraphLaunch`/step, behind `ZINC_CUDA_GRAPH`. **Shim** (`cuda_shim.c/.h`): added `cuda_graph_{create,begin,end_launch,free}` — `cuda_graph_begin`=`cuStreamBeginCapture(stream, RELAXED)`; `cuda_graph_end_launch`=`cuStreamEndCapture`→`cuGraphExecUpdate` the cached exec (cheap, topology-invariant) with a re-`cuGraphInstantiate` fallback→`cuGraphLaunch`→`cuStreamSynchronize`. **forward_cuda.zig**: `decodeStepGraph` runs the existing per-block builders under a `capturing` flag so `submit` skips the event-record/sync (the single graph launch+sync drains everything) and `naive_attention` requests its dynamic shared-mem at the constant `max_ctx*4` (not `seq_len*4`) so the captured node shape is invariant every step → the exec UPDATES in place (only per-token push scalars — pos/seq_len/KV-offset/conv-offset — change) instead of re-instantiating. EMBED (host dequant + H2D) stays OUTSIDE capture (it syncs); MoE excluded (router host-readback is illegal mid-capture) so graphs only touch the 2 dense qwen models. Graph-OFF path is byte-for-byte the prior async ring (gated on `self.graph==null`). **Built isolated-cache** (`ZIG_LOCAL/GLOBAL_CACHE_DIR=/tmp/e25_*`, fresh binary md5 `a917ca35…`, `rm -rf .zig-cache`). **Catalog: 5/5 token-correct with `ZINC_CUDA_GRAPH=1`** (qwen35-9b 12/12, qwen36-27b 12/12 — both graph-captured; qwen36-35b-a3b MoE 12/12, gemma 31b teacher-forced 11/12 known near-tie, gemma 26b 12/12 — all non-graph). Smoke: graph ON vs OFF emit IDENTICAL token ids (bit-equivalent). **A/B (4090 pinned, interleaved OFF/ON back-to-back, NGEN=160, qwen35-9b):** R1 83.66→86.56, R2 80.45→85.54, R3 78.95→85.42, R4 75.86→86.25 tok/s — **ON wins 4/4 (+3.5%..+13.7%, ~+8% median)** AND is far LOWER-variance (ON steady ~85–86.5; OFF drifts 76–84 under boost decay) → the graph replay is more deterministic. **Util moved UP** (the hard-rule check): 6.6%→7.9% (sm ~1.6 GHz, ~38 W both) — still launch-bound overall (one stream-sync/token for the argmax readback + CPU embed-dequant glue remain), so graphs recovered the GPU-internal inter-kernel bubbles but not the per-token CPU/sync floor. The ~9× isolated-proof gain shrinks to +8% end-to-end because the real chain's big matvecs (LM head Q6_K, FFN) do substantial GPU work, so launch bubbles are a smaller fraction; the async ring also already removed the CPU-side launch blocking, leaving only the GPU-internal bubbles for graphs to reclaim. **NEXT cycle 3:** attack the remaining per-token CPU/sync floor — GPU-side embed (kill the host dequant+H2D) and/or overlap the argmax D2H so the stream needn't fully drain each token; then extend graph capture to qwen36-27b's longer chain + measure, and consider folding the MoE routed/shared chain (the router sync blocks whole-step capture, but the post-router sub-chain is capturable).

**2026-06-13 — Cycle 1 (ISOLATED PROOF): GATE PASSED, decisively.** Change: standalone CUDA harness `~/cuda_proto/graph_proof.cu` (box research dir, NO repo source change) — times a chain of N tiny kernels + a per-step `cudaStreamSynchronize` (mimics decode: N-kernel layer chain then argmax D2H) two ways: (A) relaunch the N kernels every step; (B) `cudaStreamBeginCapture`→N launches→`cudaStreamEndCapture`→`cudaGraphInstantiate` once, then `cudaGraphLaunch`+sync per step. Built: standalone `nvcc -O3 -arch=sm_89` (no zig build, no catalog impact). Catalog: N/A (no repo code touched). **A/B (4090, pinned `GPU-e59a6fce-…`, interleaved A/B/A/B, min-of-2, 3 runs across n_embd=4096 AND 2816, all agree):** per-step ms relaunch→graph — N=60: ~1.2→~0.34 (**~3.6–6×**); N=480 (≈60-layer real chain): **~4.5–5.0→~0.51–0.56 = ~8–9.6×**. Relaunch grows ~LINEARLY with N (the launch-bound signature); graph replay stays ~FLAT (~0.5 ms ≈ a single WSL2 sync round-trip) regardless of N → the entire per-kernel launch + inter-kernel-bubble cost collapses to one replay. This is the GPU-internal launch latency the CPU-side async ring explicitly *can't* touch (per [[zinc-cuda-backend]] gemma note), so graphs are additive to the ring. GATE: "graph replay materially faster than N launches at small kernel sizes" → YES, ~9× at the real chain length. **Branch: `perf/e25-cuda-graphs-proof` (effort-file log entry only; harness stays in `~/cuda_proto` per the research-dir convention).** NEXT cycle 2: wire `cuGraph` capture into `src/cuda/command.zig`'s async ring — capture the per-step command chain on the single CUstream on step 1, `cuGraphLaunch` on identical-shape subsequent steps; handle per-step-varying params (position, KV offset) via device-buffer reads so graph topology is invariant (else `cuGraphExecKernelNodeSetParams`). Behind `ZINC_CUDA_GRAPH`. Gate = catalog 5/5 bit-identical + interleaved decode A/B with util before→after.
