# Effort 28 — Multi-tenant serving + continuous (request-level) batching on CUDA

> **Status:** 🔬 OPEN (spawned 2026-06-14). Goal: ZINC+CUDA serves **many concurrent requests batched into one GPU forward** (continuous/dynamic batching, the vLLM/TGI model), not one-sequence-at-a-time. This is a **multi-cycle BUILD on ONE persistent branch `feat/e28-batching`** (NOT independent perf tweaks, NOT main). Each cycle advances the next increment, validates it, commits to `feat/e28-batching`, and updates `## CURRENT STATE` below.

Forward paths: `src/compute/forward_cuda_gemma.zig` (gemma4 dense+MoE — **the increment-1 target**), `src/compute/forward_cuda.zig` (qwen35/36 hybrid-SSM — increment 4). Serving: `src/server/routes.zig`, `src/server/http.zig`, `src/main.zig`. Scaffolding (exists, UNWIRED in CUDA): `src/scheduler/{scheduler,request,kv_cache}.zig`.

## Where we are today (the honest baseline — confirmed by code audit 2026-06-14)

- **Server is thread-per-request but GPU-SERIALIZED.** `routes.zig` spawns a thread per connection, but every generation funnels through one global `ServerState.generation_mutex` (`routes.zig:351,554,605`; `GenerationGuard` at `:346`). Concurrent clients → correct, isolated outputs, but the GPU runs **one sequence at a time**; the rest queue (head-of-line blocking). So multi-tenant *correctness* exists; multi-tenant *throughput* does not.
- **`--parallel N` (default 4, `main.zig:171,606`) is cosmetic** — parsed + logged "max N concurrent requests" (`main.zig:2198/2426`), only sizes the unused scheduler's slot array. Batches nothing.
- **CUDA forward is single-sequence.** `decodeStep(token, pos)` (`forward_cuda_gemma.zig:638`, `forward_cuda.zig:456`); monolithic per-layer KV `kv_k[L] = [max_ctx·kv_dim]` indexed `pos·kv_dim` (`forward_cuda.zig:258,657`). No batch/sequence dimension.
- **`src/scheduler/`** has a `Scheduler` (slot accounting), `Request` lifecycle, and a real paged `KvPagePool` — but the module header says *"the batched prefill/decode dispatch loop is not wired yet,"* `pendingPrefill()`/`activeDecoding()` are stubs returning `&.{}`, and it's only `_ = @import`'d (dead) in the CUDA path. `KvPagePool` IS used by the generic `forward.zig` (CPU/Metal) but NOT by `forward_cuda*.zig`.

## Why this is the single biggest decode-throughput lever (not just a serving feature)

Decode is **GPU-launch / weight-bandwidth-bound at ~7–12% util** (established across Efforts 23/25): each step re-reads the **entire model weights** to emit ONE token. Batching B sequences reads those **same weights once** and amortizes the **same kernel launches** across B tokens → throughput scales ~linearly in B until compute-bound. So the very thing that caps single-stream decode (it's bandwidth/launch-bound, not compute-bound) is exactly what makes request-batching a **multi-× aggregate-throughput win**. `llama-server` does continuous batching — so this is the *real* "beat llama" comparison for serving, and where ZINC has the most headroom.

## THE KEY INSIGHT — batched decode ≈ the batched PREFILL we already have

`prefillBatched` (`forward_cuda_gemma.zig:729`) **already runs a B-wide forward correctly**: B token-rows through `attentionLayerBatched` (`:936`) + `ffnBlockBatched` (`:1015`), with the qkv/o/ffn **GEMMs already sized for B rows** (`BatchScratch` buffers `[T, …]`). Decode-batching reuses ALL of it. The **only** single-sequence assumptions are in TWO spots:

1. **The attention kernel `gemma_attention_batched`** (`kernels.cu:3150`): `blockIdx.y = t` is the query row, and it hard-codes `seq_len = t + 1u` (causal length = the query's position *within the prompt*) over **one shared KV** base. → Generalize to: query row `b` belongs to sequence `b` at **its own** position `positions[b]`, reading **its own** KV slot. So `seq_len = positions[b] + 1`, and the K/V base gets a per-sequence slot offset. The sliding-window logic (`:3164`) carries over per-sequence unchanged.
2. **KV write + KV layout**: today one sequence writes `kv_k[L]` at `pos·kv_dim`. → slot-based: sequence in slot `s` at pos `p` writes `(s·max_ctx + p)·kv_dim`.

Everything else (norms, RoPE, GEMMs, GeGLU, sampling) is already per-row over B, or is a trivial per-row grid. **So increment 1's genuinely-new GPU work = generalize ONE attention kernel + slot-index the KV write. The rest is buffer sizing + a driver + a harness.** This is what makes a from-scratch serving engine unnecessary.

## CURRENT STATE (read this FIRST; update it LAST every cycle)

**🚩 THIS CYCLE — ✅ VALIDATED BUILD (lever ii(a), commit `8936aa4a`): EXTENDED the `btok` token-batch matvecs from B≤16 to B≤24 (toward the ~B≈27 Q4_K roofline ceiling) so the B=17..24 higher-concurrency serving regime stops falling back to the padded 64×64 tile GEMM.** The prior cycle's explicit NEXT lever (ii)(a) — "raise the cap further" — picked up as in-progress work on the branch. The small-B `btok` matvecs read each weight row ONCE and amortize the dequant over B tokens (bandwidth-bound, no tile-padding waste); they were capped at B≤16, so the B=17..24 serving regime hit the wasteful tile GEMM again. Added `dmmv_q4k/q6k/q5k/q8_0_btok17..24` (`kernels.cu`, +32 kernels — `dmmv_*_btok_impl<N>` instantiated N=17..24; the per-thread `float sum[B]` register-array grows but there is NO shared-mem scaling and the block-reduce is serial-per-b → safe at B=24), grew the pipeline arrays `[15]→[23]` + unified the compile loop over B=2..24, bumped the `decode_mrow` gate AND the `gemmDispatch(A)` btok guard `B≤16 → B≤24` (BOTH archs), and the `dbg_cuda batch` harness `MAXB 16→24`. **ADDITIVE**: production single-sequence `decodeStep`/`prefillBatched` untouched; `btok` rides the default-on `decode_mrow` path; each `btok` is bit-identical-per-row to its `*_fast` matvec → token-identical. **Validated on a CLEAN 5090 window (`GPU-5126d018`, 0% foreign util, no e27/perf loops running):** built isolated-cache cuda-dbg md5 `a15e83bd` (≠ prior HEAD `05741bd1` → new code in binary); `dbg_cuda batch` **B=24 token-identity BATCH_GATE PASS + BATCH_SANITY PASS** (batched B=24 == solo B=1 == serial, 24 seqs mixed lengths 3–5, ngen 12) for **qwen35-9b AND gemma4-31b**; **serial catalog 5/5 ALL PASS** (12/12 each, production path unaffected). In-process boost-comparable A/B at B=24 (btok OFF→ON, same model load): qwen35-9b **104.77→372.97 tok/s agg (~3.56×)**, gemma4-31b **44.34→122.31 tok/s agg (~2.76×)** — far outside boost noise (B=24 previously hit the tile GEMM; now uses `btok24`). **NEXT: lever (ii) is near its natural ceiling — `btok` now covers B=2..24, one short of the ~B≈27 Q4_K roofline crossover where the bandwidth-bound matvec stops beating the tile GEMM, so raising the cap further has little headroom (and register pressure from `sum[B]` keeps growing). The remaining decode levers are (a) a TRUE warp-tiled higher-B batched-GEMM kernel (no 64-pad) for B>~27 if the serving regime needs it, and (b) deeper per-step kernel FUSION (Effort-23 playbook) on the batched decode step — the ~3× gap vs llama at B=8 is the per-step kernel, NOT launch overhead (graphs are RESOLVED/neutral). Also still-open: MoE graph WIN measurement pending a fully-idle e27 window (qwen36-35b-a3b 18GB needs the 24GB cap free).**

**✅ PRIOR CYCLE — VALIDATED BUILD (lever ii, commit `8eed1680`): EXTENDED the `btok` token-batch matvecs from B≤8 to B≤16 so the B=9..16 higher-concurrency serving regime stops falling back to the padded 64×64 tile GEMM.** The small-B matvecs (`dmmv_q4k/q6k/q5k/q8_0_btok`) read each weight row ONCE and amortize the dequant over B tokens (bandwidth-bound, no tile-padding waste); they were capped at B≤8. Added `btok9..16` instantiations for all four quants (`kernels.cu`, +32 kernels), grew the pipeline arrays `[7]→[15]` + unified the compile loop over B=2..16, bumped the `decode_mrow` gate `B≤8 → B≤16` (BOTH archs), and the `dbg_cuda batch` harness `MAXB 8→16`. **ADDITIVE**: production single-sequence `decodeStep` untouched; `btok` rides the default-on `decode_mrow` path; each `btok` is bit-identical-per-row to its `*_fast` matvec. **Validated on a CLEAN 5090 window (`GPU-5126d018`, 0% foreign util):** built isolated-cache cuda-dbg md5 `05741bd1` (≠ prior HEAD `6d46d4ae` → new code in binary); `dbg_cuda batch` **B=16 token-identity BATCH_GATE PASS + BATCH_SANITY PASS** (batched B=16 == solo B=1 == serial) for **qwen35-9b AND gemma4-31b**; **serial catalog 5/5 ALL PASS** (production path unaffected). In-process boost-comparable A/B at B=16 (btok OFF→ON, same model load): qwen35-9b **72.3→344.3 tok/s agg (~4.8×)**, gemma4-31b **33.0→134.7 tok/s agg (~4.1×)** — far outside boost noise (the B=16 case previously hit the tile GEMM; now it uses `btok16`). **NEXT: lever (ii) continues — (a) the agg tok/s still falls when B exceeds the btok ceiling (>16) and at the ~B≈27 roofline crossover; consider raising the cap further OR a true higher-B batched-GEMM kernel (warp-tiled, no 64-pad) for B>16; (b) deeper per-step kernel fusion (Effort-23 playbook) on the batched decode step (the remaining ~3× gap vs llama at B=8 is the per-step kernel, NOT launch overhead — graphs are RESOLVED/neutral). Also still-open: MoE graph WIN measurement pending a fully-idle e27 window (qwen36-35b-a3b 18GB needs the 24GB cap free).**

**✅ PRIOR CYCLE — MEASUREMENT FINDING (NO code change, tree clean; RESOLVES NEXT lever i for the DENSE arch): on a CLEAN 5090 window the DENSE batched-decode CUDA-graph replay is NEUTRAL-to-slightly-NEGATIVE → do NOT flip `batch_graph_on` default-on; the graph path correctly stays OPT-IN (`ZINC_BATCH_GRAPH`).** A clean 5090 window opened (e27 does discrete `cuda-dbg gen` calls on gemma-26b with gaps; caught uncontended rounds via the harness's per-round `f0`/`foreign_after` guard). Built HEAD (`48c75f88`, cuda-dbg md5 `6d46d4ae`, NO source change) and ran the in-process `GRAPH_TIMING` A/B (graph OFF then ON in ONE model load = boost-comparable; mrow held ON = the default serving path) on qwen35-9b dense B=4. **3 FULLY-CLEAN rounds (`f0=0` AND `foreign_after=0`): ON/OFF agg tok/s = 282.95/289.67 (−2.3%), 257.39/263.43 (−2.3%), 258.44/279.43 (−7.5%) — graph ON is consistently NEUTRAL-to-slightly-SLOWER, NEVER a win.** This CONFIRMS the standing hypothesis: the dense batched step was already launch-collapsed (`3c6ca650`, +56.6%), draining the per-step launch overhead → CUDA-graph replay adds no headroom (mirrors E25's SERIAL finding that graphs add only ~+8% on top of the async ring — the batched path has even less residual). **DECISION: the "flip default-on iff a real win across BOTH archs" condition FAILS at the dense arch alone → the flip is UNWARRANTED; the opt-in `ZINC_BATCH_GRAPH` stays the correct shape.** **MoE arm BLOCKED (cannot measure clean):** qwen36-35b-a3b (18GB) cannot co-load with e27's gemma-26b (16GB) under the shared 24GB agent-zinc cgroup cap (BOTH run as the SAME user) → OOM/wedge; and e27's A/B loop holds a near-continuous 5090 context (`f0=2` after the first few rounds) → no clean MoE window. Only last cycle's CONTENDED +69% remains (untrustworthy). **Lever (iii) found ALREADY DONE:** `decodeBatch` auto-dispatches to `decodeBatchGraph` when `batch_graph_on` (set from `ZINC_BATCH_GRAPH` at init, `forward_cuda.zig:1188`), and `ServeEngine` calls `fwd.decodeBatch` → the live server ALREADY replays the captured graph when launched with `ZINC_BATCH_GRAPH=1`; no wiring needed. Did NOT disturb e27 (effort spec's "box DEDICATED / e27 stopped" claim is STALE — e27 still co-tenants the box). **NEXT: the graph-capture lever set is now RESOLVED — dense graph stays OPT-IN (neutral on a clean window); the MoE graph WIN measurement remains pending a window where e27 is FULLY idle (gemma-26b NOT loaded, so the 18GB MoE model fits the 24GB cap). The real remaining decode gap vs llama (~3× at B=8) is the PER-STEP KERNEL, NOT launch overhead → next levers = (ii) higher-B batched-GEMM kernel efficiency (B>8, where `btok` B=2..8 doesn't apply → falls to the wasteful 64×64 tile GEMM) and deeper kernel fusion (Effort-23 playbook), NOT graphs.**

**✅ PRIOR CYCLE — CORRECTNESS-COMPLETE BUILD (commit `b33370f3`): EXTENDED CUDA-GRAPH CAPTURE to the MoE BATCHED-DECODE step (qwen36-35b-a3b), opt-in `ZINC_BATCH_GRAPH`; token-identical (BATCH_GATE+SANITY PASS), catalog 5/5; GRAPH_TIMING reads CONTENDED → not a perf claim.** The explicit NEXT lever (ii). The dense batched step was already graph-captured (`dcf1250b`); this brings MoE under capture too. **KEY ENABLER (no new sync work needed):** after `4439e0be` (GPU-side expert-id for ALL supported quants incl. Q6_K) + `6516c010` (per-row sync collapse), the batched MoE routed+shared path already contains NO host readback and NO `commitAndWait`, and `submit`/`waitPending` are NO-OPS under `capturing` (capture-path `submit` releases without populating `pending` → `n_pending` stays 0 → `waitPending` loops 0×). The expert id is read GPU-side from a device buffer (`router_out_buf`), NOT a push constant, so the captured topology AND push-constants stay invariant across steps → `cuGraphExecUpdate` is a cheap no-op (only buffer contents change per step). **CHANGES (additive, surgical):** (1) new `moe_graph_capturable` field computed ONCE at init = true iff EVERY MoE layer's gate/up/down expert tensors are `expertsSupported` (q4k/q5k/q6k) → no host id gather on ANY layer (false for dense, which stays on the `n_experts==0` gate); (2) init: `ZINC_BATCH_GRAPH` now arms batched-graph replay for MoE-capturable models too (was hard `n_experts==0`); (3) `use_graph` gate: MoE permitted when `moe_graph_capturable AND moe_shared_batched AND moe_collapse` (the no-sync batched routed+shared path; all ride `decode_mrow`/mrow-default-on); (4) `decodeBatchGraph` layer loop now dispatches `moeFfnBlockBatchedDecode` for MoE layers (was hard `ffnBlockBatchedDecode`); (5) `dbg_cuda` GRAPH_TIMING A/B generalized from dense-only to `(n_experts==0 OR moe_graph_capturable)`. **Opt-in** via `ZINC_BATCH_GRAPH`; default OFF → production serial `decodeStep`/`prefillStep`/`prefillBatched` AND default `decodeBatch` byte-identical. **Validated 5090** (cuda-dbg md5 `0907c59d` ≠ dense-graph `1afd03bf`): catalog **5/5 serial token-correct** (12/12 each); **qwen36-35b-a3b dense+SSM+MoE B=4 mixed positions (`760,6511,314,9338,369|450,3271,310|1102,323,1023,1024,7,8|99,100,101`) `ZINC_BATCH_GRAPH=1 ZINC_BATCH_MROW=1` NG=20: `moe_capturable=true` + BATCH_GATE:PASS + BATCH_SANITY:PASS** (captured MoE graph == solo == serial, token-identical — proves the captured replay is bit-equivalent to the async MoE chain); **dense qwen35-9b graph still BATCH_GATE+SANITY PASS** (`moe_capturable=false`, dense path byte-unchanged → no regression on the shared `decodeBatchGraph`/gate code). **GRAPH_TIMING (CONTENDED — e27 PID 1075304 holds a 5090 ctx, `foreign_on_gpu≥1`): MoE qwen36-35b-a3b B=4 OFF 62.29→ON 105.36 (+69%), dense qwen35-9b B=4 OFF 275.51→ON 300.16 (+9%)** — directional only, NOT a perf claim (single contended/boost-noisy read). The whole catalog's batched decode step (dense + MoE) is now graph-capturable, correctness-complete + token-identical. **NEXT: (i) MEASURE the graph WIN on a CLEAN 5090 window (re-run GRAPH_TIMING A/B for BOTH dense + MoE, or wire `ZINC_BATCH_GRAPH` into `throughput_vs_llama.sh`, when `foreign_on_gpu==0`; if a real win across both archs, flip `batch_graph_on` default-on — correctness already complete for the whole catalog, the flip just makes the validated path default); (ii) higher-B batched-GEMM kernel efficiency (B>8 where btok doesn't apply); (iii) wire `ZINC_BATCH_GRAPH` into `ServeEngine` so the live server replays the captured graph per distinct B.** The per-layer-sync + graph-capture lever set is now DRAINED for the whole catalog; the remaining decode levers are clean-window WIN measurement + higher-B GEMM efficiency.

**🚩 PRIOR CYCLE — CORRECTNESS-COMPLETE BUILD (commit `dcf1250b`): BUILT CUDA-GRAPH REPLAY for the DENSE batched-decode step (qwen35, `forward_cuda.zig`) — per-B captured `CUgraphExec`, opt-in `ZINC_BATCH_GRAPH`; token-identical (BATCH_GATE+SANITY PASS), catalog 5/5; throughput A/B NEUTRAL but single CONTENDED-5090 read → not a perf claim (WIN measurement needs a clean window).** The explicit NEXT lever (i) the prior cycle unblocked. The dense batched-decode step is sync-free after the launch-collapse (`3c6ca650`), so I mirrored the serial `decodeStepGraph` onto `decodeBatch`: new `decodeBatchGraph(B, db, pos_buf, slot_buf, max_seq_len, out_norm, lm_head, lm_type, out)` sets `capturing=true`, `cuda_graph_begin`, records the batched layer blocks (which ride the async `submit` no-sync path under `capturing` — already true after the collapse) + a captured B-row tail (rms→LM-head→argmax into one cmd, `releaseCompleted` not `commitAndWait`), then `cuda_graph_end_launch` (update-or-instantiate the cached exec + launch + ONE sync) + a B-wide argmax D2H. **KEY SIMPLIFICATION vs serial:** the embed + per-seq pos/slot/token uploads already ran (sync) into device scratch in `decodeBatch` BEFORE the graph branch, and every per-seq scalar (positions/slots) is read from a DEVICE BUFFER not a push constant → the captured topology AND push-constants are INVARIANT across steps (unlike serial where `pos` is a push constant) → `cuGraphExecUpdate` is a trivially-cheap in-place no-op; only the device-buffer CONTENTS change between steps (updated by the pre-capture sync uploads). **CAVEAT handled:** the batched attention smem now sizes at the constant `self.slot_ctx*4` under `capturing` (was data-dependent `max_seq_len*4`) so the kernel-node shape stays invariant as positions grow (mirrors the serial `if (self.capturing)` trick). Per-B cache `batch_graph[9]` (lazily created), gated dense-only (`n_experts==0` — MoE router host-readback is illegal mid-capture, same as serial) AND `decode_collapse` (a captured region must contain no `commitAndWait`). **Opt-in** via `ZINC_BATCH_GRAPH` (`batch_graph_on`, set in init) / harness `batch_graph_force`; **default OFF → the production serial `decodeStep`/`prefillStep`/`prefillBatched` AND the default `decodeBatch` path are byte-identical** (the new fields default null/false; `capturing` is only ever true inside `decodeBatchGraph`; serial gen never calls `decodeBatch`). New `GRAPH_TIMING` A/B in `dbg_cuda batch` (mrow held ON, toggles `batch_graph_force`, dense only). **Validated 5090** (cuda-dbg md5 `1afd03bf` ≠ collapse `64786348`): catalog **5/5 serial token-correct** (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b); **qwen35-9b dense B=4 mixed positions (`760,6511,314,9338,369|450,3271,310|1102,323,1023,1024,7,8|99,100,101`) `ZINC_BATCH_GRAPH=1 ZINC_BATCH_MROW=1` NG=24: BATCH_GATE:PASS + BATCH_SANITY:PASS** (batched-graph == solo == serial, token-identical — proves the captured replay is bit-equivalent to the async chain). **GRAPH_TIMING B=4 OFF 294.87 → ON 291.65 tok/s agg — NEUTRAL (−1.1%) but a SINGLE measurement on a CONTENDED 5090** (e27 PID `1061336` holds a 5090 CUDA context, `foreign_on_gpu≥1`) → NOT a perf claim; ALSO the dense batched step is already launch-collapsed (prior cycle +56.6%), so the residual graph headroom is small (mirrors E25 serial: CUDA graphs add only ~+8% on top of the async ring, and that was on a CLEAN box). **NEXT: (i) MEASURE the graph WIN on a CLEAN 5090 window — re-run `dbg_cuda batch ... ZINC_BATCH_GRAPH` GRAPH_TIMING A/B (or wire `ZINC_BATCH_GRAPH` into `throughput_vs_llama.sh`) when `foreign_on_gpu==0`; if a real win, consider flipping `batch_graph_on` default-on like the mrow flip. The graph path is correctness-complete + token-identical, so the flip is just making the validated-better path the default. (ii) EXTEND graph capture to the MoE batched step (qwen36-35b-a3b) — now viable since `4439e0be` put ALL MoE routed layers on the GPU-side-expert-id path (no host readback) + `6516c010` collapsed the per-row syncs; the remaining blocker is the per-layer `moeSharedExpertBatched`→`waitPending` inside `moeFfnBlockBatchedDecode` (drop it under `capturing` so the MoE block also rides the no-sync ring, then gate the dense-only restriction off). (iii) higher-B batched-GEMM kernel efficiency (B>8 where btok doesn't apply).** The per-layer-sync lever set is DRAINED; per-step launch overhead is now also collapsible via graph replay (opt-in, correctness-proven).

**✅ PRIOR CYCLE — VALIDATED WIN (commit `3c6ca650`): LAUNCH-COLLAPSED the DENSE batched-decode layer chain (qwen `forward_cuda.zig`) → qwen35-9b B=4 +56.6% / qwen36-35b-a3b MoE B=4 +17.0% (clean 5090 in-process A/B), token-identical; ALSO the structural prerequisite for graph-capturing the batched step (the explicit NEXT lever i, BUILT this cycle above).** The 3 dense batched-decode blocks (`attentionLayerBatchedDecode`/`ssmLayerBatchedDecode`/`ffnBlockBatchedDecode`) each ended with a blocking `cmd.commitAndWait()` so the shared per-layer scratch (`db.norm`/`db.qfull`/`db.gate`/…) was safe to reuse next layer. That sync is UNNECESSARY — all commands ride ONE shared `CUstream` strictly in-order (the same `6516c010` MoE-collapse argument: `cuda_begin_command` sets `m->stream = c->stream`), so layer L+1's writes serialize AFTER layer L's reads with NO host round-trip. **FIX (additive, surgical, qwen decode-batch only):** new `decode_collapse` flag (default-on, `decode_collapse_force` for the harness A/B) routes those 3 blocks through the async `submit` ring instead of per-layer `commitAndWait`; drained ONCE at the decode tail via `drainPending()` after the tail's `commitAndWait` (which drains the shared stream) → removes ~2 CPU↔GPU round-trips per layer per token on the boost-starved launch-bound decode (dense qwen35 ~36 layers × 2 syncs/tok → 1). **MoE-safe by construction:** the MoE block's `moeSharedExpertBatched` ends with `waitPending()` which drains the WHOLE ring (incl. the async attn/ssm commands) each MoE layer → for MoE models the dense collapse just lets attn/ssm ride into the MoE tail sync (in-order → correct). PURE sync removal, ZERO math change → ARGMAX-identical → batched==N-serial token-identical. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never set `decode_collapse`). New `DCOLLAPSE_TIMING` A/B in `dbg_cuda batch` (holds mrow ON, toggles only `decode_collapse`, both archs). **Validated 5090** (cuda-dbg md5 `64786348` ≠ flip `e3d3f77c`): catalog **5/5 serial token-correct** (12/12 each); **qwen35-9b dense B=4 + qwen36-35b-a3b MoE B=4 mixed positions `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial — the MoE case proves the dense-collapse↔MoE interaction); **DCOLLAPSE_TIMING (CLEAN 5090, 0 MiB foreign): dense qwen35-9b B=4 collapse OFF 189.11 → ON 296.23 tok/s agg (+56.6%), MoE qwen36-35b-a3b B=4 OFF 77.66 → ON 90.86 (+17.0%)** — mrow held ON for both arms so the A/B isolates the per-layer sync removal. **NEXT (the per-step kernel is the remaining ~3× gap vs llama; the collapse just unblocked it): (i) GRAPH-CAPTURE the batched decode step per distinct B — now FULLY viable (the dense blocks + MoE routed path contain NO commitAndWait, so the captured region is sync-free): mirror the serial `decodeStepGraph` (set `capturing=true`, `cuda_graph_begin`, record embed + all batched layers + tail through the `submit` no-sync capture path, `cuda_graph_end_launch`); per-step scalars (positions/slots/tokens) already live in persistent scratch buffers re-uploaded before each replay → shape-invariant topology; cache one `CudaGraph` per distinct B. WIN measured on the now-clean 5090, A/B graph ON vs OFF on the now-default path. CAVEAT: the batched attention smem currently uses `max_seq_len*4` (data-dependent) — capture must size it to `max_ctx*4` like the serial path's `if (self.capturing)` trick at line ~1899, else the captured launch under-sizes smem as positions grow. (ii) higher-B batched-GEMM kernel efficiency (B>8 where btok doesn't apply).** The flip gate is CLOSED; the MoE/dense per-layer-sync lever set is now DRAINED.

**🚩 PRIOR CYCLE — THE FLIP LANDED (commit `493a721d`): `ZINC_BATCH_MROW` is now DEFAULT-ON (`mrowMatvecOn()` default `false`→`true`, both archs) — the recurring default-on gate, blocked for many cycles on e27 5090 contention, finally cleared on a CLEAN 5090 window.** A clean window opened (5090 0% util / 0 MiB / 0 compute-apps, `foreign_on_gpu==0`, no e27/perf loops running) → ran `MODEL=$HOME/workspace/Qwen3.5-9B-Q4_K_M.gguf ROUNDS=5 NG=64 bash scripts/throughput_vs_llama.sh` on the 5090. **GATE VERDICT: CLEAN — all 60/60 rounds uncontended, medians fully trustworthy.** Medians ext_agg_tok/s (qwen35-9b, 5090): **B=1 ZINC0 22.10 / ZINCM 21.77** (mrow only engages 2≤B≤8 → B=1 unchanged = NO regression), **B=2 8.40 / 34.76 (4.14×), B=4 16.97 / 48.02 (2.83×), B=8 26.36 / 60.33 (2.29×)** [llama-server 78.19/116.52/153.52/180.73]. ZINCM (mrow ON) clean-beats ZINC0 (mrow OFF) at EVERY batched B with no B=1 regression → the gate's exact pass condition (CLEAN + ZINCM>ZINC0, no regression). The llama gap closes from the old gemma mrow-OFF ~30-42× to **~3× on qwen35-9b** (B=8 180.73 vs 60.33). FLIP (surgical, 2 files): `mrowMatvecOn()` in `forward_cuda.zig` + `forward_cuda_gemma.zig` now defaults `true` (opt OUT with `ZINC_BATCH_MROW=0/off/false/no`, mirroring `moeSharedBatchedOn`). The whole btok + MoE shared-expert-batch + MoE launch-collapse + GPU-side-expert-id stack (every prior cycle's token-identical win) now ships as the serving DEFAULT. **Additive/safe BY CONSTRUCTION: the serial `decodeStep`/`prefillStep`/`prefillBatched` path NEVER sets `decode_mrow` (only `decodeBatch` does, for 2≤B≤8) → serial catalog correctness is unaffected; the batched mrow-ON path is proven token-identical to N-serial every cycle, so the flip just makes the validated-better path the default.** Validated 5090 (cuda-dbg md5 `e3d3f77c` ≠ prior `1cf4a733`; zinc md5 `6af7dd4a`): catalog **5/5 serial token-correct** (12/12 each); **qwen35-9b 3 seqs mixed positions `ZINC_BATCH_MROW=1` BATCH_GATE:PASS + BATCH_SANITY:PASS** (token-identical batched==solo==serial on the flipped binary). **NEXT (the per-step kernel is now the gap vs llama, ~3×): (i) graph-capture the batched decode step per distinct B — now FULLY viable for MoE (no per-row sync anywhere on the routed path after the `6516c010` collapse + `4439e0be` GPU-side expert-id); the Effort-25/27 playbook; WIN measurement needs a clean 5090 window (use the now-default path, A/B graph ON vs OFF). (ii) higher-B batched-GEMM kernel efficiency (the B≥2 path still pays GEMM-vs-matvec at B>8 where btok doesn't apply). The MoE per-row-sync lever set is DRAINED; the flip gate is now CLOSED (landed).**

**✅ PRIOR CYCLE — VALIDATED WIN (commit `4439e0be`): GPU-side expert-id for the mixed-quant Q6_K MoE layers (new `dmmv_q6k_experts` kernel) → the 5 host-id FALLBACK layers of qwen36-35b-a3b now ride the launch-collapse; token-identical, catalog 5/5.** This is the prior CURRENT STATE's NEXT lever (iii) — the residual MoE lever — chosen because it does NOT need a clean 5090 window: its validation is correctness-only (token-identical + catalog 5/5, both contention-INDEPENDENT). e27 is STILL holding a continuous 5090 context (`foreign_on_gpu`≥1: gemma-26b `cuda-dbg gen` on the 4090 + the documented device-0 leak) → the throughput flip gate stays BLOCKED, so I took the build-only lever. **Root cause of the residual sync:** in `moeFfnRoutedRowDecode` the `batched_experts` async (GPU-side, no readback) path was hard-gated to EXACTLY `gate=q4k ∧ up=q4k ∧ down=q5k`; the 5 mixed-quant layers (down bumped to Q6_K by the UD-Q4_K_XL dynamic quant) failed that test → fell to the host path, which downloads `router_out_buf` to host-gather expert ids → a per-row `waitPending` sync per MoE layer per token (the LAST per-row sync the `6516c010` collapse left behind). **FIX (additive, surgical, qwen-MoE decode-batch only):** (1) new `dmmv_q6k_experts` in `kernels.cu` = the experts transpose of `dmmv_q6k_fast` (SAME per-element Q6_K dequant + `zinc_block_reduce_sum`, but the chosen expert id is read GPU-side from `expert_ids`/`router_out_buf`; per-expert weight slice `id*slice+base` bytes; x shared for gate/up or per-expert for down; output slot-major `y[e*M+row]`) → row b is bit-for-bit `dmmv_q6k_fast` on x[b]; (2) generalized the dispatch: `expertsSupported(t)` = `t ∈ {q4_k,q5_k,q6_k}` + new `expertsDispatch` helper picking the `dmmv_*_experts` kernel by `dmmvIdx(w.info.type_)`; `batched_experts` is now `expertsSupported(wge) ∧ expertsSupported(wue) ∧ expertsSupported(wde)` — a STRICT SUPERSET of the old condition (the existing q4k/q4k/q5k layers dispatch IDENTICALLY → byte-identical; only the q6k-containing layers change, going GPU-side; q8_0/f32 expert tensors, if any, still keep the host fallback). Pure kernel-SELECTION change, ZERO math change → ARGMAX-identical → batched==N-serial token-identical. Production `moeFfnBlock`/`decodeStep`/`prefillStep` + the per-row `moeFfnRowDecode` (the mrow-off A/B arm) UNTOUCHED. **Validated 5090** (cuda-dbg md5 `1cf4a733` ≠ collapse `43b32ae9`): catalog **5/5 serial token-correct** (12/12 each); **qwen36-35b-a3b B=4 mixed lengths (4/3/5/6) `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial through the now-GPU-side Q6_K layers). Throughput UNMEASURABLE this cycle (e27 contention — in-process COLLAPSE_TIMING B=4 read 64.11→79.19 ON but CONTENDED/boost-noisy → NOT a claim; the structural win is that ALL MoE layers now ride the async/collapse path, no per-row host sync remains). This also fully UNBLOCKS MoE graph-capture (the per-row sync in the routed loop is gone for EVERY layer now). **NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED head-to-head vs `llama-server --parallel` — STILL the recurring gate, STILL pending a CLEAN 5090 window (`foreign_on_gpu==0`); re-run `MODEL=qwen35-9b ROUNDS=5 bash scripts/throughput_vs_llama.sh`, VERDICT must read CLEAN/USABLE-PARTIAL with ZINCM>ZINC0, then flip `mrowMatvecOn()` default→true; (ii) graph-capture the batched decode step per distinct B — now FULLY viable for MoE (no per-row sync anywhere on the routed path); WIN measurement also needs a clean window; (iii) the MoE per-row-sync lever set is now DRAINED — remaining decode levers are graph-capture (ii) or higher-B GEMM kernel efficiency.**

**✅ PRIOR CYCLE — RAN the flip gate on qwen35-9b (the co-tenant-safe small model) → BLOCKED by live e27 contention (all rounds CONTENDED, medians empty), BUT captured STRONG directional corroboration that the flip is justified; hardened the harness with a self-reporting GATE VERDICT + small-model path (commit `fd7dc25e`).** The prior cycle hardened the harness to EXCLUDE contended rounds but never got to run it; that cycle ran it co-tenant-safely. KEY OPERATIONAL UNLOCK: the documented blocker for the head-to-head was gemma-31b's 17GB × 3 sequential servers colliding with e27's 16GB gemma-26b under the 24GB cap (wedge risk). **`MODEL=$HOME/workspace/Qwen3.5-9B-Q4_K_M.gguf` (~5GB) sidesteps it** — fits alongside e27, reloads ~15s not ~45s, and the ZINCM-vs-ZINC0 flip decision does NOT need llama. Ran `throughput_vs_llama.sh` (qwen35-9b, NG=64, ROUNDS=5, BLIST="1 2 4 8"): **every round of all 3 arms tagged CONTENDED(f0/f1≥1)** — e27 holds a 5090 CUDA context CONTINUOUSLY (foreign_on_gpu≥1 always), so the contention-guard correctly EXCLUDED all rounds → MEDIANS section EMPTY → **GATE VERDICT: BLOCKED (0 clean), flip stays PENDING a clean window.** (Harness fix this cycle: the empty-medians state was SILENT — added an explicit GATE VERDICT line `BLOCKED|USABLE-PARTIAL|CLEAN`, validated against the live data = "BLOCKED: 20/20 rounds CONTENDED, 0 clean". Script-only → binary byte-identical to the 5/5-validated HEAD `d892fe89`/`93134ebb` → serial catalog correctness preserved by construction, no re-validate needed.) **DIRECTIONAL READ (all arms under IDENTICAL e27 contention → the ZINCM/ZINC0 ratio is contention-CONTROLLED even though absolutes are excluded as untrustworthy): ZINC0 mrow-OFF B=2/4/8 ≈ 9/17/28 tok/s; ZINCM mrow-ON ≈ 75-81/98-105/71-119; LLAMA (it DID serve qwen35) ≈ 140-168/136-224/152-310.** So mrow-ON beats mrow-OFF ~8×/~6×/~3-4× END-TO-END THROUGH ServeEngine (corroborating the in-process btok 9.49× B=4), closing the llama gap from the old gemma mrow-OFF ~30-42× down to ~1.5-2× on qwen35-9b. Combined with the multi-cycle in-process token-identical A/B wins, the evidence for the flip is now OVERWHELMING and multi-modal — but per the gate's OWN design (exclude contended) + the hard rule (never trust boost-noisy/contended throughput), I did NOT flip on contended data. **NEXT: the moment a CLEAN 5090 window opens (e27 paused/finished/between-phases with foreign_on_gpu==0), re-run `MODEL=qwen35-9b ROUNDS=5 bash scripts/throughput_vs_llama.sh` — GATE VERDICT must read CLEAN/USABLE-PARTIAL with ZINCM medians > ZINC0 medians, no regression → then FLIP `ZINC_BATCH_MROW` DEFAULT-ON (the flip is just `mrowMatvecOn()` default→true; `moeSharedBatchedOn`/`moe_collapse` already default-true). Serial catalog 5/5 is unaffected BY CONSTRUCTION (serial `decodeStep` never calls `decodeBatch`/sets `decode_mrow`); the batched-mrow-ON path is already proven token-identical every cycle, so the flip only makes the validated-better path the default.** Alternatives if no clean window: (ii) graph-capture the batched decode step per distinct B (viable for MoE post-collapse; WIN also needs a clean window); (iii) GPU-side expert-id for the host-id FALLBACK layers (new `dmmv_q6k_experts`; correctness-gateable under contention but unmeasurable win).

**✅ PRIOR CYCLE — GATE-HARNESS HARDENING + BLOCKED-GATE FINDING (the recurring default-on gate, NEXT i): the "round-2 collapse" is e27 5090-CONTENTION, not (purely) a zinc bug; the head-to-head harness is now contention-guarded; default-on flip stays pending a CLEAN 5090 window.** Picked up the in-progress `scripts/throughput_vs_llama.sh` gate work (ZINC0 mrow-OFF vs ZINCM mrow-ON vs LLAMA, 3 rounds + medians). It was already running and producing GARBAGE: ZINC0 B=1 collapsed 24.90→0.92 tok/s between rounds. ROOT CAUSE (confirmed via `nvidia-smi --query-compute-apps`): the box is SHARED with **Effort-27**, whose `validate_catalog`/A-B runs **UNPINNED** and holds a 5090 CUDA context CONTINUOUSLY (its PID is on `GPU-5126d018` even at 0% util between gen bursts) → co-running inference crushes serve throughput. **So the long-documented "sustained round-2 collapse" (lever C) is CONFOUNDED by shared-box contention** — likely the prior lever-C measurements were contaminated too; the suspect#1/#2 fixes may have been chasing a measurement artifact. FIX (additive, **script-only — touches NO forward/serve code path**, so serial catalog correctness is preserved by construction; binary byte-identical to the 5/5-validated HEAD `93134ebb`): `foreign_on_gpu(server_pid)` counts compute procs on the 5090 that aren't our server; `sweep` snapshots before+after each `(B,round)` and tags it `CONTENDED(f0,f1)`; the MEDIANS awk skips CONTENDED rows → contended rounds are EXCLUDED → the gate yields clean medians only from uncontended rounds (or honestly reports all-contended = no clean data, vs silently averaging garbage). Detection VALIDATED live (returned `foreign_on_5090=1` against the live e27 context); `bash -n` clean. I must NOT edit the e27 worktree to pin it, and pausing e27's live A/B for a ~35-min / 3×17GB-reload head-to-head is too disruptive (user: "KEEP BOTH e27+e28"). Aborted the contaminated run + freed the 5090. **NEXT: run the hardened gate in a CLEAN 5090 window (when e27 is paused/finished) — it now auto-excludes contended rounds → trustworthy ZINCM-vs-ZINC0-vs-llama medians; IF ZINCM robustly beats ZINC0 with no regression AND the collapse is gone clean → flip `ZINC_BATCH_MROW` DEFAULT-ON (`mrowMatvecOn()` default true + `moeSharedBatchedOn` + `moe_collapse`).** Alternatives if no clean window opens: (ii) graph-capture the batched decode step per distinct B (now viable for the MoE step post-collapse — but its WIN measurement ALSO needs a clean window); (iii) GPU-side expert-id path for the host-id FALLBACK layers — requires a NEW `dmmv_q6k_experts` kernel (NOT on this branch; e27 has one on its branch but don't cross-copy worktrees).

**✅ PRIOR PERF LEVER — VALIDATED WIN (commit `6516c010`): LAUNCH-COLLAPSED the MoE ROUTED-EXPERT per-row syncs (async path) — qwen36-35b-a3b decode B=4 +7.6% / B=8 +21.8% (isolated A/B), token-identical.** The "BIGGER MoE-decode lever" the prior CURRENT STATE flagged as NEXT (i). In `moeFfnBlockBatchedDecode` the B per-row `moeFfnRoutedRowDecode` calls each ended with a blocking `waitPending` (to make the shared per-row scratch `router_*`/`gate_buf`/`up_buf`/`swiglu_buf`/`down_buf` safe to reuse next row). For the pure-GPU async (`batched_experts`) path that sync is UNNECESSARY: every command rides the context's ONE shared `CUstream` strictly in-order (`cuda_begin_command` sets `m->stream = c->stream` → `commitAsync` just records an event), so the next row's writes serialize AFTER this row's reads — no host round-trip needed. Dropped it for that path (new `moe_collapse` flag, default-on, `_force` for the harness A/B) and let the layer tail (`moeSharedExpertBatched`'s `waitPending`) drain all B rows together → removes B blocking CPU↔GPU round-trips per MoE layer per token (the boost-starved qwen MoE decode is launch-bound). The host-id FALLBACK path (`!batched_experts`, the 5 mixed-quant Q6_K-expert layers) KEEPS its per-row sync — it downloads `router_out_buf` to host-gather the expert ids + reuses the host `host_router_ids` scratch next row. **Pure sync removal, ZERO math change → ARGMAX-identical → batched==N-serial token-identical.** Rides the existing `decode_mrow`/btok gate (`moeFfnRoutedRowDecode` only runs when `moe_shared_batched`) → the default mrow-off path is UNTOUCHED, no regression. Production `decodeStep`/`prefillStep`/`moeFfnBlock` UNTOUCHED. Harness: new `COLLAPSE_TIMING` in `dbg_cuda batch` A/Bs collapse OFF (per-row sync restored) vs ON in ONE model load (boost-comparable), with `decode_mrow`+`moe_shared_batched` both forced on. **Validated 5090** (cuda-dbg md5 `43b32ae9` ≠ shared-expert `7e5c7ce4`): catalog **5/5 serial token-correct** (12/12 each); **qwen36-35b-a3b B=4 + B=8 `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial through the SSM + MoE path); **COLLAPSE_TIMING (in-process A/B) B=4 collapse OFF 58.74→ON 63.21 (+7.6%), B=8 OFF 70.14→ON 85.44 (+21.8%)** — robust across 2 sizes, the win GROWS with B (more rows → more per-row syncs collapsed); independently cross-checked on the 4090 (B=4 54.00→61.43 = +13.8%). Cumulative MOE_TIMING (full per-row OFF → shared-batched+collapse ON): B=4 53.82→63.84 (+18.6%), B=8 59.15→89.15 (+50.7%). This also UNBLOCKS MoE graph-capture (the per-row sync inside the routed loop previously broke a single-graph capture of the MoE step). **NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED-server head-to-head vs `llama-server --parallel` (the recurring gate — now BOTH the shared-expert AND launch-collapse wins ride along when btok defaults on); (ii) graph-capture the batched decode step per distinct B (now viable for the MoE step too — Effort-25/27 playbook, attacks the per-launch overhead the matvec doesn't); (iii) the residual MoE lever — the host-id FALLBACK path (5 mixed-quant Q6_K-expert layers) still syncs per row: a GPU-side expert-id path (skip the host gather) would let those layers ride the collapse too.**

**✅ PRIOR PERF LEVER — VALIDATED WIN (commit `b22898b4`): BATCHED the MoE SHARED EXPERT over B rows (rides `btok`) → qwen36-35b-a3b decode B=4 +7.0% / B=8 +14.6%, token-identical.** The MoE-serving throughput sub-step the prior CURRENT STATE flagged as NEXT (iii). The shared expert (gate/up/down) is a DENSE FFN run identically per row, so `moeFfnBlockBatchedDecode`'s per-row loop read its weights B× redundantly. Restructured (`forward_cuda.zig`, decode-batch path only): split each row into router+routed-experts only (new `moeFfnRoutedRowDecode` — the router+routed half of `moeFfnRowDecode` VERBATIM but reading its precomputed `db.ffn_norm` slice instead of recomputing the per-row rms_norm), accumulate routed output into its `db.hidden` row, THEN run the shared expert ONCE batched over all B rows (new `moeSharedExpertBatched` — gate/up/down as ONE `gemmDispatch` each over the [B,n_embd] norm → each shared weight read once; `swiglu` over the contiguous [B,sf]; the per-row sigmoid(gate-logit)·down scale stays a tiny element-wise per-row dispatch since the weight reads — the cost — are batched; the `ffnBlockBatchedDecode` pattern). The batched pre-norm is computed up front (one launch over B) BEFORE the routed accumulation overwrites `db.hidden`, so the shared expert reads the correct pre-norm. **KEY FINDING — it only wins through `btok`, not the tile GEMM:** the batched shared GEMMs route through `gemmDispatch`, which at small B uses the wasteful 64×64 tile GEMM unless `decode_mrow`/btok is on → measured **−16% at B=4 vs the per-row matvec** (tile-padding tax, exactly the gap `btok` was built for). So it is GATED on `decode_mrow` (`moe_shared_batched = self.decode_mrow and (force orelse moeSharedBatchedOn())`): rides the existing `ZINC_BATCH_MROW` path, and the default mrow-off path keeps the efficient per-row matvec → **no regression**. Two new small [B,*] `DecodeBatch` scratch (`moe_down`, `moe_gate_scalar`); NO kernel/CUDA change (pure Zig wiring reusing rms_norm/gemm/swiglu/sigmoid_scale_acc). Math byte-identical per row → ARGMAX-identical (GEMM reduction order) → batched == N-serial token-identical. Production `decodeStep`/`prefillStep`/`moeFfnBlock` + the full per-row `moeFfnRowDecode` (kept for the A/B-off arm) UNTOUCHED. Harness: `MOE_TIMING` in-process A/B in `dbg_cuda batch` (meaningful with `ZINC_BATCH_MROW=1`). **Validated 5090** (cuda-dbg md5 `7e5c7ce4` ≠ btok-extend `c1b20acd`): catalog **5/5 serial token-correct** (12/12 each — serial never sets `decode_mrow` → `moe_shared_batched` always false → `moeFfnBlock` path identical); **qwen36-35b-a3b B=4 (4838,1665,15,16|…) + B=8 (slots=8) with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial through the SSM + MoE path); **MOE_TIMING (ZINC_BATCH_MROW=1, in-process A/B) B=4 shared OFF 50.55→ON 54.10 (+7.0%), B=8 OFF 58.69→ON 67.23 (+14.6%)** — robust across 2 sizes, the win GROWS with B (more rows → more shared-weight-read amortization). NOTE: the un-gated default-on version was a NEGATIVE (B=4 30.48→25.52, −16%) → the `decode_mrow` gate is load-bearing. The MoE shared expert now rides btok consistently with the dense GEMMs. **NEXT (pick one): (i) the BIGGER MoE-decode lever — batch the ROUTED experts across B rows (the bulk: router GEMM `[B,n_experts]` + `build_expert_order` over B·n_used + per-tensor `dmmv_*_experts`); BUT it has the sparse-MoE collision problem (8 active of 256 → rows rarely share an expert → little cross-row weight-amortization), so the real win there is LAUNCH-collapse — making `moeFfnRoutedRowDecode`'s B per-row `waitPending` syncs into ONE tail sync via per-row [B,*] scratch (the boost-starved qwen MoE decode is launch-bound; mirrors 4c-step-2 for attn/SSM, AND it enables MoE graph-capture which the per-row sync currently blocks); (ii) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED-server head-to-head vs `llama-server --parallel` (the recurring gate — now the shared-expert win rides along when btok defaults on); (iii) graph-capture the batched decode step per distinct B (orthogonal).**

**✅ PRIOR PERF LEVER — VALIDATED WIN (commit `a4b5eb34`): EXTENDED `btok` to Q5_K/Q6_K/Q8_0 → covers the non-Q4_K decode GEMMs that still hit the tile-padding GEMM; the small-B aggregate-decode win MULTIPLIES (gemma-31b B=4 13.0×, B=8 7.93×; qwen35-9b B=4 9.49×), token-identical.** The Q4_K-only `dmmv_q4k_btok` (`28fc7352`/`5ef499dc`) left the residual non-Q4_K weights — **gemma-31b's ffn_down is Q6_K** (the single largest dense GEMM) + qwen's O-proj/FFN-down/SSM-out on mixed-quant layers — STILL running through the wasteful 64×64 tile GEMM at small B (one token-row per tile column → 56-62/64 row-slots wasted). Added three new kernel families to `kernels.cu`: `dmmv_q6k_btok<B>` / `dmmv_q5k_btok<B>` / `dmmv_q8_0_btok<B>` (B=2..8), each the token-BATCH transpose of its `*_fast` matvec (read each weight row + dequant ONCE, amortize across B token x-vectors) using the EXACT per-element dequant of `dmmv_q6k_fast`/`dmmv_q5k_fast`/`dmmv_q8_0_fast` + the proven `dmmv_q4k_btok` per-token-local-sum reduction → token-identical to the `*_fast` matvec the serial `decodeStep` uses. Wiring (both archs): the `decode_mrow` guard in `gemmDispatchA` (gemma) / `gemmDispatch` (qwen) is now a `switch (w.info.type_)` over `{q4_k,q6_k,q5_k,q8_0}` → the matching `btok[B-2]` pipeline (else fall through to the GEMM); same OPT-IN `ZINC_BATCH_MROW=1` knob, same `defer`-reset, qwen passes `acc_mode` through (gemma all acc_mode 0). Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never set `decode_mrow` → the new switch arm is dead on the serial path). **Validated 5090** (cuda-dbg md5 `c1b20acd` ≠ gemma-btok `f7c12c2d`): catalog **5/5 serial token-correct** (12/12 each — qwen35/qwen36/qwen36-a3b/gemma-31b/gemma-26b); **gemma-31b `ZINC_BATCH_MROW=1` BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial, Q6_K ffn_down via btok exercised) **BTOK_TIMING B=4 8.64→112.29 (13.0×), B=8 16.83→133.47 (7.93×)**; **qwen35-9b B=4 BATCH_GATE+SANITY PASS, BTOK_TIMING 19.24→182.64 (9.49×)** — a BIG jump over the Q4_K-only btok (gemma B=8 was 2.80× / qwen B=4 was 1.97×) because the large Q6_K ffn_down (+ qwen residuals) now ride btok instead of the wasteful tile GEMM. The whole catalog's small-B decode GEMMs (every common quant) now have the bandwidth-bound fast path. **NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON — the in-process A/B is now a large, consistent, token-identical win across both archs; the remaining gate is a SUSTAINED-server head-to-head re-measure vs `llama-server --parallel` confirming it holds end-to-end through `ServeEngine` (NOT just the in-process A/B); (ii) graph-capture the batched decode step per distinct B (Effort-25/27 playbook, orthogonal/stackable — attacks per-launch overhead the btok matvec doesn't); (iii) the MoE-serving throughput sub-step (`moeFfnBlockBatchedDecode` still loops rows per-token).**

**✅ PRIOR PERF LEVER — VALIDATED WIN (commit `5ef499dc`): PORTED `dmmv_q4k_btok` to GEMMA `decodeBatch` (`forward_cuda_gemma.zig`) → ~2.8–3.3× aggregate decode at B=3..8, token-identical.** Mirrors the qwen port (`28fc7352`, below) onto `ForwardGemma`. gemma-31b dense decode hit the SAME small-B GEMM penalty (the 64×64 batched GEMM processes one token-row per tile column → wastes 56-62/64 row-slots → compute-bound on tile padding). The btok kernel was already in `kernels.cu` (generic `DmmvPush`, token-major x/y), so this is a pure wiring port: `dmmv_q4k_btok[7]` pipelines (B=2..8, idx B-2) + a `decode_mrow` flag (set in `decodeBatch` ONLY when `2≤B≤8` AND `mrowMatvecOn()`; **OPT-IN, default-off, `ZINC_BATCH_MROW=1`** — SAME env knob as qwen; `defer`-reset) + a `gemmDispatchA` guard (placed AFTER the B==1 matvec check, BEFORE the `gi` GEMM switch) routing Q4_K decode GEMMs through `btok`. KEY DIFFS vs qwen: gemma's decode GEMMs are ALL **acc_mode 0** (the residual is the separate `rms_norm_residual`/`rms_norm_residual_scale`, NOT a GEMM accumulate — unlike qwen's O-proj/FFN-down/SSM-out), so the btok push is the plain default; AND the guard skips when `a_preconv and use_tc_normf16` (the normf16 opt-in stages the fp16 norm into `act_f16` instead of materializing the f32 norm buffer btok reads — a no-op for the default/validated config where normf16 is off). Same Q4_K dequant as `dmmv_q4k_fast` → bit-identical per row → ARGMAX-identical to the GEMM. Harness `decode_mrow_force` + a `BTOK_TIMING` in-process A/B in `dbg_cuda batch`'s GEMMA branch (mrow OFF then ON in ONE model load = boost-comparable). Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never set `decode_mrow`). **Validated 5090** (cuda-dbg md5 `f7c12c2d` ≠ qwen-btok `16e98c77`): catalog **5/5 serial token-correct** (12/12 each — qwen35/qwen36/qwen36-a3b/gemma-31b/gemma-26b); **gemma-31b B=3/4/8 with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial, btok path exercised); **BTOK_TIMING agg tok/s (in-process A/B): B=3 6.49→21.42 (3.30×), B=4 8.62→27.62 (3.20×), B=8 16.90→47.35 (2.80×)** — robust ~2.8–3.3× across 3 sizes (BIGGER than qwen's 1.8–2× — gemma-31b's larger matvecs waste more tile-padding, so btok recovers more). **btok now lands on BOTH archs (qwen `28fc7352` + gemma `5ef499dc`); the whole catalog's Q4_K decode GEMMs have the small-B fast path.** **NEXT (pick one): (i) extend `btok` to Q5_K/Q6_K/Q8_0 (gemma-31b's ffn_down is Q6_K + qwen residual O-proj/FFN-down on some layers are not Q4_K → still hit the tile-padding GEMM; the next biggest single lever before default-on); (ii) flip `ZINC_BATCH_MROW` DEFAULT-ON after a sustained-server head-to-head re-measure vs `llama-server --parallel` confirms the aggregate win holds end-to-end through `ServeEngine` (NOT just the in-process A/B); (iii) graph-capture the batched step (Effort-25/27 playbook, orthogonal/stackable).**

**✅ PRIOR PERF LEVER — VALIDATED WIN (commit `28fc7352`): small-B Q4_K token-batch matvec (`dmmv_q4k_btok`) for qwen `decodeBatch` → ~1.8–2× aggregate decode throughput at B=2..8, token-identical.** Attacks the head-to-head's headline gap: batched decode is COMPUTE-bound at small B because the 64×64 `gemm_q4k_tiled_v2` processes one token-row per tile column → wastes 56-62/64 row-slots and goes compute-bound on tile padding (B=8 = 100% util but slow). NEW kernel `dmmv_q4k_btok<B>` (`kernels.cu`) = the TRANSPOSE of `dmmv_q4k_mrow`: reads each Q4_K weight row ONCE (the dominant decode traffic) and amortizes its dequant across the B token x-vectors → bandwidth-bound, no tile waste. SAME Q4_K dequant arithmetic as `dmmv_q4k_fast` (bit-exact); `btok` at row b == `dmmv_q4k_fast` on x[b] bit-for-bit → ARGMAX-identical to the GEMM. Instantiated B=2..8 (pipeline idx B-2). `forward_cuda.zig`: `dmmv_q4k_btok[7]` pipelines + a `decode_mrow` flag (set in `decodeBatch` ONLY when `2≤B≤8` AND `mrowMatvecOn()`; **OPT-IN, default-off, `ZINC_BATCH_MROW=1`**, `defer`-reset) + a `gemmDispatch` guard routing Q4_K projection/FFN/residual GEMMs through `btok` (honors `acc_mode`; x/y token-major contiguous); non-q4k weights fall through to the GEMM. Harness `decode_mrow_force` + a `BTOK_TIMING` in-process A/B in `dbg_cuda batch` (mrow OFF then ON in ONE model load = boost-comparable). Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never set `decode_mrow`). **Validated 5090** (cuda-dbg md5 `16e98c77` ≠ inc4 `1213b5c1`): catalog **5/5 serial token-correct** (12/12 each); **qwen35-9b B=3/4/8 with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial, btok path exercised); **BTOK_TIMING agg tok/s (in-process A/B): B=3 13.36→24.17 (1.81×), B=4 19.29→37.95 (1.97×), B=8 37.46→70.84 (1.89×)** — robust ~1.8–2× across batch sizes (3 sizes, not a single noisy point; the worst-case B=8 nearly doubles). **NEXT (pick one): (i) PORT `btok` to gemma `decodeBatch` (`forward_cuda_gemma.zig` has the same small-B GEMM penalty — its proj/FFN are Q4_K too); (ii) extend `btok` to Q5_K/Q6_K/Q8_0 (the residual O-proj/FFN-down on some qwen layers + gemma ffn_down are not Q4_K → still hit the GEMM); (iii) flip `ZINC_BATCH_MROW` DEFAULT-ON after a sustained-server head-to-head re-measure vs `llama-server --parallel` confirms the aggregate win holds end-to-end through `ServeEngine` (NOT just the in-process A/B); (iv) graph-capture the batched step (Effort-25/27 playbook, orthogonal/stackable). NOTE: btok covers only Q4_K weights — Q5_K/Q6_K/Q8_0 residual GEMMs still pay the tile-padding tax, so extending the quant coverage (ii) is the next biggest single lever before default-on.**

**INCREMENT 4 COMPLETE (prior cycle) — wired qwen into the CUDA server; the multi-tenant HTTP/SSE server now serves the WHOLE catalog (gemma4 dense + qwen35/36 hybrid-SSM + MoE), not just gemma-dense; commit `d37623ee`.** Pivoted from the perf levers to the remaining BUILD milestone (the qwen `decodeBatch` was already correctness-complete for dense+MoE — 4a–4d — but `runCudaServe`/`ServeEngine` were gemma-dense-only). FIX (additive, surgical):
- **`src/server/cuda_serve.zig`**: new `Forward` union (`gemma: *ForwardGemma` | `qwen: *ForwardCuda`) dispatching `allocSlots`/`freeSlots`/`decodeBatch`/`resetSlot` by arch (the slot-state alloc differs only in NAME — `allocSlotKv` vs `allocSlotState`; the `decodeBatch` signatures are identical). `ServeEngine.g: *ForwardGemma` → `fwd: Forward`; init/deinit/workerLoop dispatch through it. **The worker calls `fwd.resetSlot(slot_id)` for each freshly-admitted slot BEFORE per-token prefill.**
- **`src/compute/forward_cuda.zig`**: new `resetSlot(slot)` — zeros a REUSED slot's SSM conv ring + recurrent state for all SSM layers. **THE genuinely-new correctness piece: qwen's SSM recurrent state is ACCUMULATED (not position-indexed), so a slot reused across requests (nslots < concurrent clients) would inherit the prior request's state → corruption.** The attention slot KV needs NO reset (position-indexed, overwritten on prefill — same reason gemma reuses cleanly). No-op for gemma (the union's `.gemma` arm). This path was NEVER exercised before — the qwen `decodeBatch` proofs (4b–4d) used `dbg_cuda batch` which gives each seq its OWN fresh slot (no reuse).
- **`src/main.zig`**: `runCudaServe` takes `cuda_serve.Forward`; gemma branch passes `.{ .gemma = &fwd }`, qwen branch `.{ .qwen = &fwd }` (was `error.ServerModeUnavailableOnThisBackend`).
- **`src/dbg_cuda.zig`**: generalized the `serve` harness to qwen (Engine union now dispatches `allocSlots`/`freeSlots`/`decodeBatch`/`resetSlot`; `ServeCtx.g` → `eng: *Engine`; `serveWorker` resets reused slots; `serveMode` calls `resetState()` between isolated references so qwen's unindexed production SSM state doesn't leak; dropped the gemma-only + MoE skips) → `validate_cuda_serve.sh` GATE B now provides the isolated reference for qwen = a SECOND independent slot-reuse-reset proof.

Production `decodeStep`/`prefillStep`/`prefillBatched` + gemma serving UNTOUCHED. **Validated 5090** (zinc md5 `8de51146` ≠ 3c `6c2ab9a4`; cuda-dbg md5 `1213b5c1` ≠ qwen-suspect#2 `aba47627`): catalog **5/5 serial token-correct** (12/12 each); **qwen35-9b serving, nslots=2 < 4 clients (FORCES slot reuse), NG=10: SERVE_HTTP_GATE_A PASS (concurrent N=4 == sequential B=1 through the server) + GATE_B PASS (server B=1 == `dbg_cuda serve` isolated reference == decodeStep) + SERVE_GATE PASS (published=4)** — every qwen stream token-identical across concurrent/sequential/isolated → the slot-reuse SSM reset is correct. **The CUDA server now serves the whole catalog; Increments 1+2+3+4 COMPLETE.** **NEXT (perf-led — the head-to-head gap is the only thing between the proven architecture and beating `llama-server`): (B) graph-capture the batched decode step per distinct B; OR verify lever C via the sustained re-measure; OR (E) the MoE-serving throughput sub-step (4d's `moeFfnBlockBatchedDecode` still loops rows per-token — batch the router GEMM `[B,n_experts]` + `build_expert_order` over B·n_used + per-tensor `dmmv_*_experts`).** NOTE qwen serving inherits the SAME per-step kernel inefficiency at high B (batched-GEMM compute-inefficiency / no graph capture) — serving more models doesn't close the head-to-head gap; the per-step kernel lever does.

**PRIOR PERF/ROBUSTNESS LEVER (lever C suspect #2 — PORTED TO QWEN) — batched the qwen `decodeBatch` EMBED + TAIL into single command buffers + ONE B-wide download + persistent scratch; commit `6088db38`.** The qwen analog of the gemma `bf4ad2a6` fix below. `ForwardCuda.decodeBatch` (`forward_cuda.zig`) had the SAME per-row B-serial GPU round-trips PER DECODED TOKEN: (a) EMBED looped B rows — the GPU path (`embed_gpu`, default for qwen35-9b Q4_K) did B `commitAndWait`s (one embed_q4k dispatch per row), the CPU path B separate uploads into the single-row `host_embed`; (b) the TAIL looped B rows each doing its OWN `rms_norm→LM-head→argmax` + `commitAndWait` + argmax `download`. FIX (additive, surgical, qwen only): three persistent slot-scoped scratch buffers (sized `n_slots ≥ B`, alloc'd in `allocSlotState` / freed in `freeSlotState`): `argmax_scratch[n_slots]` u32 (tail argmaxes → ONE B-wide `download` replaces B), `tok_scratch[n_slots]` u32 (B token ids uploaded ONCE → GPU embed is now ONE command buffer dispatching embed_q4k per row; the kernel reads `tok[0]` so each dispatch aliases `tok_scratch` at `bi·4` → ONE `commitAndWait` was B), `embed_host[n_slots·n_embd]` f32 (CPU embed staged for all B rows → ONE upload to the contiguous `[B,n_embd]` `db.hidden` was B). The TAIL chains all B rows into ONE command buffer + ONE `commitAndWait`, reusing `norm_buf`/`logits_buf` (in-order on one stream → hazard-free, identical math). Production `decodeStep`/`prefillStep` (single-seq `argmax_buf`/`host_embed`/`tok_in_buf`) UNTOUCHED. **Validated 5090** (cuda-dbg md5 `aba47627` ≠ gemma-suspect-#2 `060d96fb`): catalog **5/5 serial token-correct** (12/12 each); **qwen35-9b dense 3 seqs mixed positions (4/3/5) BATCH_GATE+BATCH_SANITY PASS (B1_TIMING matvec OFF 4.93→ON 85.80 tok/s) + qwen36-35b-a3b MoE 3 seqs mixed positions BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial). **Lever C suspect #2 is now DONE in BOTH archs** (gemma `bf4ad2a6` + qwen `6088db38`). REMAINING for lever C: the sustained-server re-measure to CONFIRM the round-2 collapse is gone (the actual verification — 17GB reload, bundle util-gated with lever B). STILL OPEN at HIGH B: the batched-GEMM compute-inefficiency at B≥2 (lever B, graph-capture). **NEXT (pick one): (B) graph-capture the batched decode step per distinct B (the HIGH-B aggregate lever, B≥2 GEMM inefficiency — closes the head-to-head gap vs llama-server --parallel); OR verify lever C via the sustained gemma-server re-measure; OR (D, deprioritized) wire qwen into the server.**

**PRIOR LEVER (lever C suspect #2, gemma) — batched the gemma `decodeBatch` TAIL into ONE command + ONE B-wide download AND hoisted the per-step host embed buffer to persistent scratch; commit `bf4ad2a6`.** Continues the sustained-throughput degradation root-cause (suspect #1 = the per-step pos/slots device alloc/free, fixed `7be561f2`). Suspect #2 was the remaining per-step host/round-trip churn in `ForwardGemma.decodeBatch`: (a) the TAIL looped B rows each doing its OWN `rms_norm→LM head→argmax` + `commitAndWait` + argmax `download` → **B serial GPU round-trips per decoded token**; (b) a per-step `allocator.alloc(f32, B·n_embd)`/`free` for the embed staging. FIX (additive, surgical, gemma only): (a) the tail now chains ALL B rows' rms_norm/LM-head/argmax into ONE command buffer + ONE `commitAndWait` + ONE B-wide `download` — on ONE stream the dispatches execute strictly in order, so reusing the single `norm_buf`/`logits_buf` scratch across rows is hazard-free (row b+1's rms_norm can't run until row b's LM head consumed `norm_buf`); only the argmax OUTPUT must be per-row, so each row writes its own slot of a NEW persistent `argmax_scratch[n_slots]` (aliased at bi·4), downloaded once → **bit-identical math, B→1 tail round-trip**; (b) `embed_host[n_slots·n_embd]` f32 persistent host buffer (alloc'd in `allocSlotKv`, freed in `freeSlotKv`) replaces the per-step host alloc/free. Both new buffers sized to `n_slots` (max batch ≥ B). Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never touch slot scratch). **Validated 5090** (cuda-dbg md5 `060d96fb` ≠ suspect-#1 `9e0f6464`): catalog **5/5 serial token-correct** (12/12 each); **gemma-31b 3 seqs mixed lengths (4/3/5): SLOTKV_SMOKE:ok BATCH_GATE:PASS + BATCH_SANITY:PASS** (token-identical batched==solo==serial). NOTE: this is gemma only — the qwen `decodeBatch` tail (`forward_cuda.zig`) has the SAME per-row tail loop + per-step embed alloc → port this next as the qwen analog. The actual sustained-server re-measure (confirm the round-2 collapse is GONE) is STILL the deferred verification for lever C (17GB reload → bundle with lever B's util-gated A/B). STILL OPEN at HIGH B: the batched-GEMM compute-inefficiency at B≥2 (lever B, graph-capture).

**PRIOR PERF LEVER (lever C suspect #1) — hoisted the per-step `decodeBatch` `pos_buf`/`slots_buf` device buffers to PERSISTENT scratch in BOTH archs; commit `7be561f2`.** Root-cause work on the documented sustained-throughput DEGRADATION (round-2 collapse B=1 1.72→0.96, monotonic, not contention). Code-read confirmed the #1 suspect: every `decodeBatch` step did `buffer.createBuffer`×2 + `buffer.freeBuffer`×2 for the per-seq `positions[]`/`slots[]` upload (a cudaMalloc+cudaFree pair per decoded token) — interleaved with the serving loop's admit/evict slot churn, this fragments the device allocator and slows monotonically over a sustained run. FIX (additive, surgical, BOTH `ForwardGemma` AND `ForwardCuda`): added `pos_scratch`/`slots_scratch` `?CudaBuffer` fields allocated ONCE in `allocSlotKv`/`allocSlotState` (sized to `n_slots`, the max batch ≥ B), freed in `freeSlotKv`/`freeSlotState`; `decodeBatch` now `std.debug.assert(B<=n_slots)` and re-uploads into the persistent scratch (kernels read only the first B entries) instead of malloc/free per step. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (they never touch slot scratch). **Validated 5090** (cuda-dbg md5 `9e0f6464` ≠ prior `07e46933`): catalog **5/5 serial token-correct** (12/12 each); **gemma-31b 3 seqs mixed lengths (4/3/5) BATCH_GATE+BATCH_SANITY PASS + qwen35-9b 3 seqs mixed lengths BATCH_GATE+BATCH_SANITY PASS** (token-identical batched==solo==serial; SLOTKV/SLOTSTATE smokes ok). The fix is correctness-preserving + structurally removes the alloc-fragmentation source; a sustained-throughput re-measure on the gemma server (confirm the round-2 collapse is GONE) is the explicit remaining VERIFICATION for this lever — deferred this cycle to avoid the 17GB-reload wedge risk; bundle it with lever B's util-gated A/B. STILL OPEN at HIGH B: the batched-GEMM compute-inefficiency at B≥2 (lever B, graph-capture).

**PRIOR PERF LEVER — B==1 matvec fast path PORTED to qwen `decodeBatch` (`forward_cuda.zig`), ~13× the B=1 path; commit `2cb95904`.** The gemma B==1 fast path (`0fee12cc`) had a 1:1 qwen analog (qwen's 4c `gemmDispatch` over B rows runs at B==1 for every per-token prefill + single-client decode → wastes 63/64 of the 64×64 tile). FIX (additive, mirrors gemma): a `decode_b1` flag on `ForwardCuda` (set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()`, default-on opt-out `ZINC_BATCH_B1_MATVEC=0` — the SAME env knob as gemma; `defer`-reset so it never leaks past the call) + a guard at the TOP of `gemmDispatch`: `if (B==1 and self.decode_b1) → dmmvDispatch` (the SAME tuned matvec `decodeStep` uses). KEY DIFF vs gemma: the qwen guard passes `acc_mode` THROUGH (qwen's O-proj / FFN-down / SSM-out GEMMs use `acc_mode=1` for the residual, unlike gemma's all-acc_mode-0 decode GEMMs which let it hard-code 0); `dmmvDispatch` already honors `acc_mode`+`a_offset`(=0 at B==1, contiguous) and handles EVERY decode weight type incl. f32 (`dmmvIdx` f32→4 = `dmmv_f32`; production `decodeStep`'s SSM projections already route f32 through it). The fused per-seq attn/SSM `_seq` kernels (grid.y/z=B) already work at B=1 and are unchanged. Harness-only `decode_b1_force: ?bool` + a `B1_TIMING` A/B in the `dbg_cuda batch` QWEN branch (mirrors gemma) time both paths in ONE model load. Production `decodeStep`/`prefillStep` UNTOUCHED — serial path never sets `decode_b1`. **Validated 5090** (cuda-dbg md5 `07e46933` ≠ gemma-fast-path `bb3982e1`): catalog **5/5 serial token-correct** (12/12 each); qwen35-9b 3 seqs mixed positions (4/3/5) **BATCH_GATE PASS + BATCH_SANITY PASS** (PASS-A-solo runs B=1 `decodeBatch` → exercises the matvec path; token-identical batched==solo==serial); **B1_TIMING in-process A/B (NG=64): matvec OFF 4.52 → ON 59.98 tok/s (~13.3×)**. Both gemma (4d→`0fee12cc`) AND qwen (`2cb95904`) `decodeBatch` now have the B==1 fast path → no per-stream penalty at low B for the whole catalog. STILL OPEN at HIGH B (BOTH archs): the head-to-head's B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (matvec path doesn't apply) — separate lever (graph-capture / better batched GEMM).

**PRIOR PERF LEVER — B==1 matvec fast path in gemma `decodeBatch`, ~21× the B=1 path; commit `0fee12cc`.** Pivoted off inc-4 server-wiring to the higher-EV perf lever the 2026-06-15 head-to-head flagged (zinc serving ~30-42× behind llama-server; the architecture is proven-correct+scales but the per-step kernel is inefficient, and the per-STREAM path is ~4× slower than the tuned single-stream `decodeStep`). ROOT CAUSE found by code-read: every B==1 `decodeBatch` step — **all per-token prefill AND any single-client decode** — ran its per-layer projection/FFN through the 64×64-tiled batched TC GEMM (`gemm_q4k_tc_f16a_lowsmem`), which processes ONE row and wastes 63/64 of its tile (cuBLAS self-gates off at T=1<128). FIX (additive, surgical): a `decode_b1` flag on `ForwardGemma` — set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0`), `defer`-reset so a stale true never leaks into a later `prefillBatched` — plus a guard at the TOP of `gemmDispatchA`: when `T==1 and self.decode_b1`, route to `dmmvDispatch` (the SAME tuned matvec `decodeStep` uses). Drop-in because every decode-path GEMM (attn Q/K/V/O + FFN gate/up/down) overwrites y (acc_mode 0) and is contiguous (a_offset 0) at T==1. The fused per-seq attention kernels (`rms_norm_rope_qkv_seq`/`gemma_attention_batched_seq`) already work at B=1 (grid.y=1) and are unchanged. A harness-only `decode_b1_force: ?bool` override lets `dbg_cuda batch` time both paths in ONE model load (boost-comparable A/B). Production `decodeStep`/`prefillBatched` UNTOUCHED — serial path never sets `decode_b1`. **Validated 5090** (cuda-dbg md5 `bb3982e1` ≠ 4d `53992af4`): catalog **5/5 serial token-correct** (12/12 each); gemma-31b 3 seqs mixed positions **BATCH_GATE PASS + BATCH_SANITY PASS** (PASS-A-solo runs B=1 `decodeBatch` → exercises the matvec path; token-identical batched==solo==serial); **B1_TIMING in-process A/B: matvec OFF 2.19 → ON 46.37 tok/s** (the OFF figure matches the documented "B=1 ≈2.2" exactly → the fast path eliminates the batched-GEMM-at-B=1 penalty). NOTE: ON 46 tok/s even exceeds the older "decodeStep ≈8" figure (different warm/boost context; the async fused-kernel attention path helps) — the clean claim is the boost-comparable one-load ratio, not the cross-context 46-vs-8. This fixes the crossover (batched serving no longer loses to single-stream at low B) AND speeds ALL per-token prefill 4-21×. STILL OPEN at HIGH B: the head-to-head's B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (where the matvec path doesn't apply) — separate lever (graph-capture / better batched GEMM). The qwen `decodeBatch` (`forward_cuda.zig`) has the SAME B==1 penalty → port this fast path there next (it's the analog change in its `gemm`/`gemmDispatch`).

**Increment in progress:** 4 — qwen/MoE serving. **4d DONE + validated (cycle 13) → qwen `decodeBatch` now handles MoE (qwen36-35b-a3b), so it is correctness-complete for BOTH dense qwen35 AND MoE qwen36.** Lifted the `if (d.n_experts>0) return error.Unsupported` guard in `ForwardCuda.decodeBatch`; the per-layer FFN now routes to a new `moeFfnBlockBatchedDecode` when `n_experts>0` (else `ffnBlockBatchedDecode`). KEY: the MoE FFN is STATELESS (no KV, no SSM recurrent state — reads `hidden`, routes to experts, accumulates back into `hidden`), so unlike attn/SSM it needs NO per-slot state and NO per-seq position → each of the B rows is an INDEPENDENT single-token MoE FFN. `moeFfnBlockBatchedDecode` loops the rows, aliases row b's `db.hidden` slice, and runs the production `moeFfnBlock` body VERBATIM on it via `moeFfnRowDecode` (rms_norm → router top-k → routed experts SwiGLU+down → weighted combine → shared expert; batched-q4k/q5k fast path OR per-quant host-id fallback, byte-identical to production), reading/accumulating into the row alias + a synchronous `waitPending` per row so the shared per-row scratch (`ffn_norm_buf`/`gate_buf`/`host_router_ids`/…) is safe to reuse next row → **batched==N-serial token-identical by construction** (mirrors the 4b→4c method for attn/SSM). `dbg_cuda batch` qwen branch drops the MoE skip → runs the same PASS-A-solo/PASS-B-batched proof on qwen36-35b-a3b. Production `moeFfnBlock`/`decodeStep`/`prefillStep` UNTOUCHED. 4c (attn+SSM fused, all GEMMs batched) + 4a + 4b + Increments 1 + 2 + 3 COMPLETE; **the qwen `decodeBatch` decode step is now correctness-complete for the whole catalog.** NEXT = **wire qwen into the server** (`runCudaServe`/`ServeEngine` are currently gemma-dense-only — generalize the engine's per-model decode call + slot-state alloc to dispatch ForwardCuda(qwen)/ForwardGemma by arch, so the HTTP server serves qwen35/36 + MoE, not just gemma-dense) — OR the SEPARATE perf lever (B==1 matvec fallback / graph-capture batched step / the per-row MoE loop's launch collapse). NOTE 4d is the CORRECTNESS step — `moeFfnBlockBatchedDecode` still LOOPS rows per-token (each row a full MoE FFN, weights NOT yet read once across B); fusing the router GEMM `[B,n_experts]` + `build_expert_order` over B·n_used pairs into batched expert matvecs is a later throughput sub-step (the MoE analog of 4c's GEMM/attn/SSM amortization).

**4c-step-1 (this cycle) gave qwen the batched-GEMM amortization the spec calls for — the qwen analog of gemma's 1b structure.** Qwen `forward_cuda.zig` on this branch had NO batched-GEMM path (e26's qwen prefillBatched is on main, not this branch fork-point), so this cycle BUILT it: new `gemm:[4]CudaPipeline`+`gemm_f32` (the generic `gemm_*_tiled_v2` kernels, already in kernels.cu, used by gemma — token-major `[T,M]` out, `acc_mode` for the residual), a `GemmPush`, a `gemmDispatch(w,x,y,M,K,B,acc_mode)`, and a lazily-allocated token-major `[B,dim]` `DecodeBatch` scratch (dedicated `ssm_delta`/`ssm_y` buffers so the SSM intermediates never alias with conflicting strides). `decodeBatch` REWRITTEN layer-major: embed all B rows into `db.hidden` slices (same embed kernel as serial), then per layer the pre-norm + Q(+gate)/K/V/O, ssm qkv/z/alpha/beta/out, and ffn gate/up/down run as ONE `gemm` each over B rows (weights read once); the per-seq inner (deinterleave/q-k-norm/RoPE/KV-write/softmax/gate for attn; conv1d/delta_net/gated_norm for ssm) still LOOPS per row on row b's slice + its slot KV / conv ring / recurrent state (block math copied verbatim → token-identical). FFN is FULLY batched. Per-row argmax tail. The 4b looped helpers `attentionLayerSlot`/`ssmLayerSlot` are now superseded/dead (kept as reference). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED; the serial catalog path never runs the new `gemm` pipelines. NOTE: GEMM-vs-matvec reduction order makes the batched path ARGMAX-identical, not bit-identical (gate is token-identical, PASSED).

**4b (this cycle) built the qwen batched-decode driver — the qwen analog of inc-1's 1b.** New `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])` (`forward_cuda.zig`, additive) for DENSE qwen35: a per-row loop runs each sequence's forward against its OWN slot state via two slot-variant helpers whose block math is copied VERBATIM from production `attentionLayer`/`ssmLayer` (so batched==N-serial is token-identical by construction). `attentionLayerSlot(L,pos,slot)` — KV aliased to the slot's region (`kv_base = slot·slot_ctx·kv_dim·4`), kv-write + `naive_attention` pos-relative within the slot (`seq_len=pos+1`, sinks + sigmoid-z gate), synchronous `commitAndWait` (keeps the per-row slot aliases valid). `ssmLayerSlot(L,pos,slot)` — conv ring + recurrent state aliased to the slot (`slotConvOffsetBytes`/`slotStateOffsetBytes`), the genuinely-new hybrid-SSM piece; conv ring offset DERIVED `conv_off = pos % (d_conv-1)` (production advances `conv_off[L]` one step/token → equals `pos%(d_conv-1)`; deriving it makes each slot self-consistent without per-slot host state). MoE (`d.n_experts>0`) → `error.Unsupported` (qwen36 is 4d). Also added `resetState()` (zero the production single-seq SSM conv+recurrent state + clear `conv_off`) used by the harness to make each SERIAL reference TRULY single-sequence — without it qwen's unindexed SSM recurrent state leaks from the prior reference sequence; no-op for gemma's position-indexed KV. The `dbg_cuda batch` qwen branch now runs the 4a slot-state smoke, then the SAME PASS-A-solo / PASS-B-batched proof as gemma (per-token B=1 prefill into each slot, then decode all seqs together at MIXED positions) → `BATCH_GATE`(batched==solo) + `BATCH_SANITY`(solo==serial). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED.

**4a (prior cycle) plumbed the per-sequence slot state — the qwen analog of inc-1's 1a.** Added to `ForwardCuda` (`forward_cuda.zig`, qwen35/36 hybrid-SSM) ADDITIVE per-sequence slot buffers + helpers: `kv_k_slots`/`kv_v_slots` (attn layers, `[n_slots·slot_ctx·kv_dim]`) + `ssm_conv_slots`/`ssm_state_slots` (ssm layers, `[n_slots·conv_state_len]` / `[n_slots·ssm_state_len]`, zero-init) + `n_slots`/`slot_ctx`; `allocSlotState`/`freeSlotState` (uniform stub for the wrong layer type, re-callable), `slotKvOffsetBytes(slot,pos)`/`slotConvOffsetBytes(slot)`/`slotStateOffsetBytes(slot)`, and `slotStateSmoke` (non-overlap round-trip proving distinct slots → distinct device regions for KV, the conv ring, AND the recurrent state). The `dbg_cuda batch` qwen branch (was a bare skip) now allocs slot state + runs the smoke. The genuinely-new hybrid-SSM piece vs gemma's slot KV: the **per-slot SSM conv + recurrent state** (gemma is attention-only). Production single-sequence `kv_k`/`kv_v`/`ssm_conv_state`/`ssm_state` + `decodeStep`/`prefillStep` UNTOUCHED. **The qwen serial reference `BATCH_SEQ` (production prefill+decodeStep) the 4b proof must reproduce is now emitted by the harness and cross-checks ==`gen`.**

**3c proved the HEADLINE: request-batching scales aggregate decode throughput ~linearly.** Drove the running `zinc` CUDA server with B=1/2/4/8 concurrent clients; new `/stats` counters (worker-only writes, atomic reads) let the gate diff two snapshots around each B-phase → pure server-side decode tok/s + mean batch occupancy. On gemma-31b dense (5090, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS): **srv decode tok/s 2.2 / 4.2 / 8.3 / 15.9 = 1.00 / 1.95 / 3.81 / 7.34× vs B=1**, occupancy 1.00 / 1.98 / 3.96 / 7.92 (batch fills to B) → **TP_GATE PASS**. This IS the core Effort-28 thesis demonstrated end-to-end over the real HTTP server: B sequences read the weights ONCE and amortize launches across B tokens.

**3b stood up the FIRST CUDA HTTP server.** `zinc -m gemma.gguf` with NO `--prompt` now runs a real multi-tenant HTTP/SSE server (previously `main.zig:1662` errored "server mode not yet wired on CUDA"). `routes.zig`/`InferenceEngine` are Vulkan/Metal-only, so per the plan's option (a) we brought up a minimal CUDA serving path directly in `main.zig` (`runCudaServe`) backed by a reusable engine module — NOT via routes.zig. The single-sequence prompt path is unchanged.

**Done so far:**
- **Increment 1 COMPLETE** — `decodeBatch(tokens[B], positions[B], slots[B], out[B])` for dense gemma-31b: batched projections/FFN (weights read once over B rows) + two batched per-seq attention kernels (`rms_norm_rope_qkv_seq` + `gemma_attention_batched_seq`, grid.y=b, per-seq `positions[]`/`slots[]`), 4 batched launches + single tail `waitPending`. Token-identical batched==solo==prod-gen (`b251442d` 1a, `51b4af11` 1b, `708e798a` 1c).
- **Increment 2 COMPLETE** — `src/scheduler/scheduler.zig` wired as a real continuous-batching scheduler (additive): `pending` FIFO + `scratch`; `enqueue`/`admitNext`/`hasFreeSlot`/`isIdle`; real `pendingPrefill()`/`activeDecoding()`. `dbg_cuda sched` harness (`schedMode`) drives the ragged running batch (admit→prefill→`.decoding`, ONE `decodeBatch`/tick over all decoders, EOS/budget evict→free→admit waiter, variable per-request lengths). `SCHED_GATE` = every scheduled stream token-identical to its isolated run incl. EOS-truncated length (`1add3049` 2a, `537306f2` 2b).
- **3a DONE** — the server's THREADING MODEL, proven in isolation (no HTTP transport yet). New `dbg_cuda serve <seqs> <ngen> <nslots> [model]` harness (`serveMode` + `serveWorker`/`serveClient`/`serveFinish` + `ServeCtx` in `src/dbg_cuda.zig`): ONE GPU **worker thread** runs the admit→prefill→`decodeBatch`→evict loop (reusing the SAME `Scheduler` API + `decodeBatch`), fed by **N concurrent producer threads** that `enqueue` independently and block for their own stream via a mutex+condvar handoff. Thread-safety: ALL GPU work is confined to the worker thread (the CUDA shim rebinds the context per call — `cuCtxSetCurrent` at every shim entry), so the only cross-thread state is the scheduler `pending` FIFO + the result/done registry, both under ONE mutex; slot prefill/decode/append/release are worker-only and lock-free. Per-sequence isolation (inc 1) means the nondeterministic concurrent admit/interleave order can't change any stream → exactly what the gate asserts. New host test `Scheduler concurrent enqueue under external mutex assigns unique slots`. Production `decodeStep`/`decodeBatch`/`prefillBatched` + the `sched`/`batch` harnesses UNTOUCHED. Commit `d7a188d7`.
- **3b DONE + validated** (this cycle) — the **first CUDA HTTP/SSE server**. New reusable engine `src/server/cuda_serve.zig::ServeEngine`: ONE GPU worker thread runs the admit→per-token-prefill→batched `decodeBatch`→evict loop (reuses `Scheduler` + `decodeBatch` from inc 1+2); a per-request `ReqChannel` registry (keyed by request id, under ONE mutex) delivers each request's tokens **INCREMENTALLY** as produced — the genuinely-new piece vs 3a (which delivered the whole stream only at finish). All GPU/slot work is worker-only + lock-free; handlers `submit` (enqueue + register + broadcast) and `nextChunk` (block until new tokens, copy out UNDER the lock since the worker may realloc `tokens`). `main.zig::runCudaServe` (reached when `zinc -m gemma` has NO `--prompt`): per-connection handler threads tokenize → `submit` → SSE-stream their own tokens; routes `/v1/completions`, `/v1/chat/completions`, `/health`. `ZINC_SERVE_DEBUG_IDS=1` = raw-token-id-in / token-id-out contract for exact gating; `ZINC_SCHED_EOS` overrides the stop token. Additive: production paths + the `serve`/`sched`/`batch` harnesses + the dbg `serveMode` UNTOUCHED. New gate `scripts/validate_cuda_serve.sh`. Commit `6a290c0f`.
- **3c DONE + validated** (this cycle) → **INCREMENT 3 COMPLETE**. The aggregate-throughput gate. Added `ServeEngine` throughput counters (`decode_steps`/`decode_tokens`/`decode_wall_ns` + `prefill_*` + `peak_batch`, worker-thread-only writes via `fetchAdd`, `/stats` handler reads via atomic load — lock-free, never contends the worker); `decode_tokens` SUMS the batch occupancy `ndec` per batched `decodeBatch` step, so over a phase `Δdecode_tokens/Δdecode_wall_s` = aggregate decode tok/s and `Δdecode_tokens/Δdecode_steps` = mean occupancy. New `GET /stats` route (`main.zig` → `statsJson`). New `scripts/throughput_cuda_serve.sh` builds + starts the server once, warms, then runs B=1/2/4/8 concurrent-client phases × ROUNDS, diffs `/stats` per phase. Additive: production `decodeStep`/`prefillBatched` + the `serve`/`sched`/`batch` harnesses UNTOUCHED. Commit `8d29967d`.
**Validated on the 5090** (this cycle, fresh isolated-cache build, `zinc` bin md5 `6c2ab9a41b5fa2d64acd936b88833fbe` ≠ 3b `6e625344`): catalog **5/5 serial token-correct** (12/12 each — production prompt path additive-only). 3c throughput gate (gemma-31b dense, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS): **srv decode tok/s 2.2/4.2/8.3/15.9 (1.00/1.95/3.81/7.34× vs B=1), occupancy 1.00/1.98/3.96/7.92 → TP_GATE_SCALING PASS + TP_GATE_OCCUPANCY PASS + TP_GATE PASS.** Near-linear aggregate scaling to B=8.
**(prior, 3b) Validated on the 5090** (`zinc` bin md5 `6e625344a3056dda5e3b27f0b14dc93f`): catalog **5/5 serial token-correct** (12/12 each — production prompt path unchanged). HTTP serve gate, B=4 mixed-length raw-token-id prompts, `nslots=2` (slot reuse), `NG=10`, budget-only EOS: **SERVE_HTTP_GATE_A PASS** (concurrent N=4 streams == the same prompts run one-at-a-time B=1 through the server) + **SERVE_HTTP_GATE_B PASS** (server B=1 streams == `dbg_cuda serve` SERVE_SEQ reference, itself == isolated decodeStep) — every client's SSE stream token-identical across all 4 seqs. Chain: concurrent HTTP == isolated == llama-correct (catalog). GOTCHA SOLVED: the agent-zinc box is **net-fenced to 127.99/16** → plain `127.0.0.1` TCP connect times out; the server binds `0.0.0.0` so reach it via a `127.99.x` address (gate uses `127.99.0.1`).
**Exact next step (pick one — perf levers now lead, per the head-to-head verdict):**
- **(A, DONE this cycle `2cb95904`) ~~PORT the B==1 matvec fast path to qwen `decodeBatch`~~** → done (qwen35-9b B1_TIMING 4.52→59.98 tok/s ~13.3×, BATCH_GATE+SANITY PASS, catalog 5/5). The whole catalog (gemma+qwen) now has the B==1 fast path. Remaining perf levers below.
- **(B, high-B win) the REAL serving-throughput lever — graph-capture the batched decode step** (Effort-25/27 playbook). The head-to-head B=8 gap is the batched-GEMM compute-inefficiency at B≥2 (where the B==1 matvec path does NOT apply) + per-launch overhead. Capture the `decodeBatch` step per distinct batch shape (shape changes on admit/evict → a small graph cache keyed by B). Util-gate the A/B. This is the lever that closes the aggregate gap vs `llama-server --parallel`.
- **(C, robustness, PARTLY DONE this cycle `7be561f2`) root-cause the sustained-throughput DEGRADATION** (round-2 collapse B=1 1.72→0.96, monotonic, not contention). ✅ Suspect #1 FIXED: the per-call `pos_buf`/`slots_buf` `createBuffer`/`freeBuffer` in `decodeBatch` (2 cudaMalloc+2 cudaFree EVERY step → fragmenting) are now persistent scratch (`pos_scratch`/`slots_scratch`, alloc'd in `allocSlotKv`/`allocSlotState`). REMAINING for lever C: (i) re-measure the sustained gemma-server run to CONFIRM the round-2 collapse is gone (the actual verification — 17GB reload, do it util-gated alongside lever B); (ii) if still degrading, attack suspect #2 = the per-row tail `commitAndWait`+download loop (B serial round-trips per step → batch into one command + one B-wide download) and the per-step host `allocator.alloc(f32, B*n_embd)` embed buffer (gemma `decodeBatch`). Scheduler audited clean (release deinits, slots reuse, no unbounded growth).
- **(D, model generality, deprioritized) WIRE QWEN INTO THE SERVER.** The qwen `decodeBatch` is correctness-complete for the WHOLE catalog (dense qwen35 + MoE qwen36; 4c+4d done). But `runCudaServe` (`main.zig`) + `src/server/cuda_serve.zig::ServeEngine` are still **gemma-dense-only** — they construct a `ForwardGemma` and call `ForwardGemma.decodeBatch`/`allocSlotKv`. (Per the head-to-head this just serves MORE models all 42× behind → do a perf lever first.) To serve qwen35/36 + MoE:
- Generalize `ServeEngine` (and `runCudaServe`'s model-load + worker loop) to hold EITHER a `ForwardGemma` OR a `ForwardCuda`, dispatched by architecture at load time (a tagged union or a thin vtable over `allocSlotState`/`decodeBatch`/per-token-prefill). The `Scheduler` + `ReqChannel` + worker threading are model-agnostic — only the GPU-forward calls differ.
- qwen's per-token prefill into a slot: the harness uses `decodeBatch` B=1 per prompt token into the slot (the same path the gemma server uses). Reuse that. `allocSlotState(n_slots, slot_ctx)` already exists on `ForwardCuda` (4a).
- Re-run `scripts/validate_cuda_serve.sh` + the throughput gate with a qwen model (qwen35-9b dense first, then qwen36-35b-a3b MoE) → concurrent streams token-identical to isolated + catalog 5/5.
- (SEPARATE perf lever, deprioritized) 4d's `moeFfnBlockBatchedDecode` still LOOPS rows per-token — the throughput sub-step is to batch the router GEMM `[B,n_experts]` + `build_expert_order` over the B·n_used (token,expert) pairs + per-tensor `dmmv_*_experts` over B tokens (the MoE analog of 4c). Plus the still-open B==1 matvec fallback / graph-capture levers.

**Perf lever (SEPARATE, still open, deprioritized behind inc 4):** 3c showed the batched-decode path's B=1 ≈2.2 tok/s vs `decodeStep` ≈8 (gemma-31b) — crossover ~B≈3-4. Levers: (a) B==1 matvec fallback in `decodeBatch`; (b) CUDA-graph-capture the batched step per distinct B; (c) batched-prefill-on-admit. Util-gate any A/B; do NOT async gemma decode.
**Open risks / notes:** (1) `decodeBatch` is ARGMAX-identical (not bit-identical) to `decodeStep` — gate is token-identical, PASSED. (2) 3b locking: handlers lock only around `submit`/`nextChunk`/`finish`; the worker locks only to `publish` a token + flip `done`; ALL GPU + slot work is worker-only lock-free. `nextChunk` copies tokens out UNDER the lock (the worker's `tokens.append` may realloc) — never read `chan.tokens` unlocked. (3) prompt token buffer + `ReqChannel` are handler-owned: the handler drains to `finished` (worker done touching them) BEFORE freeing — `defer finish` then `defer free` (LIFO). (4) prefill still per-token B=1 into one slot (no cross-admit batching). (5) gemma-31b reloads ~17GB/call → server startup ~2 min; bump readiness timeouts well past 120s (gate uses 360s). (6) do NOT async gemma decode (proven regression). (7) throughput NOT measured until 3c (gemma decode boost-saturated → A/B before then is noise). (8) **default `dbg_cuda` model is QWEN** — pass the gemma path to `serve`/`sched`/`batch`. (9) gate-script footgun (FIXED): a bare `wait` also waits on the long-lived server bg job → hangs; `wait` only on the curl PIDs. (10) nohup buffers ~4KB → a backgrounded box script's tail can look stalled mid-run; check `ps wchan` + output files, not just the log tail. (11) serving is gemma4-dense-only (qwen/MoE serving = inc 4; `runCuda` errors clearly for qwen server mode). (12) net-fence: use `127.99.x` not `127.0.0.1` to reach the local server. (13) **NEW (3c): the batched-decode path is per-step SLOW vs the optimized `decodeStep`** — B=1 through `decodeBatch` ≈ 2.2 tok/s vs production single-stream gemma-31b ≈ 8 tok/s. Batching wins on AGGREGATE (B=8 → 15.9 tok/s = 7.34× the B=1 batched path) but NOT yet per-stream; crossover vs production single-stream is ~B≈3-4. `decodeBatch` has no CUDA-graph capture and at B=1 pays GEMM-vs-matvec overhead. This is the next perf lever (see Exact next step #2), NOT a correctness issue — TP_GATE (scaling + occupancy) PASSED and catalog stays 5/5. (14) 3c gate is server-side-counter-based (`/stats` diff) so it's robust to curl-start jitter; it does NOT yet compare vs `llama-server --parallel B` (left for a later head-to-head once the per-step path is competitive).

## Increments (ORDERED and DEPENDENT — do them in order; each is several cycles)

### Increment 1 — Batched-decode ISOLATED PROOF (gemma-31b dense, slot-based KV). THE ARCHITECTURAL CRUX.
Build a batched decode path for the **dense gemma-31b** (no SSM, no MoE — the cleanest forward), and PROVE it token-identical to N separate single-sequence runs. Sub-steps (commit each as it builds, all to `feat/e28-batching`; keep the production `decodeStep`/`prefillBatched` UNTOUCHED — batching is ADDITIVE behind a new entrypoint):
- **1a. Harness + slot KV.** Add a `dbg_cuda batch` subcommand (mirror the existing `dbg_cuda gen`/`gemm` harness pattern) taking B comma-sep token-id prompts; allocate slot-based KV `kv_k_slots[L] = [n_slots·max_ctx·kv_dim]` (small n_slots e.g. 4, modest max_ctx e.g. 2048 to keep VRAM/reloads cheap). No forward yet — just plumbing + a coherent single-slot smoke (B=1 must equal `gen`).
- **1b. Batched buffers + driver.** A `decodeBatch(tokens[B], positions[B], slots[B])` that reuses `BatchScratch` (B rows) for the GEMMs and runs the per-layer loop. Embed B rows, run norms/qkv/rope/o/ffn over B rows (all already B-capable), final norm + logits [B,vocab] + per-row argmax → B tokens.
- **1c. Per-sequence kv_write + the generalized attention kernel.** kv_write into each sequence's slot at its `positions[b]`. Add `gemma_attention_batched_seq` (or extend with a `positions[]`+slot-base arg): `seq_len = positions[b]+1`, K/V base offset by slot. Per-sequence causal + sliding window.
- **1d. THE PROOF.** Run B distinct prompts through `decodeBatch` for N steps (each advancing its own position), AND each prompt separately through `decodeStep`. **GATE: the B batched token streams must be TOKEN-IDENTICAL to the B serial streams.** Mixed positions (start sequences at different lengths) to exercise per-sequence `positions[]`. This proves no cross-contamination + correct per-seq KV/attention. Until 1d passes, increment 1 is NOT done.

### Increment 2 — Continuous-batching scheduler (host-side, ragged batch).
Wire `src/scheduler/scheduler.zig`: a running batch where sequences sit at **different positions** and **join/leave between steps**. Admit a new request (prefill it into a free slot via the existing single-sequence/prefillBatched path, then add it to the decode batch); evict on EOS or max_tokens and free its slot; admit a waiting request into the freed slot. Implement `pendingPrefill()`/`activeDecoding()` for real. **GATE: an interleaved arrival/exit schedule produces per-sequence outputs token-identical to isolated runs.**

### Increment 3 — Wire the server (the actual multi-tenant throughput win).
One GPU **worker thread** runs the continuous-batch loop; HTTP request threads enqueue into the scheduler and stream their tokens back (SSE) as their sequence produces them; relax/replace the global `generation_mutex`. **GATE: N concurrent HTTP clients get correct, isolated streams AND aggregate tok/s > the serialized baseline** (measure 1,2,4,8 concurrent; this is the headline number — compare to `llama-server --parallel`).

### Increment 4 — Extend to qwen (per-sequence SSM state) + batched MoE.
Per-sequence SSM recurrent state (a batch dim on the ssm-state buffers + the `ssm_delta_net` scan in `forward_cuda.zig`); batched MoE routing (reuse `build_expert_order` over the B tokens). **GATE: qwen35/36 + MoE catalog rows token-identical batched-vs-serial.**

### Increment 5 — Paged KV (memory efficiency → higher concurrency).
Replace slot-based fixed reservation with the existing `KvPagePool` block tables, so max concurrency isn't capped by `n_slots·max_ctx` VRAM. **GATE: correctness preserved + higher max concurrency at fixed VRAM.**

## Validation contract

- **The batched path is ADDITIVE.** The production single-sequence `decodeStep`/`prefillBatched`/server path must stay the default and stay correct at every commit. Add batched paths behind a new entrypoint / flag until increment 3 flips serving over — and even then, gate it.
- **Correctness gate (increments 1,2,4): batched == serial, TOKEN-IDENTICAL** per sequence (the `dbg_cuda batch` proof). Mixed positions required.
- **`scripts/validate_catalog.sh` must stay 5/5 token-correct** (`ZINC_GPU` = 5090 UUID) on the **serial** path every cycle — batching must not regress single-sequence inference.
- **Throughput gate (increment 3):** aggregate tok/s at B concurrent clients > serialized baseline, interleaved/util-gated, on the SAME 5090. Beat `llama-server --parallel B` is the bar.
- Isolated-cache builds (`ZIG_LOCAL_CACHE_DIR`+`ZIG_GLOBAL_CACHE_DIR`; verify the binary md5 changed or you are measuring stale code).

## HARD RULES

- Work in **this worktree** `/Users/stepan/Workspace/zinc-e28` on the **persistent branch `feat/e28-batching`** (accumulate increments here; push it each cycle). **Never** work in `/Users/stepan/Workspace/zinc` (main checkout) or `…/zinc-e26`/`…/zinc-e27`. **Never push to main.**
- **5090-pinned** (UUID `GPU-5126d018-ec86-be8b-1bf5-b5ac323d3350`): `export CUDA_VISIBLE_DEVICES=…` + `ZINC_GPU=…`. The box is **dedicated to this effort** now (the e26/e27 perf loops were stopped 2026-06-14). The 4090 (`GPU-e59a6fce-…`) is also free — use it for a parallel build/validate if helpful.
- Isolated box dir `~/zinc-e28`, **never** `~/workspace/zinc`. Box gotchas: `DECODE/PREFILL/GEN_IDS` print on **STDERR** (`2>&1`); `nohup … >FILE 2>&1 &` + poll the FILE (a backgrounded `ssh '… bash'` orphans the remote script); `pkill -f <pat>` self-matches the ssh argv (kill by PID); **gemma-31B reloads 18 GB/call (~45 s) and can WEDGE the WSL2 box's sshd** — keep B/max_ctx modest, batch your box work, and if SSH hangs past connect the box needs a human restart.
- **DO NOT async gemma decode** (boost-saturated, proven regression — Efforts 23/25). Orthogonal to batching; don't introduce it.
- ONE validated increment per cycle. Commit ONLY the scoped change. Update `## CURRENT STATE` (done-so-far + exact next step + risks) and append a Cycle log entry. Never commit host/IP/port. A blocked/negative cycle is still a logged finding — leave the tree clean.

## Cycle log

(append dated entries: cycle | increment+sub-step | change | built+md5-changed? | catalog 5/5 (serial)? | batched==serial proof result | branch/sha | exact next step)

- **2026-06-15 | VALIDATED BUILD (lever ii(a)) — extend `btok` token-batch matvecs B≤16 → B≤24 (toward the ~B≈27 roofline ceiling) | `kernels.cu`: +32 kernels (`dmmv_q4k/q6k/q5k/q8_0_btok17..24`); `forward_cuda{,_gemma}.zig`: pipeline arrays `[15]→[23]`, unified compile loop B=2..24, `decode_mrow` gate + `gemmDispatch(A)` btok guard `B≤16→B≤24` (both archs); `dbg_cuda batch` `MAXB 16→24`. ADDITIVE — production `decodeStep` untouched, `btok` rides default-on `decode_mrow`, each `btok` bit-identical-per-row to its `*_fast` matvec. CLEAN 5090 window (`GPU-5126d018`, 0% foreign, no e27 loops). | built isolated-cache, cuda-dbg md5 `a15e83bd772c8b6443ffd2ffb5b0ba29` (≠ prior HEAD `05741bd1` → new code in binary) | **catalog 5/5 ALL PASS** (serial, 5090, 12/12 each) | **B=24 BATCH_GATE:PASS + BATCH_SANITY:PASS for qwen35-9b AND gemma4-31b** (batched B=24 == solo B=1 == serial, 24 seqs mixed lengths 3–5, ngen 12). In-process boost-comparable A/B at B=24 (btok OFF→ON, same load): qwen35-9b 104.77→372.97 tok/s agg (~3.56×), gemma4-31b 44.34→122.31 (~2.76×) — far outside boost noise (B=24 previously hit the tile GEMM, now uses `btok24`). | `feat/e28-batching` `8936aa4a` | NEXT: lever (ii) near its ceiling (`btok` covers B=2..24, one short of the ~B≈27 roofline crossover; raising further has little headroom + growing `sum[B]` register pressure). Remaining decode levers: (a) a TRUE warp-tiled higher-B batched-GEMM (no 64-pad) for B>~27 if needed; (b) deeper per-step kernel FUSION (Effort-23 playbook) — the ~3× gap vs llama at B=8 is the per-step kernel, NOT launch overhead. Still-open: MoE graph WIN measurement pending a fully-idle-e27 window.**
- **2026-06-15 | VALIDATED BUILD (lever ii) — extend `btok` token-batch matvecs B≤8 → B≤16 (higher-concurrency serving regime) | `kernels.cu`: +32 kernels (`dmmv_q4k/q6k/q5k/q8_0_btok9..16`); `forward_cuda{,_gemma}.zig`: pipeline arrays `[7]→[15]`, unified compile loop B=2..16, `decode_mrow` gate `B≤8→B≤16` (both archs); `dbg_cuda batch` `MAXB 8→16`. ADDITIVE — production `decodeStep` untouched, `btok` rides default-on `decode_mrow`, each `btok` bit-identical-per-row to its `*_fast` matvec. CLEAN 5090 window (`GPU-5126d018`, 0% foreign util). | built isolated-cache, cuda-dbg md5 `05741bd15ad80a744779793ed4b6ba91` (≠ prior HEAD `6d46d4ae` → new code in binary) | **catalog 5/5 ALL PASS** (serial, 5090) | **B=16 BATCH_GATE:PASS + BATCH_SANITY:PASS for qwen35-9b AND gemma4-31b** (batched B=16 == solo B=1 == serial, 16 seqs mixed lengths 3–5, ngen 12). In-process boost-comparable A/B at B=16 (btok OFF→ON, same load): qwen35-9b 72.3→344.3 tok/s agg (~4.8×), gemma4-31b 33.0→134.7 (~4.1×) — far outside boost noise (B=16 previously hit the tile GEMM, now uses `btok16`). | `feat/e28-batching` `8eed1680` | NEXT (lever ii continues): (a) agg tok/s still drops past the btok ceiling (B>16) / at the ~B≈27 roofline crossover → raise the cap further OR build a true warp-tiled higher-B batched-GEMM (no 64-pad) for B>16; (b) deeper per-step kernel fusion (Effort-23 playbook) on the batched decode step. Graphs RESOLVED. Still-open: MoE graph WIN measurement pending a fully-idle-e27 window (qwen36-35b-a3b 18GB needs the 24GB cap free).**

- **2026-06-15 | MEASUREMENT FINDING (NEXT lever i, DENSE half) — clean-window `GRAPH_TIMING` A/B for the DENSE batched-decode CUDA-graph replay = NEUTRAL-to-slightly-NEGATIVE → flip UNWARRANTED, graph stays OPT-IN; NO code change | a clean 5090 window opened intermittently (e27 does discrete `cuda-dbg gen` calls on gemma-26b with gaps). Built HEAD `48c75f88` (no source change) + ran the in-process `dbg_cuda batch` `GRAPH_TIMING` A/B (graph OFF then ON in ONE model load = boost-comparable; mrow held ON) on qwen35-9b dense B=4, filtering to FULLY-clean rounds via the harness `f0`/`foreign_after` per-round guard. | built clean 5090, cuda-dbg md5 `6d46d4ae3d9b361abee2a1fa30f55c7d` (HEAD, unchanged source) | catalog serial correctness unchanged (no code change; HEAD already 5/5 last cycle) | **3 FULLY-CLEAN B=4 rounds (`f0=0`+`foreign_after=0`) ON/OFF agg tok/s: 282.95/289.67 (−2.3%), 257.39/263.43 (−2.3%), 258.44/279.43 (−7.5%) → graph ON consistently NEUTRAL-to-slightly-SLOWER, never a win** (contended rounds `f0=2` excluded; B=8 + MoE blocked — see below). CONFIRMS: the dense batched step is already launch-collapsed (`3c6ca650`) so graph replay adds no headroom (mirrors E25 serial). **DECISION: do NOT flip `batch_graph_on` default-on — the "win across BOTH archs" condition fails at dense alone; opt-in `ZINC_BATCH_GRAPH` is the correct shape.** MoE arm BLOCKED: qwen36-35b-a3b (18GB) + e27's gemma-26b (16GB) > 24GB shared cgroup cap (same user) → OOM; e27 near-continuous 5090 ctx → no clean window. Lever (iii) ALREADY DONE: `decodeBatch` auto-dispatches `decodeBatchGraph` when `batch_graph_on`; `ServeEngine` calls `decodeBatch` → server replays the graph under `ZINC_BATCH_GRAPH=1` with no wiring. Did NOT disturb e27. | `feat/e28-batching` (doc-only commit) | NEXT: graph-capture lever set RESOLVED (dense neutral/opt-in; MoE WIN pending a fully-idle-e27 window). Real ~3× gap vs llama = the PER-STEP KERNEL → (ii) higher-B GEMM efficiency (B>8 where `btok` doesn't apply) + deeper kernel fusion, NOT graphs.**

- **2026-06-15 | BUILD (NEXT lever ii) — EXTENDED CUDA-GRAPH CAPTURE to the MoE batched-decode step (qwen36-35b-a3b), opt-in `ZINC_BATCH_GRAPH`; correctness-complete + token-identical, GRAPH_TIMING contended (not a claim) | the dense batched step was already graph-captured (`dcf1250b`); brought MoE under capture. KEY ENABLER — no new sync work: after `4439e0be` (GPU-side expert-id ALL quants incl Q6_K) + `6516c010` (per-row sync collapse), the batched MoE routed+shared path already has NO host readback / NO `commitAndWait`, and `submit`/`waitPending` are NO-OPS under `capturing` (capture-path `submit` releases without populating `pending` → `n_pending` stays 0). The expert id is read GPU-side from `router_out_buf` (a device buffer), NOT a push constant → captured topology + push-constants invariant across steps → `cuGraphExecUpdate` no-op. CHANGES: (1) new `moe_graph_capturable` (computed once at init) = true iff EVERY MoE layer's gate/up/down are `expertsSupported` (q4k/q5k/q6k); (2) init: `ZINC_BATCH_GRAPH` arms MoE-capturable models too (was hard `n_experts==0`); (3) `use_graph` gate: MoE permitted when `moe_graph_capturable AND moe_shared_batched AND moe_collapse` (all ride `decode_mrow`); (4) `decodeBatchGraph` layer loop dispatches `moeFfnBlockBatchedDecode` for MoE layers; (5) `dbg_cuda` GRAPH_TIMING A/B generalized to `(n_experts==0 OR moe_graph_capturable)`. Opt-in; default OFF → serial + default `decodeBatch` byte-identical. | built clean 5090, isolated cache (`ZIG_LOCAL/GLOBAL_CACHE_DIR=/tmp/e28c_*`), cuda-dbg md5 `0907c59da5f95ef9b24ae83c617c75d4` (≠ dense-graph `1afd03bf`) | catalog **5/5 serial token-correct** (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b dense+SSM+MoE B=4 mixed positions (`760,6511,314,9338,369\|450,3271,310\|1102,323,1023,1024,7,8\|99,100,101`) `ZINC_BATCH_GRAPH=1 ZINC_BATCH_MROW=1` NG=20: `moe_capturable=true` + BATCH_GATE:PASS + BATCH_SANITY:PASS** (captured MoE graph == solo == serial, token-identical); **dense qwen35-9b graph still BATCH_GATE+SANITY PASS** (`moe_capturable=false`, dense byte-unchanged → no regression). GRAPH_TIMING CONTENDED (e27 PID `1075304` 5090 ctx): MoE B=4 OFF 62.29→ON 105.36 (+69%), dense B=4 OFF 275.51→ON 300.16 (+9%) — directional only, NOT a perf claim | `feat/e28-batching` `b33370f3` | NEXT: (i) MEASURE the graph WIN on a CLEAN 5090 window (re-run GRAPH_TIMING dense+MoE / wire `ZINC_BATCH_GRAPH` into `throughput_vs_llama.sh` when `foreign_on_gpu==0`; if real across both archs, flip `batch_graph_on` default-on — correctness already complete for the whole catalog); (ii) higher-B GEMM efficiency (B>8); (iii) wire `ZINC_BATCH_GRAPH` into `ServeEngine` (replay per distinct B in the live server).**

- **2026-06-15 | BUILD (NEXT lever i) — CUDA-GRAPH REPLAY for the DENSE batched-decode step (qwen35), opt-in `ZINC_BATCH_GRAPH`; correctness-complete + token-identical, throughput A/B neutral-under-contention (not a claim) | mirrored the serial `decodeStepGraph` onto `decodeBatch`. New `decodeBatchGraph` (`forward_cuda.zig`): `capturing=true` → `cuda_graph_begin` → record the batched layer blocks (they already ride the async `submit` no-sync path under `capturing` after the `3c6ca650` launch-collapse) + a captured B-row tail (rms→LM-head→argmax, `releaseCompleted` not `commitAndWait`) → `cuda_graph_end_launch` (update-or-instantiate cached exec + launch + ONE sync) → B-wide argmax D2H. The embed + per-seq pos/slot/token uploads run (sync) into device scratch in `decodeBatch` BEFORE the graph branch, and every per-seq scalar (positions/slots) is read from a DEVICE BUFFER not a push constant → topology AND push-constants INVARIANT across steps (cheaper than serial, where `pos` is a push constant) → `cuGraphExecUpdate` is a no-op; only device-buffer contents change. CAVEAT handled: batched attention smem sized at constant `slot_ctx*4` under `capturing` (was data-dependent `max_seq_len*4`) so the kernel-node shape stays invariant. Per-B cache `batch_graph[9]` lazily created; gated dense-only (`n_experts==0`, MoE router host-readback illegal mid-capture) AND `decode_collapse` (no commitAndWait in a captured region). Opt-in via env/`batch_graph_force`; default OFF → serial + default `decodeBatch` byte-identical. New `GRAPH_TIMING` A/B in `dbg_cuda batch`. | built clean 5090, isolated cache (`rm -rf .zig-cache` + `ZIG_LOCAL/GLOBAL_CACHE_DIR=/tmp/e28gc_*`), cuda-dbg md5 `1afd03bf7be98842854a44f578565836` (≠ collapse `64786348`) | catalog **5/5 serial token-correct** (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b dense B=4 mixed positions (`760,6511,314,9338,369\|450,3271,310\|1102,323,1023,1024,7,8\|99,100,101`) `ZINC_BATCH_GRAPH=1 ZINC_BATCH_MROW=1` NG=24: BATCH_GATE:PASS + BATCH_SANITY:PASS** (batched-graph == solo == serial, token-identical — captured replay bit-equivalent to the async chain); **GRAPH_TIMING B=4 OFF 294.87 → ON 291.65 tok/s agg NEUTRAL (−1.1%) — SINGLE CONTENDED-5090 read** (e27 PID `1061336` holds a 5090 context) → NOT a perf claim; dense step already launch-collapsed so graph headroom small (E25 serial parallel: graphs ~+8% on the async ring, on a CLEAN box) | `feat/e28-batching` `dcf1250b` | NEXT: (i) MEASURE the graph WIN on a CLEAN 5090 window (re-run GRAPH_TIMING / wire `ZINC_BATCH_GRAPH` into `throughput_vs_llama.sh` when `foreign_on_gpu==0`; if real, flip `batch_graph_on` default-on like the mrow flip — correctness-complete already); (ii) EXTEND graph capture to the MoE batched step (drop the `moeSharedExpertBatched`→`waitPending` under `capturing` → MoE rides the no-sync ring; all MoE routed layers already GPU-side-expert-id'd `4439e0be` + collapsed `6516c010`); (iii) higher-B GEMM efficiency (B>8).**

- **2026-06-14 | inc 1 / sub-step 1a | `dbg_cuda batch` serial-reference harness + additive slot-based KV (`allocSlotKv`/`freeSlotKv`/`slotKvOffsetBytes`/`slotKvSmoke`) on `ForwardGemma`; production path untouched | built clean on the 5090, fresh isolated-cache (rm -rf .zig-cache + ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), bin md5 `50fcc2043743369e5077bbffba4716b4` | catalog 5/5 serial token-correct (12/12 each; qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | `batch` `BATCH_SEQ0`==`gen` seq0 (369…) AND `BATCH_SEQ1`==`gen` seq1 (240017…) token-identical; `SLOTKV_SMOKE:ok` | `feat/e28-batching` | NEXT: 1b — `decodeBatch(tokens[B],positions[B],slots[B])` for dense gemma-31b reusing BatchScratch + the batched GEMMs; wire allocSlotKv in for real; B=1 sanity ==gen before 1c generalizes per-seq positions/attention.**
- **2026-06-14 | inc 1 / sub-step 1c (PERF/structural; inc 1 COMPLETE) | fused the per-row decode attention loop into ONE batched launch each via two new kernels — `rms_norm_rope_qkv_seq` (grid.y=b batched V/Q/K norm+RoPE+slot-KV-write at per-seq positions[]/slots[] device arrays) + `gemma_attention_batched_seq` (grid.y=b batched per-seq causal/SWA softmax over each row's own slot KV, seq_len=positions[b]+1). `attentionLayerBatchedDecode` rewritten to 4 batched launches submitted async on the shared stream (no per-row commitAndWait); `decodeBatch` uploads positions[]/slots[] u32 device buffers once + single tail waitPending. Block math copied verbatim → bit-identical to 1b looped form. Production paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `56cba1d62d2f73cdc9ef211712f83956` (≠1b 27e59fb8) | catalog 5/5 serial token-correct (12/12 each) | **B=5 mixed prompt-lengths (4/3/4/5/5), varying stream on seq3 (`954,236758,236813,107,236820,236758`): BATCH_GATE PASS + BATCH_SANITY PASS — token-identical fused-batched==solo==prod-gen across all 5 seqs** | `feat/e28-batching` `708e798a` | NEXT: Increment 2 — wire `src/scheduler/scheduler.zig` continuous-batching (admit/prefill into free slot → add to decode batch; evict on EOS/max → free slot; implement pendingPrefill/activeDecoding); GATE = interleaved arrival/exit schedule token-identical to isolated runs (extend the dbg harness, keep additive — don't touch the server mutex until Inc 3).**
- **2026-06-14 | inc 2 / sub-step 2a | wired `src/scheduler/scheduler.zig` as a real continuous-batching scheduler (additive): `pending` FIFO + `scratch`; new `enqueue`/`admitNext`/`hasFreeSlot`/`isIdle`; implemented `pendingPrefill()`/`activeDecoding()` for real (slot-IDs by state). New `dbg_cuda sched <seqs> <ngen> <nslots> [model]` harness (`schedMode`) drives a ragged running batch — staggered arrivals (seq j @ tick j·2), admit→per-token B=1 prefill into slot→`.decoding`, ONE `decodeBatch` over all decoders/tick (mixed positions), evict @ `ng` budget→free slot→admit waiter; nslots<nseq forces reuse. GATE `SCHED_GATE` = every scheduled stream token-identical to its isolated production run. Added a scheduler unit test. Production decode/server paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `318411d9a153985d859360b0db6f99af` (≠1c `56cba1d6`) | catalog 5/5 serial token-correct (12/12 each) | **4 seqs mixed lengths (4/3/5/3), staggered arrivals, varying streams seq2 `1023,1024,53100,…`/seq3 `236774×3,15462×5`: `nslots=2` (slots 0+1 both reused) SCHED_GATE PASS completed=4/4; `nslots=1` (all 4 serialize through ONE slot, max reuse) SCHED_GATE PASS completed=4/4** — token-identical to isolated runs | `feat/e28-batching` (pending push) | NEXT: 2b — EOS-driven eviction w/ variable per-request gen lengths (re-run token gate); then Increment 3 — GPU worker thread runs this admit/decode loop, HTTP threads enqueue + SSE-stream, relax `generation_mutex`; GATE = N concurrent clients correct+isolated AND aggregate tok/s > serialized (beat `llama-server --parallel`).**
- **2026-06-14 | inc 2 / sub-step 2b | EOS-driven eviction + VARIABLE per-request gen lengths in `schedMode` (additive, harness+host-test only): pick an EOS token id (env `ZINC_SCHED_EOS`, else auto = seq0's mid-stream token; `maxInt` = pre-2b budget-only) applied to BOTH the isolated reference and the scheduled run via `req.shouldStop(eos)`; truncate references at first EOS (`serial_len[]`), record scheduled length in `finishSched` (`sched_len[]`), gate now requires token-identical streams INCLUDING matching EOS-truncated length → sequences leave the running batch at DIFFERENT ticks (mid-decode eviction → free slot → admit waiter while siblings keep decoding). New host-only scheduler unit test (EOS eviction frees a slot for a waiter). Production decodeStep/decodeBatch/server UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `59f0db87a85b50796cef3fc2c39f89b0` (≠2a `318411d9`) | catalog 5/5 serial token-correct (12/12 each) | **`ZINC_SCHED_EOS=108`, 4 chat-template-shaped prompts: SEQ0 stops MID-DECODE at len=6 (`7611,1492,573,2134,235336,108`) while SEQ1/2/3 run full 12 → `nslots=2` SCHED_GATE PASS completed=4/4 (seq0 evicts mid-flight, slot reused by waiter); `nslots=3` SCHED_GATE PASS completed=4/4 — all token-identical incl. length.** auto-eos (constant synthetic streams→first-token EOS): lengths (1,1,12,12), `nslots=1`+`nslots=2` SCHED_GATE PASS. host scheduler/request unit tests 13/13 | `feat/e28-batching` `537306f2` | NEXT: Increment 3 — GPU worker thread runs the admit→prefill→decodeBatch→evict loop (reuse Scheduler API + decodeBatch); HTTP threads enqueue + SSE-stream their own tokens; relax/replace `generation_mutex` (routes.zig:351/554/605). GATE = N concurrent clients correct+isolated AND aggregate tok/s > serialized (beat `llama-server --parallel B`). First cycle to touch the server — keep serial catalog green + a single-seq fallback.**
- **2026-06-14 | inc 3 / sub-step 3a (first cycle toward wiring the server) | CONCURRENT SERVING-ENGINE PROOF. Finding: on CUDA there is NO HTTP server yet (`main.zig:1662` — prompt-mode only; `routes.zig`+`forward.zig::InferenceEngine` are Vulkan/Metal and don't use ForwardGemma) → Inc 3 = stand up a CUDA serving path, decomposed 3a(engine core)/3b(HTTP-SSE)/3c(throughput gate). 3a builds the server THREADING MODEL in isolation: new `dbg_cuda serve` harness (`serveMode`+`serveWorker`/`serveClient`/`serveFinish`+`ServeCtx`) — ONE GPU worker thread runs the admit→prefill→decodeBatch→evict loop (reuses Scheduler API + decodeBatch), N concurrent producer threads enqueue + receive their own stream via mutex+condvar. GPU work confined to the worker thread (CUDA shim rebinds ctx per call); only `pending` FIFO + result registry cross-thread, under one mutex; slot work lock-free. New host test 'concurrent enqueue under external mutex assigns unique slots'. Production decode/harness paths UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), bin md5 `bfc7399fc2f589b89ddb287dabf0feff` (≠2b `59f0db87`) | catalog 5/5 serial token-correct (12/12 each) | **`serve` B=4 mixed-length prompts, nondeterministic concurrent enqueue order (req ids 1,3,2,4) → `nslots=2` (slot reuse) SERVE_GATE PASS 4/4; `nslots=4` (full batch) PASS 4/4; `nslots=2` budget-only PASS 4/4 — every client stream token-identical to its isolated single-seq reference incl. length.** host scheduler/request tests 14/14 | `feat/e28-batching` `d7a188d7` | NEXT: 3b — HTTP/SSE transport + a CUDA serve entrypoint (bring up a minimal CUDA HTTP path OR factor the 3a worker into a reusable serve module; per-connection enqueue + SSE-stream that request's own tokens; reuse src/server/http.zig). Keep CLI prompt path + serial catalog green. Then 3c — aggregate-tok/s gate vs `llama-server --parallel B`.**
- **2026-06-14 | inc 1 / sub-step 1b (+1d correctness) | `ForwardGemma.decodeBatch(tokens[B],positions[B],slots[B],out[B])` + `attentionLayerBatchedDecode`: batched DECODE for dense gemma — projections+FFN BATCHED over B rows (reuse `gemmDispatch`/`ffnBlockBatched`), attention inner LOOPED per row via single-token `rms_norm_rope_qkv`/`gemma_attention` on each seq's slot KV at its `positions[b]` (`seq_len=pos[b]+1`, slot-base offset). Harness extended: PASS A solo (B=1) + PASS B batched (B=nseq mixed positions) → `BATCH_GATE`(batched==solo) + `BATCH_SANITY`(solo==prod gen). Production decodeStep/prefillBatched UNTOUCHED. | built clean 5090, fresh isolated-cache, bin md5 `27e59fb85657f6c4981243bf7a467575` (≠1a) | catalog 5/5 serial token-correct (12/12 each) | **B=5 mixed prompt-lengths (4/3/4/5/5), varying stream on seq0 (`954,236758,236813,107,…`): BATCH_GATE PASS + BATCH_SANITY PASS — token-identical batched==solo==prod-gen across all 5 seqs** (also B=2 PASS) | `feat/e28-batching` `51b4af11` | NEXT: 1c is now PERF-ONLY — fuse the per-row attention loop into one `gemma_attention_batched_seq` (positions[]+slot-base, blockIdx.y=b) + batched per-seq kv-write, drop per-row syncs/async-submit; re-run the same proof (must stay PASS) + catalog. OR jump to Increment 2 (scheduler) — correctness crux already proven.**
- **2026-06-14 | inc 3 / sub-step 3b (first CUDA HTTP server) | stood up CUDA multi-tenant HTTP/SSE serving. New reusable `src/server/cuda_serve.zig::ServeEngine` (ONE GPU worker thread: admit→per-token-prefill→batched `decodeBatch`→evict, reusing Scheduler + decodeBatch from inc 1+2) with a per-request `ReqChannel` registry that streams each request's tokens INCREMENTALLY (the new piece vs 3a's whole-stream-at-finish): worker `publish`es each token under one mutex + broadcast; handler `nextChunk` blocks + copies out UNDER the lock (worker may realloc). `main.zig::runCudaServe` (reached when `zinc -m gemma` has NO --prompt): per-connection handler threads tokenize→submit→SSE-stream own tokens; routes /v1/completions, /v1/chat/completions, /health; `ZINC_SERVE_DEBUG_IDS=1` raw-token-id contract for exact gating, `ZINC_SCHED_EOS` overrides stop token. Additive — production decodeStep/prefillBatched + serve/sched/batch harnesses UNTOUCHED. New gate `scripts/validate_cuda_serve.sh`. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), `zinc` bin md5 `6e625344a3056dda5e3b27f0b14dc93f` | catalog 5/5 serial token-correct (12/12 each) | **HTTP serve gate, B=4 mixed-length raw-token-id prompts, nslots=2 (slot reuse), NG=10, budget-only EOS: SERVE_HTTP_GATE_A PASS (concurrent N=4 == sequential B=1 through the server) + SERVE_HTTP_GATE_B PASS (server B=1 == dbg_cuda serve reference == isolated decodeStep) — every client SSE stream token-identical across all 4 seqs; dbg SERVE_GATE PASS published=4/4** | `feat/e28-batching` `6a290c0f` | NEXT: 3c — aggregate-tok/s gate, B=1/2/4/8 concurrent clients vs serialized + vs `llama-server --parallel B` on the same 5090 (decode launch/bw-bound → expect ~linear scaling); util-gate the A/B. Box gotcha SOLVED: net-fenced to 127.99/16 → reach the local server via 127.99.x, NOT 127.0.0.1; gate-script `wait` must target curl PIDs only (a bare `wait` hangs on the server bg job).**
- **2026-06-14 | inc 4 / sub-step 4a (qwen batched-decode plumbing; mirrors 1a) | added ADDITIVE per-sequence slot state to `ForwardCuda` (qwen35/36 hybrid-SSM): `kv_k_slots`/`kv_v_slots` (attn layers `[n_slots·slot_ctx·kv_dim]`) + `ssm_conv_slots`/`ssm_state_slots` (ssm layers `[n_slots·conv_state_len]`/`[n_slots·ssm_state_len]`, zero-init) + `n_slots`/`slot_ctx`; `allocSlotState`/`freeSlotState` (re-callable, stub for the wrong layer type → uniform free), `slotKvOffsetBytes(slot,pos)`/`slotConvOffsetBytes(slot)`/`slotStateOffsetBytes(slot)`, `slotStateSmoke` (non-overlap round-trip for KV + conv ring + recurrent state across slots). NEW vs gemma's slot KV = the **per-slot SSM conv + recurrent state** (the hybrid-SSM piece). `dbg_cuda batch` qwen branch (was a bare skip) now allocs slot state + runs the smoke; the qwen serial `BATCH_SEQ` reference (the 4b target) is emitted + cross-checks ==`gen`. Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `86da205e7b19df9ec45502bc2e38d3af` | catalog 5/5 serial token-correct (12/12 each) | **`SLOTSTATE_SMOKE:ok` (slots=3 slot_ctx=512, KV+conv+recurrent non-overlap); qwen `BATCH_SEQ0` == `gen` `GEN_IDS` `11751,13,198,760,6511,314,9338,369` (serial reference == production decode)** | `feat/e28-batching` (commit pending push of this doc) | NEXT: 4b — `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])`: batched projections/FFN over B rows (reuse prefill GEMM path) + per-row attn (kv-write/`naive_attention` into the slot KV at `pos[b]`) + per-row SSM (conv1d/delta_net/gated_norm reading/writing each slot's own conv+recurrent state; per-slot conv ring offset). Use DENSE qwen35-9b for the 4b proof (MoE→4d). Gate: extend `dbg_cuda batch` qwen branch with PASS-A-solo/PASS-B-batched → `BATCH_GATE`+`BATCH_SANITY` token-identical, mixed positions; catalog stays 5/5.**
- **2026-06-14 | inc 4 / sub-step 4b (qwen batched decodeBatch — the qwen analog of gemma's 1b) | new `ForwardCuda.decodeBatch(tokens[B],positions[B],slots[B],out[B])` for DENSE qwen35 (hybrid-SSM): a per-row loop runs each sequence's forward against its OWN slot state via two slot-variant helpers copied VERBATIM from production `attentionLayer`/`ssmLayer` (block math identical → batched==N-serial token-identical by construction). `attentionLayerSlot(L,pos,slot)` = KV aliased to the slot region (`kv_base=slot·slot_ctx·kv_dim·4`), kv-write + `naive_attention` pos-relative within the slot (`seq_len=pos+1`, sinks + sigmoid-z gate), synchronous `commitAndWait`. `ssmLayerSlot(L,pos,slot)` = conv ring + recurrent state aliased to the slot (the new hybrid-SSM piece), `conv_off` DERIVED `=pos%(d_conv-1)`. MoE (`n_experts>0`) → `error.Unsupported` (4d). Added `resetState()` (zero prod single-seq SSM conv+recurrent + clear conv_off) so each harness SERIAL reference is truly single-sequence (qwen's unindexed SSM state would leak; no-op for gemma). `dbg_cuda batch` qwen branch: 4a smoke → PASS-A-solo / PASS-B-batched proof (per-token B=1 prefill into each slot, then decode all seqs together at MIXED positions). Production decodeStep/prefillStep/single-seq buffers UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `6c3e7194823e65524ce298fc057c727b` (≠4a `86da205e`) | catalog 5/5 serial token-correct (12/12 each) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions, varied seq0 stream (`5,6,24218,10,4838,1665,15,16`): SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial across all 3 seqs | `feat/e28-batching` `3b2268fc` | NEXT: 4c — FUSE the per-row loops into batched grid.y=b kernels (batch the dense qkv/o/ffn GEMMs over B rows via the prefill GEMM path = the amortization win; qwen-analog of `gemma_attention_batched_seq` for the per-seq attention inner incl. packed-gate deinterleave + per-head q/k norm; batched per-seq SSM conv/delta_net/gated_norm with per-row `conv_off`). Re-run the qwen proof (must stay PASS) + catalog 5/5. Then 4d batched MoE (qwen36-35b-a3b) lifts the Unsupported guard.**
- **2026-06-14 | inc 4 / sub-step 4c-step-1 (qwen batched dense GEMMs — the amortization win; qwen analog of gemma's 1b structure) | qwen `forward_cuda.zig` had NO batched-GEMM path on this branch (e26's qwen prefillBatched is on main, post fork-point) → BUILT it: new `gemm:[4]CudaPipeline`+`gemm_f32` (the generic `gemm_*_tiled_v2` kernels already in kernels.cu + used by gemma; token-major `[T,M]` out, `acc_mode` honored for the residual — verified `kernels.cu:1719`), `GemmPush` (byte-matches kernels.cu), `gemmDispatch(w,x,y,M,K,B,acc_mode)`, and a lazily-allocated token-major `[B,dim]` `DecodeBatch` scratch (dedicated `ssm_delta`/`ssm_y` `[B,d_inner]` buffers so SSM intermediates never alias with conflicting strides). `decodeBatch` REWRITTEN layer-major: embed all B rows into `db.hidden` slices (same embed kernel as serial → bit-aligned), then per layer the attn qkv/o + ssm qkv/z/alpha/beta/out + ffn gate/up/down run as ONE batched `gemm` each over B rows (each weight read ONCE); the per-seq inner (deinterleave/q-k-norm/RoPE/KV-write/softmax/gate; conv1d/delta_net/gated_norm) still LOOPS per row on row b's slice + its slot KV/conv/recurrent state (block math copied verbatim). FFN FULLY batched. Per-row argmax tail. 4b's `attentionLayerSlot`/`ssmLayerSlot` now superseded/dead (kept as reference). Production `decodeStep`/`prefillStep`/single-seq buffers UNTOUCHED — serial catalog never runs the new `gemm` pipelines. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `c84ed3f966ee5c15e6cc0299a7256957` (≠4b `6c3e7194`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions, varied seq0 stream (`4838,1665,15,16,17,18,19,20`): SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial across all 3 seqs (ARGMAX-identical: GEMM-vs-matvec reduction order, not bit-identical; token-gate PASSED) | `feat/e28-batching` (commit pending) | NEXT: 4c-step-2 — fuse the per-seq attention + SSM inner loops into batched grid.y=b kernels (drop the per-row `commitAndWait`s; the `db.*` buffers are already token-major `[B,dim]`); re-run the proof (stay PASS) + catalog. Then 4d batched MoE, then wire qwen into the server.**
- **2026-06-14 | inc 4 / sub-step 4c-step-2a (qwen batched-decode attention-inner FUSION; qwen analog of gemma's 1c) | fused the per-row decode-attention loop into TWO batched grid.y=B kernels (added to kernels.cu): `qwen_norm_rope_qkv_seq` (grid=(n_head+2·n_kv_head, B): per-(head,b) deinterleave from interleaved qfull + per-head q/k RMS-norm + RoPE + slot KV write at per-seq positions[]/slots[] + raw-gate extract; V copied raw — qwen does NOT normalize V; per-(head,b) math copied verbatim from deinterleave_qgate + rms_norm(N=head_dim) + rope + kv_cache_write) + `naive_attention_batched_seq` (grid=(n_head, B): per-(head,b) 3-pass softmax(QK^T)V + attention-sink rescale over each row's slot KV, seq_len=positions[b]+1; copied verbatim from naive_attention). `attentionLayerBatchedDecode` REWRITTEN: ONE command buffer / ONE commitAndWait (was B per-row commitAndWait) = batched pre-norm+Q(+gate)/K/V GEMMs → fused qkv → batched attn → sigmoid_mul over [B,q_dim] (existing kernel, token-major contiguous) → batched O proj. `decodeBatch` uploads positions[]/slots[] device u32 buffers once + max_seq_len (attn smem). SSM inner unchanged (4c-step-2b). Production decodeStep/prefillStep/single-seq UNTOUCHED (serial path never runs the new `_seq` kernels). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `96f70e14e5d25546c2196f4d9a5ba5fe` (≠4c-1 `c84ed3f9`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `369,11751,13,198,760,6511,314,9338`/SEQ1/SEQ2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `6a569531` | NEXT: 4c-step-2b — fuse the per-seq SSM scan inner (ssm_conv1d/delta_net/gated_norm) into batched grid.y=b kernels reading each slot's conv ring + recurrent state at per-row `conv_off=pos%(d_conv-1)`; collapse `ssmLayerBatchedDecode` to one commitAndWait; re-run proof (stay PASS) + catalog. Then 4d batched MoE, then wire qwen into the server.**
- **2026-06-14 | inc 4 / sub-step 4c-step-2b (qwen batched-decode SSM-inner FUSION → 4c COMPLETE) | fused the per-row decode SSM loop (conv1d / delta-net scan / gated-norm) in `ssmLayerBatchedDecode` into THREE batched grid.y/z=B kernels (added to kernels.cu) — the qwen analog of 4c-2a's attention fusion + the genuinely-new hybrid-SSM batched work (gemma had no SSM): `ssm_conv1d_seq` (grid=(ceilDiv(conv_channels,64), B): per-(channel,b) depthwise causal conv + SiLU over each row's slot conv ring `ssm_conv_slots[L]` at per-row state_offset = positions[b] % (d_conv-1); input/output token-major [B,conv_channels]; per-channel math copied verbatim from ssm_conv1d, state_offset derived from positions[b]) + `ssm_delta_net_seq` (grid=(dt_rank, head_v_dim, B): per-(head,row,b) gated delta-net scan over each row's slot recurrent state `ssm_state_slots[L]` at slot base slots[b]·ssm_state_len, ONE decode token per row — no n_tok loop; per-(h,row,b) math copied verbatim from ssm_delta_net, β/δ locals renamed to avoid the b=blockIdx.z clash) + `ssm_gated_norm_seq` (grid=(dt_rank, B): per-(head,b) gated RMS-norm over token-major rows, norm-weight indexed by head_base only NOT the row offset; copied verbatim from ssm_gated_norm). `ssmLayerBatchedDecode` REWRITTEN: ONE command buffer / ONE commitAndWait (was B per-row commitAndWait) = batched pre-norm + qkv/z/α/β GEMMs → ssm_conv1d_seq → ssm_delta_net_seq → ssm_gated_norm_seq → batched out-proj accumulate. `decodeBatch` passes the already-uploaded `pos_buf`/`slot_buf` device arrays (was host slices). The 4b/4c-1 per-row `ssmLayerSlot` reference is unchanged (still dead). Production decodeStep/prefillStep/single-seq UNTOUCHED (serial path never runs the new `_seq` kernels). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `ef484e4058f2df9e789a41f3dc362d6b` (≠4c-2a `96f70e14`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `4838,1665,15,16,17,18,19,20`/SEQ1 `544,445,49457,477,198,16,15,13`/SEQ2 `6,24218,10,4838,1665,15,16,17` all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `bd39d484` | NEXT: 4d — batched MoE over B rows (qwen36-35b-a3b): lift the `if (d.n_experts>0) return error.Unsupported` guard in `decodeBatch`; batch the MoE-FFN (router GEMM [B,n_experts] → top-k → `build_expert_order` over B·n_used pairs → per-tensor `dmmv_*_experts` expert matvec → weighted combine + shared expert per row); extend the `dbg_cuda batch` qwen path to qwen36-35b-a3b → stay BATCH_GATE/SANITY PASS + catalog 5/5. THEN wire qwen into `runCudaServe`/`ServeEngine` (currently gemma-dense-only).**
- **2026-06-14 | inc 4 / sub-step 4d (batched-decode MoE FFN over B rows → qwen `decodeBatch` correctness-complete for the whole catalog) | lifted the `if (d.n_experts>0) return error.Unsupported` guard in `ForwardCuda.decodeBatch`; the per-layer FFN now routes to a new `moeFfnBlockBatchedDecode` when `n_experts>0` (else `ffnBlockBatchedDecode`). KEY INSIGHT: the MoE FFN is STATELESS (no KV, no SSM recurrent state — reads `hidden`, routes to experts, accumulates back into `hidden`) so unlike attn/SSM it needs NO per-slot state and NO per-seq position → each of the B rows is an INDEPENDENT single-token MoE FFN. `moeFfnBlockBatchedDecode` LOOPS the rows, aliases row b's `db.hidden` slice, runs the production `moeFfnBlock` body VERBATIM on it via `moeFfnRowDecode` (rms_norm → router top-k → routed experts SwiGLU+down via the batched-q4k/q5k fast path OR the per-quant host-id fallback, byte-identical to production → weighted combine → shared expert), reading/accumulating into the row alias + a synchronous `waitPending` per row so the shared per-row scratch is safe to reuse → batched==N-serial token-identical by construction (mirrors the 4b→4c method). `dbg_cuda batch` qwen branch drops the MoE skip (was `BATCHDEC:skip MoE — 4d`). Production `moeFfnBlock`/`decodeStep`/`prefillStep` UNTOUCHED. NOTE: 4d is the CORRECTNESS step — the row loop does NOT yet read each expert weight once across B (the throughput sub-step = batch router GEMM [B,n_experts] + `build_expert_order` over B·n_used + per-tensor `dmmv_*_experts`, the MoE analog of 4c). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `53992af4f9f5b413d7b68a6006e5f163` (≠4c-2b `ef484e40`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b (256 experts, 8 active, SSM+MoE), 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok (slots=3 slot_ctx=512); BATCHDEC_SEQ0 `4838,1665,15,16,17,18,19,20`/SEQ1 `198,410,268,410,198,2,148929,152402`/SEQ2 `15,16,17,18,19,20,21,22` all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial through the SSM + MoE path (ARGMAX-identical; GEMM reduction order, token-gate PASSED) | `feat/e28-batching` `12b1c04e` | NEXT: wire qwen into the server (`runCudaServe`/`ServeEngine` are gemma-dense-only → generalize to dispatch ForwardCuda(qwen)/ForwardGemma by arch; re-run validate_cuda_serve + throughput with qwen35-9b then qwen36-35b-a3b). Then the MoE throughput sub-step + B==1/graph perf levers.**
- **2026-06-14 | inc 3 / sub-step 3c (INCREMENT 3 COMPLETE) | the aggregate-throughput gate — the HEADLINE. Added `ServeEngine` throughput counters (`decode_steps`/`decode_tokens`/`decode_wall_ns`/`prefill_tokens`/`prefill_wall_ns`/`peak_batch`, worker-thread-only `fetchAdd` writes, `/stats` handler reads via atomic load — lock-free); `decode_tokens` SUMS the per-step batch occupancy so `Δdecode_tokens/Δdecode_wall_s` = aggregate decode tok/s and `Δdecode_tokens/Δdecode_steps` = mean occupancy. New `GET /stats` route (`main.zig`→`statsJson`). New `scripts/throughput_cuda_serve.sh`: builds + starts the server once, warms, runs B=1/2/4/8 concurrent-client phases × ROUNDS, diffs `/stats` around each phase + prints external sanity tok/s. Additive — production decodeStep/prefillBatched + serve/sched/batch harnesses UNTOUCHED. | built clean 5090, fresh isolated-cache (ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), `zinc` bin md5 `6c2ab9a41b5fa2d64acd936b88833fbe` (≠3b `6e625344`) | catalog 5/5 serial token-correct (12/12 each) | **gemma-31b dense, ctx=1024, NG=96, nslots=8, 3 rounds, budget-only EOS: srv decode tok/s 2.2/4.2/8.3/15.9 = 1.00/1.95/3.81/7.34× vs B=1; occupancy 1.00/1.98/3.96/7.92 (batch fills) → TP_GATE_SCALING PASS + TP_GATE_OCCUPANCY PASS + TP_GATE PASS** — near-linear aggregate decode scaling to B=8 over the real HTTP server | `feat/e28-batching` `8d29967d` | NEXT: Increment 4 (qwen per-seq SSM state + batched MoE → serve qwen/MoE not just gemma-dense) — OR the SEPARATE perf lever: the batched-decode path is per-step slow (B=1 ≈2.2 vs production decodeStep ≈8 tok/s; no CUDA-graph capture, B=1 GEMM-vs-matvec overhead) so crossover vs single-stream is ~B≈3-4 → (a) B==1 matvec fallback, (b) graph-capture per distinct batch shape, (c) batched-prefill-on-admit. Util-gate any A/B. Box gotchas held: gemma 17GB reload ~2min server start; net-fence 127.99.x.**
- **2026-06-14 | PERF LEVER — B==1 matvec fast path in gemma `decodeBatch` | pivoted off inc-4 server-wiring to the perf lever the 2026-06-15 head-to-head flagged (zinc serving ~30-42× behind llama-server; per-STREAM path ~4× slower than `decodeStep`). Root cause (code-read): every B==1 `decodeBatch` step (ALL per-token prefill + single-client decode) ran its per-layer projection/FFN through the 64×64-tiled batched TC GEMM, which processes ONE row + wastes 63/64 of the tile (cuBLAS self-gates off at T=1<128). FIX (additive, surgical): `decode_b1` flag on `ForwardGemma` set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0`; `defer`-reset so a stale true never leaks into `prefillBatched`) + a guard at the TOP of `gemmDispatchA` (`if T==1 and self.decode_b1 → dmmvDispatch`, the SAME tuned matvec `decodeStep` uses; drop-in since every decode GEMM is acc_mode 0 + contiguous at T==1). Fused per-seq attn kernels unchanged (work at grid.y=1). Harness-only `decode_b1_force: ?bool` override + a `B1_TIMING` block in the `dbg_cuda batch` gemma branch time both paths in ONE model load (boost-comparable A/B). Production `decodeStep`/`prefillBatched` UNTOUCHED (serial never sets `decode_b1`). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `bb3982e196dca4c7b2cc0e2ed3701ed4` (≠4d `53992af4`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b, 3 seqs mixed positions (4/3/5): SLOTKV_SMOKE:ok; BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS (matvec path on by default); B1_TIMING in-process A/B (NG=64): matvec OFF 2.19 → ON 46.37 tok/s** (OFF matches the documented "B=1 ≈2.2" → the penalty is gone; the boost-comparable one-load RATIO is the clean claim, not the cross-context 46-vs-8) | `feat/e28-batching` `0fee12cc` | NEXT (perf-led): (A) port this B==1 fast path to qwen `decodeBatch` (`forward_cuda.zig`, same penalty); (B) graph-capture the batched decode step (the high-B aggregate lever — B≥2 GEMM inefficiency); (C) root-cause the sustained-throughput degradation (suspect: per-call `pos_buf`/`slots_buf` cudaMalloc/free in `decodeBatch` + per-row tail sync loop); (D, deprioritized) wire qwen into the server.**
- **2026-06-14 | PERF LEVER — B==1 matvec fast path PORTED to qwen `decodeBatch` (`forward_cuda.zig`) | the qwen analog of gemma's `0fee12cc`: qwen's 4c `gemmDispatch` over B rows runs at B==1 for every per-token prefill + single-client decode → 63/64 of the 64×64 tile wasted. FIX (additive, mirrors gemma): `decode_b1`+`decode_b1_force` flags on `ForwardCuda`; set in `decodeBatch` ONLY when `B==1` AND `b1MatvecOn()` (default-on, opt-out `ZINC_BATCH_B1_MATVEC=0` — SAME env knob as gemma; `defer`-reset); guard at the TOP of `gemmDispatch`: `if (B==1 and self.decode_b1) → dmmvDispatch` (the tuned matvec `decodeStep` uses). KEY DIFF vs gemma: passes `acc_mode` THROUGH (qwen O-proj/FFN-down/SSM-out use `acc_mode=1`, unlike gemma's all-0 decode GEMMs); `dmmvDispatch` honors acc_mode+a_offset(=0 contiguous at B==1) and handles every weight type incl. f32 (`dmmvIdx` f32→4=`dmmv_f32`; prod `decodeStep` SSM projections already route f32 through it). Fused `_seq` attn/SSM kernels work at B=1 unchanged. Harness `B1_TIMING` A/B added to the `dbg_cuda batch` qwen branch (mirror gemma). Production `decodeStep`/`prefillStep` UNTOUCHED (serial never sets `decode_b1`). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `07e46933f3f37a13dfeca2382f97fa29` (≠ gemma-fast-path `bb3982e1`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b, 3 seqs mixed prompt-lengths (4/3/5), mixed positions: SLOTSTATE_SMOKE:ok; BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS (matvec path on by default); B1_TIMING in-process A/B (NG=64): matvec OFF 4.52 → ON 59.98 tok/s (~13.3×)** | `feat/e28-batching` `2cb95904` | NEXT (perf-led): (B) graph-capture the batched decode step (the HIGH-B aggregate lever — B≥2 GEMM compute-inefficiency where the matvec path doesn't apply; Effort-25/27 playbook, graph cache keyed by batch shape); (C) root-cause the sustained-throughput degradation (suspect: per-call `pos_buf`/`slot_buf` cudaMalloc/free in `decodeBatch` + per-row tail sync loop — hoist to persistent scratch); (D, deprioritized) wire qwen into the server (`runCudaServe`/`ServeEngine` gemma-dense-only → dispatch ForwardCuda/ForwardGemma by arch). Both gemma+qwen `decodeBatch` now have the B==1 fast path → no low-B per-stream penalty for the whole catalog.**
- **2026-06-14 | PERF/ROBUSTNESS LEVER (lever C, suspect #1) — hoisted per-step `decodeBatch` pos/slots scratch to PERSISTENT buffers in BOTH archs | root-cause work on the documented sustained-throughput DEGRADATION (round-2 collapse B=1 1.72→0.96, monotonic, not contention). Code-read confirmed suspect #1: every `decodeBatch` step did `buffer.createBuffer`×2 + `buffer.freeBuffer`×2 for the per-seq `positions[]`/`slots[]` upload (a cudaMalloc+cudaFree pair per decoded token) — interleaved with the serving loop's admit/evict slot churn this fragments the device allocator + degrades monotonically. FIX (additive, surgical, BOTH `ForwardGemma` + `ForwardCuda`): `pos_scratch`/`slots_scratch` `?CudaBuffer` fields alloc'd ONCE in `allocSlotKv`/`allocSlotState` (sized to `n_slots` = max batch ≥ B), freed in `freeSlotKv`/`freeSlotState`; `decodeBatch` now `assert(B<=n_slots)` + re-uploads into the persistent scratch (kernels read only first B entries) instead of malloc/free per step. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED (never touch slot scratch). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `9e0f6464778717b3aec5bf2eba85c4f8` (≠ qwen-fast-path `07e46933`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b 3 seqs mixed lengths (4/3/5): SLOTKV_SMOKE:ok BATCH_GATE:PASS + BATCH_SANITY:PASS; qwen35-9b 3 seqs mixed lengths: SLOTSTATE_SMOKE:ok BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial. Correctness-preserving + structurally removes the alloc-fragmentation source. | `feat/e28-batching` `7be561f2` | NEXT: (verify lever C) sustained gemma-server re-measure to CONFIRM the round-2 collapse is gone (17GB reload → do it util-gated alongside lever B); if still degrading, attack suspect #2 = per-row tail `commitAndWait`+download loop (B serial round-trips/step → one batched command + one B-wide download) + the per-step host embed `alloc(f32,B·n_embd)` (gemma). THEN lever B — graph-capture the batched decode step per distinct B (the HIGH-B aggregate lever). (D, deprioritized) wire qwen into the server.**
- **2026-06-14 | PERF/ROBUSTNESS LEVER (lever C, suspect #2) — batched the gemma `decodeBatch` TAIL into ONE command + ONE B-wide download + persistent embed host scratch | continues lever C (suspect #1 = per-step pos/slots device alloc/free, fixed `7be561f2`). Suspect #2 = the remaining per-step host/round-trip churn in `ForwardGemma.decodeBatch`: (a) the TAIL looped B rows each doing its OWN rms_norm→LM-head→argmax + `commitAndWait` + argmax `download` → B serial GPU round-trips per decoded token; (b) a per-step `allocator.alloc(f32,B·n_embd)`/free for the embed staging. FIX (additive, surgical, gemma only): (a) the tail now chains ALL B rows' rms_norm/LM-head/argmax into ONE command buffer + ONE `commitAndWait` + ONE B-wide `download` — on ONE stream the dispatches run strictly in order so reusing the single `norm_buf`/`logits_buf` across rows is hazard-free (row b+1's rms_norm can't run until row b's LM head consumed `norm_buf`); only the argmax OUTPUT is per-row → each writes its own slot of a NEW persistent `argmax_scratch[n_slots]` (aliased bi·4), downloaded once → bit-identical math, B→1 tail round-trip; (b) `embed_host[n_slots·n_embd]` persistent host buffer (alloc `allocSlotKv`, free `freeSlotKv`) replaces the per-step host alloc/free. Both sized to `n_slots` ≥ B. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED. | built clean 5090, isolated-cache (rm -rf .zig-cache + ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), cuda-dbg md5 `060d96fbfc4e64ff99a7b7a739cca831` (≠ suspect-#1 `9e0f6464`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b 3 seqs mixed lengths (4/3/5): SLOTKV_SMOKE:ok BATCHDEC_SEQ0/1/2 all gate=MATCH sanity=MATCH → BATCH_GATE:PASS + BATCH_SANITY:PASS** — token-identical batched==solo==serial | `feat/e28-batching` `bf4ad2a6` | NEXT: PORT this tail-batch + embed-scratch to the qwen `decodeBatch` (`forward_cuda.zig` has the SAME per-row tail loop + per-step embed alloc); THEN (verify lever C) sustained gemma-server re-measure to confirm the round-2 collapse is gone (17GB reload → bundle util-gated with lever B); THEN lever B — graph-capture the batched decode step per distinct B (the HIGH-B aggregate lever). (D, deprioritized) wire qwen into the server.**
- **2026-06-15 | INCREMENT 4 COMPLETE — wired qwen into the CUDA server (arch-dispatched serving + per-slot SSM reset) | pivoted from the perf levers to the remaining BUILD milestone. The qwen `decodeBatch` was already correctness-complete for dense+MoE (4a–4d) but `runCudaServe`/`ServeEngine` were gemma-dense-only. (1) `src/server/cuda_serve.zig`: new `Forward` union (`gemma: *ForwardGemma` | `qwen: *ForwardCuda`) dispatching `allocSlots`/`freeSlots`/`decodeBatch`/`resetSlot` by arch (slot-state alloc differs only in NAME — `allocSlotKv` vs `allocSlotState`; `decodeBatch` signatures identical); `ServeEngine.g` → `fwd: Forward`; the worker calls `fwd.resetSlot(slot_id)` for each freshly-admitted slot BEFORE per-token prefill. (2) `src/compute/forward_cuda.zig`: new `resetSlot(slot)` zeros a REUSED slot's SSM conv ring + recurrent state (the genuinely-new correctness piece — qwen's SSM recurrent state is ACCUMULATED not position-indexed, so a slot reused across requests when nslots<concurrent would inherit the prior request's state → corruption; attn slot KV needs none, it's position-overwritten like gemma; no-op for gemma). This reuse path was NEVER exercised before — the 4b–4d proofs used `dbg_cuda batch` which gives each seq its own fresh slot. (3) `src/main.zig`: `runCudaServe` takes `cuda_serve.Forward`; qwen server branch wired (was `error.ServerModeUnavailableOnThisBackend`). (4) `src/dbg_cuda.zig`: generalized the `serve` harness to qwen (Engine union dispatch + `resetSlot` on admit + `resetState()` between isolated references) so `validate_cuda_serve.sh` GATE B provides the isolated qwen reference = a 2nd independent slot-reuse-reset proof. Production `decodeStep`/`prefillStep`/`prefillBatched` + gemma serving UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), zinc md5 `8de51146e4ecab0f2d0e07ab49708f5b` (≠3c `6c2ab9a4`), cuda-dbg md5 `1213b5c191733cc6cc072f4a56b2db11` (≠ qwen-suspect#2 `aba47627`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b serving, nslots=2 < 4 clients (FORCES slot reuse), NG=10, ctx=1024: SERVE_HTTP_GATE_A PASS (concurrent N=4 == sequential B=1 through the server) + SERVE_HTTP_GATE_B PASS (server B=1 == `dbg_cuda serve` isolated reference == decodeStep) + SERVE_GATE PASS (published=4)** — every qwen stream token-identical across concurrent/sequential/isolated → slot-reuse SSM reset correct. The CUDA server now serves the WHOLE catalog. Increments 1+2+3+4 COMPLETE. | `feat/e28-batching` `d37623ee` | NEXT (perf-led — the head-to-head gap is the only thing between the proven architecture and beating `llama-server`): (B) graph-capture the batched decode step per distinct B (HIGH-B aggregate lever); OR verify lever C via the sustained re-measure; OR (E) the MoE-serving throughput sub-step (`moeFfnBlockBatchedDecode` still loops rows per-token — batch the router GEMM `[B,n_experts]` + per-tensor `dmmv_*_experts`). NOTE qwen serving inherits the SAME per-step kernel inefficiency at high B — serving more models doesn't close the head-to-head gap; the per-step kernel lever does.**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: small-B Q4_K token-batch matvec (`dmmv_q4k_btok`) for qwen `decodeBatch` → ~1.8–2× aggregate decode at B=2..8 | attacks the head-to-head's headline gap (batched decode COMPUTE-bound at small B: the 64×64 `gemm_q4k_tiled_v2` processes one token-row per tile column → wastes 56-62/64 row-slots, B=8 = 100% util but slow). NEW kernel `dmmv_q4k_btok<B>` (`kernels.cu`) = the TRANSPOSE of `dmmv_q4k_mrow`: reads each Q4_K weight row ONCE (dominant decode traffic) + amortizes its dequant across the B token x-vectors → bandwidth-bound, no tile waste. SAME Q4_K dequant arithmetic as `dmmv_q4k_fast` (bit-exact); `btok` at row b == `dmmv_q4k_fast` on x[b] bit-for-bit → ARGMAX-identical to the GEMM. Instantiated B=2..8 (pipeline idx B-2). `forward_cuda.zig`: `dmmv_q4k_btok[7]` pipelines + `decode_mrow` flag (set in `decodeBatch` ONLY when `2≤B≤8` AND `mrowMatvecOn()`; OPT-IN, default-off, `ZINC_BATCH_MROW=1`, `defer`-reset) + a `gemmDispatch` guard routing Q4_K proj/FFN/residual GEMMs through `btok` (honors `acc_mode`; x/y token-major contiguous); non-q4k weights fall through to the GEMM. Harness `BTOK_TIMING` in-process A/B (mrow OFF then ON in ONE model load). Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `16e98c778855b4140c33039b5016b7dd` (≠ inc4 `1213b5c1`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b B=3/4/8 with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial, btok exercised); BTOK_TIMING agg tok/s B=3 13.36→24.17 (1.81×), B=4 19.29→37.95 (1.97×), B=8 37.46→70.84 (1.89×)** — robust ~1.8–2× across 3 batch sizes | `feat/e28-batching` `28fc7352` | NEXT (pick one): (i) PORT `btok` to gemma `decodeBatch` (`forward_cuda_gemma.zig`, same small-B Q4_K GEMM penalty); (ii) extend `btok` to Q5_K/Q6_K/Q8_0 (residual O-proj/FFN-down still hit the GEMM → biggest single lever before default-on); (iii) flip `ZINC_BATCH_MROW` default-on after a sustained-server head-to-head re-measure vs `llama-server --parallel` confirms the win holds end-to-end through `ServeEngine`; (iv) graph-capture the batched step (orthogonal/stackable).**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: extended `btok` to Q5_K/Q6_K/Q8_0 (covers gemma ffn_down + qwen residual GEMMs) → small-B aggregate decode MULTIPLIES (gemma-31b B=4 13.0×, B=8 7.93×; qwen35-9b B=4 9.49×) | the Q4_K-only `dmmv_q4k_btok` left the non-Q4_K decode GEMMs (gemma-31b ffn_down is Q6_K — the largest dense weight; qwen O-proj/FFN-down/SSM-out on mixed-quant layers) still hitting the wasteful 64×64 tile GEMM at small B. Added `dmmv_q6k_btok<B>`/`dmmv_q5k_btok<B>`/`dmmv_q8_0_btok<B>` (B=2..8) to kernels.cu — each the token-BATCH transpose of its `*_fast` matvec (weight row + dequant read ONCE, amortized across B tokens) using the EXACT `dmmv_q6k_fast`/`dmmv_q5k_fast`/`dmmv_q8_0_fast` per-element dequant + the proven q4k_btok per-token-local-sum reduction → token-identical per row to the `*_fast` the serial decodeStep uses. Wiring (both archs): the `decode_mrow` guard in `gemmDispatchA`/`gemmDispatch` is now a `switch(w.info.type_)` over {q4_k,q6_k,q5_k,q8_0}→matching `btok[B-2]` (else fall through to GEMM); same OPT-IN `ZINC_BATCH_MROW=1` knob + `defer`-reset, qwen passes acc_mode through. Production decodeStep/prefillStep/prefillBatched UNTOUCHED (never set decode_mrow → new switch arm dead on serial). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `c1b20acd66cdd03e0ce6f9ebc6e95dcf` (≠ gemma-btok `f7c12c2d`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b B=4 (10,11,12,13|20,21,22|30,31,32,33,34|40,41,42,43) + B=8 with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical, Q6_K ffn_down via btok exercised); BTOK_TIMING B=4 8.64→112.29 (13.0×), B=8 16.83→133.47 (7.93×). qwen35-9b B=4: SLOTSTATE_SMOKE:ok, BATCH_GATE+SANITY PASS, BTOK_TIMING 19.24→182.64 (9.49×)** — big jump over Q4_K-only (gemma B=8 2.80× / qwen B=4 1.97×) | `feat/e28-batching` `a4b5eb34` | NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED-server head-to-head vs `llama-server --parallel` confirms the in-process win holds end-to-end through `ServeEngine`; (ii) graph-capture the batched decode step per distinct B (orthogonal — attacks per-launch overhead the matvec doesn't); (iii) MoE-serving throughput sub-step (`moeFfnBlockBatchedDecode` still loops rows per-token).**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: PORTED `dmmv_q4k_btok` to GEMMA `decodeBatch` (`forward_cuda_gemma.zig`) → ~2.8–3.3× aggregate decode at B=3..8 | mirrors the qwen port (`28fc7352`) onto `ForwardGemma`. gemma-31b dense decode hit the SAME small-B GEMM penalty (the 64×64 batched GEMM processes one token-row per tile column → wastes 56-62/64 row-slots → compute-bound on tile padding). The btok kernel was ALREADY in `kernels.cu` (generic `DmmvPush`, token-major x/y), so a pure wiring port: `dmmv_q4k_btok[7]` pipelines (B=2..8, idx B-2) + a `decode_mrow` flag (set in `decodeBatch` ONLY when `2≤B≤8` AND `mrowMatvecOn()`; OPT-IN, default-off, `ZINC_BATCH_MROW=1` — SAME env knob as qwen; `defer`-reset) + a `gemmDispatchA` guard (AFTER the B==1 matvec check, BEFORE the `gi` GEMM switch) routing Q4_K decode GEMMs through `btok`. KEY DIFFS vs qwen: gemma's decode GEMMs are ALL acc_mode 0 (the residual is the separate `rms_norm_residual`/`rms_norm_residual_scale`, NOT a GEMM accumulate — unlike qwen's O-proj/FFN-down/SSM-out) → plain default push; AND the guard skips when `a_preconv and use_tc_normf16` (the normf16 opt-in stages the fp16 norm into `act_f16` instead of materializing the f32 norm buffer btok reads — a no-op for the default/validated config where normf16 is off). Same Q4_K dequant as `dmmv_q4k_fast` → bit-identical per row → ARGMAX-identical to the GEMM. Harness `decode_mrow_force` + a `BTOK_TIMING` in-process A/B in `dbg_cuda batch`'s GEMMA branch. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache+/tmp/e28gc), cuda-dbg md5 `f7c12c2d65533fadc6dcc0db8ed4c43f` (≠ qwen-btok `16e98c77`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **gemma-31b B=3/4/8 with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial, btok exercised); BTOK_TIMING agg tok/s B=3 6.49→21.42 (3.30×), B=4 8.62→27.62 (3.20×), B=8 16.90→47.35 (2.80×)** — robust ~2.8–3.3× across 3 batch sizes (BIGGER than qwen's 1.8–2×: gemma-31b's larger matvecs waste more tile-padding). btok now lands on BOTH archs → the whole catalog's Q4_K decode GEMMs have the small-B fast path. | `feat/e28-batching` `5ef499dc` | NEXT (pick one): (i) extend `btok` to Q5_K/Q6_K/Q8_0 (gemma-31b ffn_down is Q6_K + some qwen residual GEMMs are not Q4_K → still hit the tile-padding GEMM; the biggest single lever before default-on); (ii) flip `ZINC_BATCH_MROW` default-on after a sustained-server head-to-head re-measure vs `llama-server --parallel` confirms the win holds end-to-end through `ServeEngine`; (iii) graph-capture the batched step (orthogonal/stackable).**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: batched the MoE SHARED EXPERT over B rows (rides `btok`) → qwen36-35b-a3b decode B=4 +7.0% / B=8 +14.6%, token-identical | the MoE-serving throughput sub-step (NEXT iii). The shared expert (gate/up/down) is a DENSE FFN run identically per row → `moeFfnBlockBatchedDecode` read its weights B× redundantly. Restructured (decode-batch path only): split each row into router+routed-experts (new `moeFfnRoutedRowDecode` = the router+routed half of `moeFfnRowDecode` VERBATIM but reading its precomputed `db.ffn_norm` slice, no per-row rms_norm) accumulating routed output into its `db.hidden` row, THEN run the shared expert ONCE batched over all B rows (new `moeSharedExpertBatched` — gate/up/down as ONE `gemmDispatch` each over [B,n_embd] → each shared weight read once; `swiglu` over [B,sf]; per-row sigmoid·down scale stays element-wise since the weight reads are batched). Batched pre-norm computed up front (one launch over B) before routed accumulation overwrites `db.hidden`. KEY FINDING: it ONLY wins through `btok` — the batched shared GEMMs route through `gemmDispatch`, which at small B uses the 64×64 tile GEMM unless `decode_mrow`/btok is on → **−16% at B=4 vs per-row matvec** (tile-padding tax). So GATED on `decode_mrow` (`moe_shared_batched = decode_mrow and (force orelse env)`) → rides `ZINC_BATCH_MROW`, default mrow-off keeps the per-row matvec → no regression. Two new small [B,*] `DecodeBatch` scratch (`moe_down`, `moe_gate_scalar`); NO kernel/CUDA change (pure Zig wiring). ARGMAX-identical (GEMM reduction order). Production `decodeStep`/`prefillStep`/`moeFfnBlock` + full per-row `moeFfnRowDecode` (kept for A/B-off arm) UNTOUCHED. Harness `MOE_TIMING` A/B in `dbg_cuda batch`. | built clean 5090, isolated WARM cache (Zig-only rebuild over /tmp/e28gc), cuda-dbg md5 `7e5c7ce4cc5d2dc9c8fc70787e33b6b3` (≠ btok-extend `c1b20acd`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b B=4 (4838,1665,15,16\|544,445,49457\|6,24218,10,4838,1665\|198,410,268,410) + B=8 (slots=8) with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial through SSM+MoE); MOE_TIMING (ZINC_BATCH_MROW=1 in-process A/B) B=4 shared OFF 50.55→ON 54.10 (+7.0%), B=8 OFF 58.69→ON 67.23 (+14.6%)** — robust across 2 sizes, win GROWS with B. (Un-gated default-on was a NEGATIVE B=4 30.48→25.52 −16% → the `decode_mrow` gate is load-bearing.) | `feat/e28-batching` `b22898b4` | NEXT (pick one): (i) the BIGGER MoE lever — batch the ROUTED experts across B rows; sparse-MoE collision (8 of 256 active → little cross-row weight-amortization) means the real win is LAUNCH-collapse (B per-row `waitPending` → ONE tail sync via per-row [B,*] scratch; boost-starved qwen MoE decode is launch-bound; mirrors 4c-step-2; also unblocks MoE graph-capture); (ii) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED head-to-head vs `llama-server --parallel` (shared-expert win rides along); (iii) graph-capture the batched decode step per distinct B (orthogonal).**
- **2026-06-15 | GATE-HARNESS HARDENING + BLOCKED-GATE FINDING (default-on gate, NEXT i) — contention-guarded the head-to-head harness; root-caused the "round-2 collapse" as e27 5090 contention; default-on stays pending a clean window | picked up the in-progress `scripts/throughput_vs_llama.sh` work (ZINC0[mrow OFF]-vs-ZINCM[mrow ON]-vs-LLAMA, ROUNDS=3 + medians) — the recurring default-on gate. Found it ALREADY RUNNING on the box and producing UNTRUSTWORTHY numbers: ZINC0 B=1 collapsed 24.90→0.92 tok/s between rounds. ROOT CAUSE (confirmed via `nvidia-smi --query-compute-apps`): the box is SHARED with Effort-27, whose `validate_catalog`/A-B runs UNPINNED and holds a 5090 CUDA context CONTINUOUSLY (PID present on GPU-5126d018 even at 0% util between gen bursts) → co-running inference crushes serve throughput. So the documented "sustained round-2 collapse" (lever C) is CONFOUNDED by shared-box contention — it is NOT (purely) a zinc degradation, and the prior lever-C "collapse" measurements were likely contaminated too. FIX (additive, script-only, CANNOT touch any forward/serve code path): `foreign_on_gpu(server_pid)` counts compute procs on the 5090 (real UUID) that aren't our own server; `sweep` snapshots foreign-count before+after each (B,round) and tags the round `CONTENDED(f0,f1)`; the MEDIANS awk `next`s past CONTENDED rows → contended rounds are EXCLUDED, so the gate yields clean medians only from uncontended rounds (or honestly reports all-contended = no clean data, instead of silently averaging garbage). I must NOT edit the e27 worktree to pin it, and pausing e27's live A/B for a ~35min / 3×17GB-reload head-to-head is too disruptive ("KEEP BOTH"). Aborted the contaminated run, freed the 5090. | NOT built (script-only — no Zig/CUDA change; binary byte-identical to the 5/5-validated HEAD `93134ebb`) | catalog serial token-correctness preserved BY CONSTRUCTION (no forward/serve code delta; the script is not in any code path) | detection logic VALIDATED live on the box: `foreign_on_gpu` correctly returned `foreign_on_5090=1` against the live e27 5090 context; `bash -n` clean | `feat/e28-batching` (this commit) | NEXT: run the hardened gate in a CLEAN 5090 window (e27 paused/finished) — it now auto-excludes contended rounds → trustworthy ZINCM-vs-ZINC0-vs-llama medians; IF ZINCM robustly beats ZINC0 with no regression AND the collapse is gone clean → flip `ZINC_BATCH_MROW` DEFAULT-ON (the code increment: `mrowMatvecOn()` default true + `moeSharedBatchedOn` + `moe_collapse`). Alternatives if no clean window: (ii) graph-capture the batched decode step per distinct B (now viable for MoE post-collapse; win still needs a clean window); (iii) GPU-side expert-id path for the host-id FALLBACK (needs a NEW `dmmv_q6k_experts` kernel — not on this branch).**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: LAUNCH-COLLAPSED the MoE ROUTED-EXPERT per-row syncs (async path) → qwen36-35b-a3b decode B=4 +7.6% / B=8 +21.8% (isolated A/B), token-identical | the "BIGGER MoE-decode lever" (prior NEXT i). In `moeFfnBlockBatchedDecode` the B per-row `moeFfnRoutedRowDecode` calls each ended with a blocking `waitPending` (so the shared per-row scratch `router_*`/`gate_buf`/`up_buf`/`swiglu_buf`/`down_buf` was safe to reuse next row). For the pure-GPU async (`batched_experts`) path that sync is UNNECESSARY — confirmed by code-read that `cuda_begin_command` sets `m->stream = c->stream` so EVERY command rides the context's ONE shared `CUstream` strictly in-order (`commitAsync` just records an event); the next row's writes serialize AFTER this row's reads, no host round-trip needed. Dropped it for that path (new `moe_collapse` flag, default-on, `_force` for the harness A/B) → the B rows' commands queue back-to-back and the layer tail (`moeSharedExpertBatched`'s `waitPending`) drains them together, removing B blocking CPU↔GPU round-trips per MoE layer per token (boost-starved qwen MoE decode is launch-bound). Host-id FALLBACK path (`!batched_experts`, the 5 mixed-quant Q6_K-expert layers) KEEPS its per-row sync (it downloads `router_out_buf` to host-gather ids + reuses host `host_router_ids`). PURE sync removal, ZERO math change → ARGMAX-identical → batched==N-serial token-identical. Rides the existing `decode_mrow`/btok gate → default mrow-off path UNTOUCHED. Production `decodeStep`/`prefillStep`/`moeFfnBlock` UNTOUCHED. New harness `COLLAPSE_TIMING` in `dbg_cuda batch` A/Bs collapse OFF vs ON in ONE load (decode_mrow+moe_shared_batched both forced on). | built clean 5090, fresh isolated-cache (ZIG_LOCAL_CACHE_DIR=/tmp/lc-e28-collapse + ZIG_GLOBAL_CACHE_DIR=/tmp/gc-e28-collapse over rm -rf .zig-cache), cuda-dbg md5 `43b32ae948982b4fd4bdca84784f2323` (≠ shared-expert `7e5c7ce4`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b B=4 (4838,1665,15,16,17\|31,32,33,34,35\|101,202,303,404,505\|11,22,33,44,55) + B=8 (8 distinct prompts) with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial through SSM+MoE, 64 gen each); COLLAPSE_TIMING (in-process A/B) B=4 collapse OFF 58.74→ON 63.21 (+7.6%), B=8 OFF 70.14→ON 85.44 (+21.8%)** — robust across 2 sizes, win GROWS with B; 4090 cross-check B=4 54.00→61.43 (+13.8%). Cumulative MOE_TIMING (full per-row→shared-batched+collapse) B=4 53.82→63.84 (+18.6%), B=8 59.15→89.15 (+50.7%). Also UNBLOCKS MoE graph-capture (the per-row sync previously broke a single-graph MoE-step capture). | `feat/e28-batching` `6516c010` | NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON after a SUSTAINED head-to-head vs `llama-server --parallel` (BOTH the shared-expert AND collapse wins ride along); (ii) graph-capture the batched decode step per distinct B (now viable for the MoE step too); (iii) GPU-side expert-id path for the host-id FALLBACK layers (5 mixed-quant Q6_K-expert layers still sync per row → let them ride the collapse too).**
- **2026-06-15 | RAN the flip gate on qwen35-9b (co-tenant-safe small model) → BLOCKED by live e27 contention, BUT strong directional corroboration; hardened the harness with a self-reporting GATE VERDICT + small-model path (NEXT i) | the recurring default-on gate. Prior cycle hardened the harness to EXCLUDE contended rounds but never ran it; this cycle ran it co-tenant-safely. OPERATIONAL UNLOCK: the head-to-head's documented blocker was gemma-31b's 17GB × 3 sequential servers colliding with e27's 16GB gemma-26b under the 24GB cap → use `MODEL=$HOME/workspace/Qwen3.5-9B-Q4_K_M.gguf` (~5GB; fits alongside e27, reloads ~15s not ~45s, low WSL2-wedge risk). Ran `throughput_vs_llama.sh` (qwen35-9b, NG=64, ROUNDS=5, BLIST="1 2 4 8"): **EVERY round of all 3 arms (ZINC0/ZINCM/LLAMA) tagged CONTENDED(f0/f1≥1)** — e27 holds a 5090 CUDA context continuously (`foreign_on_gpu`≥1 always) → the contention-guard EXCLUDED all rounds → MEDIANS section EMPTY → GATE VERDICT BLOCKED → did NOT flip. Harness fix: empty-medians was SILENT (looks like a crash) → added an explicit GATE VERDICT line (`BLOCKED|USABLE-PARTIAL|CLEAN`) + a header note documenting the qwen35-9b co-tenant-safe gate path; verdict awk validated against the live data = "BLOCKED: 20/20 rounds CONTENDED, 0 clean". Script-only — touches NO forward/serve code. | NOT built (script-only — no Zig/CUDA change; binary byte-identical to the 5/5-validated HEAD `d892fe89`/`93134ebb`) | catalog serial token-correctness preserved BY CONSTRUCTION (no forward/serve code delta) | **DIRECTIONAL (all arms under IDENTICAL e27 contention → ratio-controlled, absolutes excluded as untrustworthy): ZINC0 mrow-OFF B=2/4/8 ≈ 9/17/28 tok/s; ZINCM mrow-ON ≈ 75-81/98-105/71-119; LLAMA (it DID serve qwen35) ≈ 140-168/136-224/152-310** → mrow-ON beats mrow-OFF ~8×/~6×/~3-4× END-TO-END THROUGH ServeEngine (corroborates the in-process btok 9.49× B=4), closing the llama gap from the old gemma mrow-OFF ~30-42× down to ~1.5-2× on qwen35-9b. Box left CLEAN (servers self-killed, no orphan zinc procs). | `feat/e28-batching` `fd7dc25e` | NEXT: the moment a CLEAN 5090 window opens (e27 paused/finished, `foreign_on_gpu==0`), re-run `MODEL=qwen35-9b ROUNDS=5 bash scripts/throughput_vs_llama.sh` — GATE VERDICT must read CLEAN/USABLE-PARTIAL with ZINCM medians > ZINC0 medians, no regression → FLIP `ZINC_BATCH_MROW` DEFAULT-ON (just `mrowMatvecOn()` default→true; serial 5/5 unaffected by construction). Alternatives if no clean window: (ii) graph-capture the batched decode step (WIN also needs a clean window); (iii) GPU-side expert-id for the FALLBACK layers (new `dmmv_q6k_experts`; correctness-gateable under contention, unmeasurable win).**
- **2026-06-15 | PERF LEVER (build-only, correctness-gated; NEXT iii) — GPU-side expert-id for the mixed-quant Q6_K MoE layers (new `dmmv_q6k_experts`) → the 5 host-id FALLBACK layers of qwen36-35b-a3b now ride the launch-collapse; token-identical | the residual MoE lever (prior NEXT iii), chosen because the flip gate (NEXT i) stays BLOCKED — e27 still holds a continuous 5090 context (gemma-26b `cuda-dbg gen` on the 4090 + device-0 leak, `foreign_on_gpu`≥1) so throughput is unmeasurable, but THIS lever's validation is correctness-only (contention-independent). In `moeFfnRoutedRowDecode` the async `batched_experts` (GPU-side, no readback) path was hard-gated to EXACTLY `gate=q4k ∧ up=q4k ∧ down=q5k`; the 5 mixed-quant layers (down→Q6_K by the UD-Q4_K_XL dynamic quant) failed it → fell to the host path (download `router_out_buf` → host-gather ids → per-row `waitPending` per MoE layer per token, the LAST per-row sync the `6516c010` collapse left behind). FIX (additive, surgical, qwen-MoE decode-batch only): (1) new `dmmv_q6k_experts` kernel = experts transpose of `dmmv_q6k_fast` (SAME per-element Q6_K dequant + `zinc_block_reduce_sum`; expert id read GPU-side from `expert_ids`; per-expert slice `id*slice+base` bytes; x shared gate/up or per-expert down; output slot-major `y[e*M+row]`) → row b is bit-for-bit `dmmv_q6k_fast` on x[b]; (2) `expertsSupported(t)`=`t∈{q4_k,q5_k,q6_k}` + `expertsDispatch` helper picking the `dmmv_*_experts` kernel by `dmmvIdx`; `batched_experts` is now `expertsSupported(wge)∧(wue)∧(wde)` — a STRICT SUPERSET (existing q4k/q4k/q5k layers dispatch IDENTICALLY → byte-identical; only q6k-containing layers change, going GPU-side; q8_0/f32 keep host fallback). Pure kernel-SELECTION, ZERO math change → ARGMAX-identical → batched==N-serial token-identical. Production `moeFfnBlock`/`decodeStep`/`prefillStep` + per-row `moeFfnRowDecode` (mrow-off arm) UNTOUCHED. | built clean 5090, fresh isolated-cache (rm -rf .zig-cache + ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), cuda-dbg md5 `1cf4a733411ffc7827898e2a0b53d624` (≠ collapse `43b32ae9`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen36-35b-a3b B=4 mixed lengths (4/3/5/6) (`15,16,17,18\|198,410,268\|15,16,17,18,19\|31,198,26,198,26,198`) with `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial through the now-GPU-side Q6_K layers); COLLAPSE_TIMING B=4 read 64.11→79.19 ON but CONTENDED/boost-noisy → NOT a perf claim** | `feat/e28-batching` `4439e0be` | NEXT (pick one): (i) flip `ZINC_BATCH_MROW` DEFAULT-ON — STILL the recurring gate, STILL pending a CLEAN 5090 window (`foreign_on_gpu==0`); re-run `MODEL=qwen35-9b ROUNDS=5 bash scripts/throughput_vs_llama.sh`, VERDICT CLEAN/USABLE-PARTIAL with ZINCM>ZINC0 → flip `mrowMatvecOn()` default→true; (ii) graph-capture the batched decode step per distinct B — now FULLY viable for MoE (no per-row sync anywhere on the routed path); WIN measurement also needs a clean window; (iii) MoE per-row-sync levers DRAINED — remaining decode levers are graph-capture (ii) or higher-B GEMM kernel efficiency.**
- **2026-06-15 | THE FLIP — `ZINC_BATCH_MROW` DEFAULT-ON (`mrowMatvecOn()` false→true, both archs) gated on a CLEAN 5090 head-to-head | a clean window opened; `MODEL=qwen35-9b ROUNDS=5 NG=64 throughput_vs_llama.sh` GATE VERDICT CLEAN 60/60 → ZINCM mrow-ON clean-beats ZINC0 at every batched B (B=2/4/8 = 4.14×/2.83×/2.29×, no B=1 regression), llama gap ~30-42×→~3×. | built 5090 cuda-dbg md5 `e3d3f77c` / zinc `6af7dd4a` | catalog 5/5 serial | qwen35-9b 3 seqs mixed positions BATCH_GATE+SANITY PASS on the flipped binary | `feat/e28-batching` `493a721d` (code) + `a7d210e4` (doc) | NEXT: graph-capture the batched decode step per distinct B (the ~3× per-step gap vs llama).**
- **2026-06-15 | PERF LEVER — VALIDATED WIN: LAUNCH-COLLAPSED the DENSE batched-decode layer chain (qwen `forward_cuda.zig`) → qwen35-9b B=4 +56.6% / qwen36-35b-a3b MoE B=4 +17.0% (clean-5090 in-process A/B), token-identical; ALSO the structural prerequisite for graph-capturing the batched step (NEXT i) | the 3 dense batched blocks (`attentionLayerBatchedDecode`/`ssmLayerBatchedDecode`/`ffnBlockBatchedDecode`) each ended with a blocking `cmd.commitAndWait()` so the shared per-layer scratch was safe to reuse next layer — UNNECESSARY: all commands ride ONE shared `CUstream` in-order (the `6516c010` MoE-collapse argument) → layer L+1's writes serialize after layer L's reads with no host round-trip. FIX (additive, surgical, qwen decode-batch only): new `decode_collapse` flag (default-on, `decode_collapse_force` for the harness A/B) routes those 3 blocks through the async `submit` ring instead of per-layer `commitAndWait`; drained ONCE at the decode tail via `drainPending()`. MoE-safe by construction: the MoE block's `moeSharedExpertBatched`→`waitPending()` drains the WHOLE ring (incl. the async attn/ssm commands) each MoE layer (in-order → correct). PURE sync removal, ZERO math change → ARGMAX-identical → batched==N-serial token-identical. Production `decodeStep`/`prefillStep`/`prefillBatched` UNTOUCHED. New `DCOLLAPSE_TIMING` A/B in `dbg_cuda batch` (holds mrow ON, toggles only `decode_collapse`). | built clean 5090, fresh isolated-cache (rm -rf .zig-cache + ZIG_GLOBAL_CACHE_DIR=/tmp/e28gc), cuda-dbg md5 `64786348ec6b58df0b90983357c90a22` (≠ flip `e3d3f77c`) | catalog 5/5 serial token-correct (12/12 each: qwen35-9b, qwen36-27b, qwen36-35b-a3b, gemma4-31b, gemma4-26b) | **qwen35-9b dense B=4 (`760,6511,314,9338,369\|450,3271,310\|1102,323,1023,1024,7,8\|99,100,101`) + qwen36-35b-a3b MoE B=4 (same prompts) mixed positions `ZINC_BATCH_MROW=1`: BATCH_GATE+BATCH_SANITY PASS (token-identical batched==solo==serial — MoE case proves the dense-collapse↔MoE interaction); DCOLLAPSE_TIMING (CLEAN 5090, 0 MiB foreign): dense qwen35-9b B=4 collapse OFF 189.11→ON 296.23 tok/s agg (+56.6%), MoE qwen36-35b-a3b B=4 OFF 77.66→ON 90.86 (+17.0%)** — mrow held ON both arms, isolating the per-layer sync removal | `feat/e28-batching` `3c6ca650` | NEXT: (i) GRAPH-CAPTURE the batched decode step per distinct B — now fully viable (dense blocks + MoE routed path are sync-free): mirror serial `decodeStepGraph` (capturing flag → `submit` no-sync path, `cuda_graph_begin`/`_end_launch`), per-step scalars already in persistent re-uploaded scratch → shape-invariant; CAVEAT: batched attention smem uses `max_seq_len*4` (data-dependent) → capture must size it to `max_ctx*4` like the serial `if (self.capturing)` trick (~line 1899). (ii) higher-B GEMM efficiency (B>8). Flip gate CLOSED; per-layer-sync lever set DRAINED.**
