# Effort 21 — CUDA Qwen 3.5 9B decode bring-up

> **Status:** **DECODE CORRECT on the 4090** — `forward_cuda` autoregressive greedy generation is token-for-token identical to llama.cpp CUDA (9/9 single-token; 6/6 prompts × 24 tok; long-context matches until a 0.15-logit near-tie). Baseline **~18–27 tok/s** correctness-first (sync-per-layer, **sync-bound: 65 `commitAndWait`/token**) — the async `CUstream`/`CUevent` ring is the lever to the **97 t/s** bar. Next: async ring; wire + validate batched prefill (Effort 20 GEMMs); 5090 correctness check.

Date: 2026-06-06

Pairs with **Effort 20 — CUDA Qwen 3.5 9B prefill** (`MULTI_HOUR_EFFORT_20_CUDA_QWEN35_9B_PREFILL.md`).
Shared bring-up plan & backend contract: **`docs/cuda-backend.md`**.

## Target model

Same as Effort 20: `qwen35-9b-q4k-m` — `unsloth/Qwen3.5-9B-Q4_K_M.gguf` (5.28 GiB, 8.95 B), `qwen35` **dense** SSM+attention hybrid, Q4_K_M. On the box: `~/workspace/Qwen3.5-9B-Q4_K_M.gguf`. Dense → **no MoE kernels**. Confirmed config: **32 layers (8 full-attn + 24 delta-net SSM, interval=4)**, hidden 4096, FFN 12288, 16h/4kv, head_dim 256, vocab 248320; SSM d_state 128 / dt_rank 32 / d_conv 4 / d_inner 4096; RoPE partial dim 64, freq 1e7. Decode DMMV quant set: Q4_K✓ Q8_0✓ F32✓ + Q5_K, Q6_K (todo).

## Why this effort exists

Decode is the per-token autoregressive path and the one ZINC is historically *good* at: on RDNA (Effort 17) ZINC decode **beat** llama.cpp on every scenario (95–97 vs 85 tok/s). So once the CUDA kernels are numerically correct, decode parity-then-win is the **more reachable** of the two hot paths. The decode perf unlock — an async stream/event command ring that overlaps GPU exec with CPU command-building — already paid off on Metal/RDNA (+22% on the 2b SSM decode) and ports directly to `CUstream` + `CUevent`.

### Baselines (the bar to clear)

| ref | hw | decode tok/s |
|---|---|---:|
| **llama.cpp CUDA** | **4090** | **97 ± 1** (tg128 clean; 120 ± 26 at tg256 when boosting) |
| **llama.cpp CUDA** | **5090** | **~156–226** (tg256 156±25 / tg128 226±293) — bench-unstable, re-measure |
| llama.cpp | RDNA4 (Effort 17) | 84.96–85.51 |
| ZINC vulkan | RDNA4 (Effort 17) | 95.39–96.91 |
| **ZINC cuda** | 4090 / 5090 | **0 (not wired)** |

Bar on the 4090 = **98 t/s** (and ZINC beat llama.cpp by ~12% on RDNA, so parity+ is realistic once correct). 5090 should exceed it; measure both.

## Bring-up path (correctness first, then decode perf)

1. **Backend wiring** — shared with Effort 20 (`build.zig` `cuda` enum + `configureCudaModule`; `gpu/interface.zig` `is_cuda`; three-way `main.zig`/`model_manager_runtime.zig`). Validate primitives + the 5 done kernels on **both** GPUs.
2. **Dense decode kernel set** (single-token, doc §5 minus MoE): templated **DMMV matvec** Q4_K✓/Q6_K/Q8_0/F32 (+accumulate mode) for Q/K/V/O, SSM in/out, LM head; `rms_norm`✓, `swiglu`✓, `scale_accumulate`✓, `sigmoid_scale_acc`✓; RoPE + qk_norm; **paged `kv_cache_write`** + naive single-query attention (`softmax(QKᵀ)V`, one query); the **`ssm_delta_net` recurrent step** (per-row register-resident state — *the* decode-critical SSM kernel) + `ssm_conv1d` (ring state) + `ssm_gated_norm`; `argmax`.
   - State buffers: paged KV pool; SSM conv ring `(d_conv−1)*inner` f32/layer + recurrent `dt_rank·…` f32/layer (sizes from config — confirm on box).
   - **M1 gate:** one token, token-for-token vs Metal/Vulkan reference.
3. **Decode perf (M3):** the async `commitAsync`/`wait`/`releaseCompleted` pending-command ring over `CUstream`+`CUevent` (the proven SSM-decode win); fused `rms_norm`+DMMV and fused gate/up; warp32 reductions tuned for sm_89 vs sm_120 occupancy.

## Measurement contract

- Metric: decode tok/s = generated_tokens / gen_time, **steady-state** (exclude first token), median of N warm reps.
- Correctness gate: token-for-token vs Metal/Vulkan reference greedy decode on a fixed prompt *before* recording tok/s.
- Devices: **4090 + 5090 separately**, UUID-pinned (4090 `GPU-e59a6fce-…`, 5090 `GPU-5126d018-…`) — index unreliable; see Effort 20.

## Cycle log

- **Cycle 0 (2026-06-06):** opened. llama.cpp CUDA decode baseline = **98.0 ± 1.0 t/s on the 4090** (clean, tg128). Backend at M0.5; `ssm_delta_net` recurrent step + attention not yet ported. **Next:** wire backend, reach M1 single-token decode correctness for the dense 9B, then port the async stream/event ring.
- **Cycle 1 (2026-06-06):** foundation validated on **both GPUs** (was 5090-only). `kernels_test` → **ALL PASS on 4090 (sm_89) and 5090 (sm_120)**: `rms_norm`, `dmmv_q4k`/`f32`/`q8_0`, `swiglu`, `scale_accumulate`, `sigmoid_scale_acc` (all ≤1.7e-5) — 7 of the dense-decode kernels green on both. **Decode-blocking ports remaining:** `dmmv_q6k`, RoPE+qk_norm, paged `kv_cache_write` + single-query attention, the SSM trio (`ssm_delta_net` recurrent step + `ssm_conv1d` + `ssm_gated_norm`), `argmax`; then `forward_cuda` + the async `CUstream`/`CUevent` ring.
- **Cycle 2 (2026-06-06):** backend **wired into the build system** (shared with Effort 20): `build.zig` `cuda` enum + `configureCudaModule` + Linux-gated `zig build cuda-smoke`; `gpu/interface.zig` `is_cuda` 3-way routing → `src/cuda/*`; `src/compute/forward_cuda.zig` scaffold. Default Metal build green (`zig build` exit 0). **`zig build cuda-smoke` → PASS on both GPUs** (5090 sm_120 / 4090 sm_89): NVRTC + vadd + dp4a async green. **Next (decode):** port the M1 decode kernels (`dmmv_q6k`, RoPE+qk_norm, paged `kv_cache_write` + single-query attention, the `ssm_delta_net` recurrent step, `argmax`) and wire `forward_cuda.decodeStep` into dispatch for the first correct token.
- **Cycle 3 (2026-06-06):** **9B config + quant confirmed on box** and folded into `forward_cuda.zig` (`Cfg`) + the Target section above. For decode this fixes the per-layer dispatch (8 full-attn vs 24 SSM layers, interval=4) and the DMMV set: the LM-head `argmax` reads **Q6_K** [4096×248320]; SSM-layer `ssm_out` + `attn_qkv` are **Q5_K** — so the decode hot path needs **dmmv_q5k + dmmv_q6k** beyond the three already green. SSM recurrent state per layer: conv ring `(4-1)×8192` + recurrent `dt_rank 32 · d_state 128 · …`.
- **Cycle 4 (2026-06-06):** decode baseline measured, but **bench is boost-sensitive on this shared Windows box**: 4090 tg128 = 97±1 (tight) yet tg256 = 120±26; 5090 swung 156–226 (it's the display GPU → contention). Clean **prefill** bar did land (pp2048 8487/10049, see Effort 20). **Decode bar = 97 t/s on the 4090** (defensible); the **5090 needs clock-pinning (sudo `nvidia-smi -lgc`) or a warmup protocol** for a tight number — a measurement-contract TODO. ZINC decode beat llama.cpp by ~12% on RDNA, so 97+ is the realistic 4090 target once the kernels are correct.
- **Cycle 5 (2026-06-07): M2 forward correctness ACHIEVED on the 4090 — token-for-token vs llama.cpp.** `forward_cuda` now predicts the **identical argmax** to llama.cpp CUDA on **9/9** diverse tokens (0,1,42,100,500,1000,5000,9999,50000) with **top-5 logits matching** (token 100: 220,151,198,8,13). The blocker was a single orchestration bug in the **attention gate layout**: qwen35 packs the gate **into `attn_q`** (output `2*q_dim`), but **interleaved per head** as `[Q(head_dim)|gate(head_dim)]` (`[Q0,g0,Q1,g1,…]`, stride `2·head_dim`) — *not* contiguous `[Q_all|gate_all]`. ZINC's contiguous split fed heads 8–15's data in as "gate" → `sigmoid(≈large)≈1` → attention never gated down → residual blew up **3.3× at the first attention layer (L3: 47.9 vs llama 14.6)**. **Fix:** project the full `2*q_dim` into `qfull_buf`, then a new `deinterleave_qgate` kernel splits it into contiguous `q_buf`/`gate_buf` (ref: llama.cpp `src/models/qwen35.cpp` `ggml_view_3d` Q@offset 0 / gate@offset `head_dim`, both stride `2·head_dim`). **How it was found:** built `src/dbg_cuda.zig` (per-layer residual-norm dump via the public `attentionLayerPub`/`ssmLayerPub`/`ffnBlockPub` hooks) + a llama.cpp CPU **eval-callback** reference (`l_out-N` per-layer norms); the diff showed L0–L2 (SSM) matching and **L3 (first attn) diverging**, isolating the gate. SSM + FFN + embedding + tail were all correct from the start. **Next: decode perf** — wire the autoregressive `decodeStep` loop (KV/conv state advance per token) + the async `CUstream`/`CUevent` command ring, then measure vs the 97 t/s 4090 bar.
- **Cycle 6 (2026-06-07): full autoregressive DECODE correctness validated + bottleneck quantified (4090).** Extended `dbg_cuda.zig` with a **`gen`** mode (greedy autoregressive generation from a prompt token-id list — prefills token-by-token via `decodeStep`, exercising pos>0 RoPE + multi-entry attention + SSM state carry) and a **`prof`** mode (decode bottleneck split). Built llama.cpp greedy refs on box: `/tmp/gen` (text→greedy ids) + `/tmp/probe` (forced-prefix top-5). **Results vs llama.cpp greedy (same prompt ids):** "The capital of France is" → **16/16 exact**; **6/6 prompts × 24 tokens exact** (code, numbers, narrative, science, repetition, imports); a 99-token-context → 48-gen run matched **28 tokens** then flipped on a **near-tie** (top-1 `39950` 22.658 vs top-2 `18922` 22.506, **gap 0.152**) and re-converged — i.e. fp arithmetic-order drift at a near-degenerate argmax, **not** a bug. **Full-vector numerical fidelity** (token 100, 248K-vocab logits via `dbg_cuda.zig logits` + `/tmp/logitdump`): **Pearson r = 0.99947, RMS logit diff 0.059, max 0.34, top-5 overlap 5/5, top-100 96/100** — numerically faithful (not bit-exact; the `*_fast` block=64 matvecs + Q4_K dequant order + deinterleave give ~0.06 RMS drift, which only flips the argmax at <~0.2-gap near-ties — self-consistent with the long-gen divergence). So pos>0 is correct: **NEOX RoPE + partial `rope_dim=64` + KV cache + GQA attention + SSM conv-ring/recurrent-state carry** all verified (the mRoPE `ggml_rope_multi` reduces to standard RoPE for 1D text positions — confirmed empirically). **Decode perf baseline (correctness-first, sync-per-layer):** ~18–27 tok/s (boost-sensitive box). `prof` split: **32-layer stack = 91%** of per-token time, embed+tail = 9%; **~65 `commitAndWait`/token @ ~0.85 ms each → the path is SYNC-bound, not compute-bound.** Removing ~64 of those syncs via one async submit/token (~45 ms) projects to ≈the 97 t/s bar — **confirms the async `CUstream`/`CUevent` ring is the dominant lever.** Note: `forward_cuda` is **decode-only** (single-token `decodeStep`, `n_tok=1`); batched **prefill** (the Effort-20 tiled GEMMs) is **not yet wired** into a forward path — that's the remaining correctness surface to validate once integrated. **5090 decode correctness not yet checked** (4090-only here to avoid the parallel 5090 research loop).
- **Cycle 7 (2026-06-07): cross-GPU + `*_fast`-wiring correctness validated via the regression gate; decode still sync-bound.** Ran `scripts/validate_cuda_decode.sh` on (a) the **5090 (sm_120)** with the correctness-first forward → **ALL CHECKS PASS** (9/9 tok, 5/5 prompts, r=0.99947), identical to the 4090 (sm_89) → no arch-specific kernel issues; (b) the loop's committed **`*_fast` decode wiring (`2f06cf3`, the "2.8×" change)** on the 4090 → **ALL CHECKS PASS**, fidelity **unchanged** (r=0.99947) → the `dmmv_*_fast` block=64 matvecs are token-correct + numerically faithful (the gate's first real use on a perf change — caught no regression). **Decode speed (4090, `*_fast`):** `gen` 64-tok burst **65.5 tok/s** (≈2.4× the base 26.9) vs `prof` sustained **24.5 tok/s** (40.8 ms/tok, 1.36× base) — the spread is this box's boost-sensitivity. The `prof` split still shows **65 `commitAndWait`/token**; only the per-commit compute shrank (0.854→**0.627 ms**) → decode is **still sync-bound**, so the async `CUstream`/`CUevent` ring (1 submit/token) remains the dominant lever *on top of* `*_fast`. **Follow-up characterization (gen sweep N=16…256, ×N repeats) shows decode is BIMODAL on this shared Windows/WSL2 box: ~66 tok/s boosted (consistent at N=16/64/128) vs ~22 throttled, bouncing even between identical runs (4090 idles at 210 MHz/7 W between runs).** The ~66 is the real `*_fast` rate (**≈68% of llama's 97**); the throttled samples (incl. the `prof` 24.5) are P-state drops *caused by* the sync-bound idle gaps between commits starving the GPU of sustained load. **Empirically confirmed by sampling the SM clock during each workload: sync-bound decode runs at a 525 MHz MEDIAN (bouncing 210↔2520) while sustained prefill (llama-bench pp2048) holds a 2520 MHz median — i.e. decode spends most of its time at ~⅕ of the boost clock** because the 65 commit gaps/token never let the GPU stay loaded. So the async `CUstream`/`CUevent` ring helps **twice**: it removes the 64 syncs AND keeps the GPU continuously loaded so it holds boost (~4–5× the median clock) — the prize is larger than pure latency removal. A clock-pinned run (`sudo nvidia-smi -lgc`) would give a tight steady number; the async ring is the real fix.
- **Cycle 8 (2026-06-07): async-ring prize QUANTIFIED via a dispatch microbench (de-risks the next decode lever, from the dispatch side).** Added `dbg_cuda.zig bench` mode (model-free, read-only): launches the same kernel N=300× under the current decode pattern (`commitAndWait` each → CPU blocks per dispatch) vs the async-ring pattern (`commitAsync` all on one stream + one ordered drain), on the actual WSL2 4090 via `command.zig`'s existing `commitAsync`/`wait`. **Result: async is 2.7–11.6× faster** (per-dispatch compute iters 200/1000/4000/16000 → 8.1× / 11.6× / 7.4× / 2.7× — the win shrinks as compute dominates, as expected). The **per-dispatch sync saving is a consistent ~0.4–0.5 ms** — the WSL2 CPU↔GPU `commitAndWait` round-trip + the boost-starvation it causes (Cycle 7). For the **65 commits/token** decode that overhead is the dominant per-token cost, so the async `CUstream`/`CUevent` ring is empirically the path toward/past the 97 t/s bar — corroborating Cycle 7 from the dispatch side. (Caveat: the synthetic FMA kernel ≠ the real decode matvecs, so the exact decode tok/s needs the actual ring wired into `forward_cuda.zig`; this sizes the prize and confirms the mechanism.) No `forward_cuda.zig` touch — the bench reuses the stable command primitives. (The box's `forward_cuda.zig` was set to committed `2f06cf3` for this run; the Mac working tree holds the loop's separate in-progress qwen36 **MoE routing** WIP — `softmax_topk`/`moe_weighted_acc`/expert buffers — left untouched.)
